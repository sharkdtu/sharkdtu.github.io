<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
    
    <entry>
      <title><![CDATA[从PageRank Example谈Spark应用程序调优]]></title>
      <url>http://sharkdtu.com/posts/spark-app-optimize.html</url>
      <content type="html"><![CDATA[<p>最近在做<a href="http://prof.ict.ac.cn/BigDataBench" target="_blank" rel="external">BigData-Benchmark</a>中PageRank测试，在测试时，发现有很多有趣的调优点，想到这些调优点可能是普遍有效的，现把它整理出来一一分析，以供大家参考。<a href="http://prof.ict.ac.cn/BigDataBench" target="_blank" rel="external">BigData-Benchmark</a>中的Spark PageRank采用的是Spark开源代码examples包里的PageRank的代码，原理及代码实现都比较简单，下面我简单地介绍下。<a id="more"></a></p>
<h2 id="PageRank基本原理介绍"><a href="#PageRank基本原理介绍" class="headerlink" title="PageRank基本原理介绍"></a>PageRank基本原理介绍</h2><p>PageRank的作用是评价网页的重要性，除了应用于搜索结果的排序之外，在其他领域也有广泛的应用，例如图算法中的节点重要度等。假设一个由4个页面组成的网络如下图所示，B链接到A、C，C连接到A，D链接到所有页面。</p>
<p><img src="/images/pagerank-graph-example.png" alt="pagerank-graph-example | center"></p>
<p>那么A的PR(PageRank)值分别来自B、C、D的贡献之和，由于B除了链接到A还链接到C，D除了链接到A还链接B、C，所以它们对A的贡献需要平摊，计算公式为:</p>
<p>$$ PR(A) = \frac {PR(B)} {2} + \frac {PR(C)} {1} + \frac {PR(D)} {3} \tag{1-1}$$</p>
<p>简单来说，就是根据链出总数平分一个页面的PR值:</p>
<p>$$ PR(A) = \frac {PR(B)} {L(B)} + \frac {PR(C)} {L(C)} + \frac {PR(D)} {L(D)} \tag{1-2}$$</p>
<p>对于上图中的A页面来说，它没有外链，这样计算迭代下去，PR值会全部收敛到A上去，所以实际上需要对这类没有外链的页面加上系数:</p>
<p>$$ PR(A) = d(\frac {PR(B)} {L(B)} + \frac {PR(C)} {L(C)} + \frac {PR(D)} {L(D)} + …) + \frac {1 - d} {N} \tag{1-3}$$</p>
<h2 id="Spark-PageRank-Example"><a href="#Spark-PageRank-Example" class="headerlink" title="Spark PageRank Example"></a>Spark PageRank Example</h2><p>Spark Examples中给出了一个简易的实现，后续讨论的相关优化都是基于该简易实现，所以并不一定可以用来解决实际PageRank问题，这里仅用于引出关于Spark调优的思考。下面是原始版本的实现代码，我们称之为V1。</p>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span><br><span class="line"> * Computes the PageRank of URLs from an input file.</span><br><span class="line"> * Input file should be in format of:</span><br><span class="line"> * URL         neighbor URL</span><br><span class="line"> * URL         neighbor URL</span><br><span class="line"> * URL         neighbor URL</span><br><span class="line"> * ...</span><br><span class="line"> * where URL and their neighbor URL are separated by space(s).</span><br><span class="line"> */</span></span><br><span class="line"><span class="keyword">val</span> lines = sc.textFile(inputPath)</span><br><span class="line"></span><br><span class="line"><span class="keyword">val</span> links = lines.map &#123; s =&gt;</span><br><span class="line">  <span class="keyword">val</span> parts = s.split(<span class="string">"\\s+"</span>)</span><br><span class="line">  (parts(<span class="number">0</span>), parts(<span class="number">1</span>))</span><br><span class="line">&#125;.distinct().groupByKey().cache()</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> ranks = links.mapValues(v =&gt; <span class="number">1.0</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> (i &lt;- <span class="number">1</span> to iters) &#123;</span><br><span class="line">  <span class="keyword">val</span> contribs = links.join(ranks).values.flatMap &#123;</span><br><span class="line">    <span class="keyword">case</span> (urls, rank) =&gt;</span><br><span class="line">      <span class="keyword">val</span> size = urls.size</span><br><span class="line">      urls.map(url =&gt; (url, rank / size))</span><br><span class="line">  &#125;</span><br><span class="line">  ranks = contribs.reduceByKey(_ + _).mapValues(<span class="number">0.15</span> + <span class="number">0.85</span> * _)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Force action, like ranks.saveAsTextFile(outputPath)</span></span><br><span class="line">ranks.foreach(_ =&gt; <span class="type">Unit</span>)</span><br></pre></td></tr></table></figure>
<p>上面的代码应该不难理解，它首先通过<code>groupByKey</code>得到每个url链接的urls列表，初始化每个url的初始rank为1.0，然后通过<code>join</code>将每个url的rank均摊到其链接的urls上，最后通过<code>reduceByKey</code>规约来自每个url贡献的rank，经过若干次迭代后得到最终的<code>ranks</code>，为了方便测试，上面代码29行我改成了一个空操作的action，用于触发计算。</p>
<h2 id="优化一-Cache-amp-Checkpoint"><a href="#优化一-Cache-amp-Checkpoint" class="headerlink" title="优化一(Cache&amp;Checkpoint)"></a><span id="opt1">优化一(Cache&amp;Checkpoint)</span></h2><p>从原始版本的代码来看，有些童鞋可能会觉得有必要对<code>ranks</code>做cache，避免每次迭代重计算，我们不妨先运行下原始代码，看看是否真的有必要，下图是指定迭代次数为3时的Job DAG图，其中蓝色的点表示被cache过。</p>
<p><img src="/images/pagerank-iter-3-dag.png" alt="pagerank-iter-3-dag | center"></p>
<p>从上图可以看到，<code>ranks</code>没有被cache，3次迭代计算是在一个job里一气呵成的，所以没必要对<code>ranks</code>做cache，因为从整个代码来看，在迭代循环里没有出现action方法，所以迭代循环中不会触发job，仅仅是组织RDD之间的依赖关系。</p>
<p>但是，一般来说迭代次数都比较大，如果迭代1000甚至10000次，上述RDD依赖关系将变得非常长。一方面会增加driver的维护压力，很可能导致driver OOM；另一方面可能导致失败重算，单个task失败后，会根据RDD的依赖链从头开始计算。所以从容错以及可用性来说，上述代码实现是不可取的。所幸，Spark提供了checkpoint机制，来实现断链及中间结果持久化。</p>
<p>使用checkpoint，我们来改造上述迭代循环，在每迭代若干次后做一次checkpoint，保存中间结果状态，并切断RDD依赖关系链，迭代循环代码改造如下：<br><figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line">...</span><br><span class="line"><span class="keyword">var</span> lastCheckpointRanks: <span class="type">RDD</span>[(<span class="type">String</span>, <span class="type">Double</span>)] = <span class="literal">null</span></span><br><span class="line"><span class="keyword">for</span> (i &lt;- <span class="number">1</span> to iters) &#123;</span><br><span class="line">  <span class="keyword">val</span> contribs = links.join(ranks).values.flatMap &#123;</span><br><span class="line">    <span class="keyword">case</span> (urls, rank) =&gt;</span><br><span class="line">      <span class="keyword">val</span> size = urls.size</span><br><span class="line">      urls.map(url =&gt; (url, rank / size))</span><br><span class="line">  &#125;</span><br><span class="line">  ranks = contribs.reduceByKey(_ + _).mapValues(<span class="number">0.15</span> + <span class="number">0.85</span> * _)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (i % <span class="number">10</span> == <span class="number">0</span> &amp;&amp; i != iters) &#123;</span><br><span class="line">    ranks.cache().setName(<span class="string">s"iter<span class="subst">$i</span>: ranks"</span>)</span><br><span class="line">    ranks.checkpoint()</span><br><span class="line">    <span class="comment">// Force action, just for trigger calculation</span></span><br><span class="line">    ranks.foreach(_ =&gt; <span class="type">Unit</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (lastCheckpointRanks != <span class="literal">null</span>) &#123;</span><br><span class="line">      lastCheckpointRanks.getCheckpointFile.foreach &#123; ckp =&gt;</span><br><span class="line">        <span class="keyword">val</span> p = <span class="keyword">new</span> <span class="type">Path</span>(ckp)</span><br><span class="line">        <span class="keyword">val</span> fs = p.getFileSystem(sc.hadoopConfiguration)</span><br><span class="line">        fs.delete(p, <span class="literal">true</span>)</span><br><span class="line">      &#125;</span><br><span class="line">      lastCheckpointRanks.unpersist(blocking = <span class="literal">false</span>)</span><br><span class="line">    &#125;</span><br><span class="line">    lastCheckpointRanks = ranks</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Final force action, like ranks.saveAsTextFile(outputPath)</span></span><br><span class="line">ranks.foreach(_ =&gt; <span class="type">Unit</span>)</span><br></pre></td></tr></table></figure></p>
<p>上述代码中每隔10次迭代，做一次checkpoint，并强制触发计算。一定要注意，在做checkpoint前，一定要对要checkpoint的RDD做cache，否则会重计算。这里简单描述下checkpoint的计算流程: 调用<code>rdd.checkpoint()</code>仅仅是标记该RDD需要做checkpoint，并不会触发计算，只有在遇到action方法后，才会触发计算，在job执行完毕后，会启动checkpoint计算，如果RDD依赖链中有RDD被标记为checkpoint，则会对这个RDD再次触发一个job执行checkpoint计算。所以在checkpoint前，对RDD做cache，可以避免checkpoint计算过程中重新根据RDD依赖链计算。在上述代码中变量<code>lastCheckpointRanks</code>记录上一次checkpoint的结果，在一次迭代完毕后，删除上一次checkpoint的结果，并更新变量<code>lastCheckpointRanks</code>。</p>
<p>为了方便测试，我每隔3次迭代做一次checkpoint，总共迭代5次，运行上述代码，整个计算过程中会有一次checkpoint，根据前面checkpoint的计算描述可知，在代码15行处会有两个job，一个是常规计算，一个是checkpoint计算，checkpoint计算是直接从缓存中拿数据写到hdfs，所以计算开销是很小的。加上最终的一个job，整个计算过程中总共有3个job，下面是测试过程中job的截图，注意图中对应的行号跟上面贴的代码没有对应关系哦。</p>
<p><img src="/images/pagerank-checkpoint-jobs.png" alt="jobs | center"></p>
<p>第一个job执行3次迭代计算，并将结果缓存起来，下面是第一个job的DAG:</p>
<p><img src="/images/pagerank-iter-3-dag-cache.png" alt="iter-3-dag-cache | center"></p>
<p>第二个job做checkpoint，由于需要checkpoint的RDD已经缓存了，所以不会重新计算，它会跳过依赖链中前面的RDD，直接从缓存中读取数据写到hdfs，所以前面的依赖链显示是灰色的:</p>
<p><img src="/images/pagerank-checkpoint-dag.png" alt="checkpoint-dag | center"></p>
<p>第三个job执行剩下的2次迭代计算，由于前3次迭代的结果已经做过checkpoint，所以这里的依赖链中不包含前3次迭代计算的依赖链，也就是说checkpoint起到了断链作用，这样driver维护的依赖链就不会越变越长了:</p>
<p><img src="/images/pagerank-after-checkpoint-dag.png" alt="after-checkpoint-dag | center"></p>
<blockquote>
<p>Tips: 对于迭代型任务，每迭代若干次后，做一次checkpoint</p>
</blockquote>
<p>到这里，我们有一个稍微比较稳定的版本了，我们称之为V2。但是，一般实际场景中，<code>links</code>可能会特别大，例如好友关系，就有近10亿的key，每个key对应的value平均应该也有100-200，全部缓存到内存对资源要求比较大，从之前文章<a href="http://km.oa.com/group/2430/articles/show/300304" target="_blank" rel="external">Spark Cache性能测试</a>的结论可知，我们可以选择<code>MEMORY_ONLY_SER</code>或<code>DISK_ONLY</code>的缓存方式来减少内存的使用，由于在YARN集群环境中磁盘资源是没有被隔离的，也就是说一台机器上的磁盘资源是多任务共享的，所以使用<code>DISK_ONLY</code>存在磁盘溢出的风险，还是建议使用<code>MEMORY_ONLY_SER</code>，并加上压缩参数<code>spark.rdd.compress=true</code>，这样可以大大降低内存的使用，同时性能不至于损失太多。在上面加了checkpoint的代码基础上，把所有使用cache的地方全部改成如下形式:<br><figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Submit conf: spark.rdd.compress=true</span></span><br><span class="line">links.persist(<span class="type">StorageLevel</span>.<span class="type">MEMORY_ONLY_SER</span>).setName(<span class="string">"links"</span>)</span><br><span class="line">...</span><br><span class="line">ranks.persist(<span class="type">StorageLevel</span>.<span class="type">MEMORY_ONLY_SER</span>).setName(<span class="string">s"iter<span class="subst">$i</span>: ranks"</span>)</span><br></pre></td></tr></table></figure></p>
<p>相同资源和参数下分别使用默认的<code>MEMORY_ONLY</code>和带压缩的<code>MEMORY_ONLY_SER</code>测试3次迭代的性能，下图是使用默认的<code>MEMORY_ONLY</code>方式缓存时，<code>links</code>在内存中的大小，可以看到<code>links</code>缓存后占用了6.6G内存:</p>
<p><img src="/images/links-string-cache.png" alt="links-string-cache | center"></p>
<p>改用带压缩的<code>MEMORY_ONLY_SER</code>的缓存方式后，<code>links</code>缓存后只占用了861.8M内存，仅为之前6.6G的12%:</p>
<p><img src="/images/links-string-cache-compress.png" alt="links-string-cache-compress | center"></p>
<p>通过在日志中打印运行时间，得到使用<code>MEMORY_ONLY</code>时运行时间为333s，使用<code>MEMORY_ONLY_SER</code>时运行时间为391s，性能牺牲了17%左右，所以使用<code>MEMORY_ONLY_SER</code>是以牺牲CPU代价来换取内存的一种较为稳妥的方案。在实际使用过程中需要权衡性能以及内存资源情况。</p>
<blockquote>
<p>Tips: 内存资源较为稀缺时，缓存方式使用带压缩的<code>MEMORY_ONLY_SER</code>代替默认的<code>MEMORY_ONLY</code></p>
</blockquote>
<h2 id="优化二-数据结构"><a href="#优化二-数据结构" class="headerlink" title="优化二(数据结构)"></a>优化二(数据结构)</h2><p>在上述PageRank代码实现中，<code>links</code>中的记录为url -&gt; urls，url类型为<code>String</code>，通常情况下，<code>String</code>占用的内存比<code>Int</code>、 <code>Long</code>等原生类型要多，在PageRank算法中，url完全可以被编码成一个<code>Long</code>型，因为在整个计算过程中根本没有用到url中的内容，这样就可以一定程度上减少<code>links</code>缓存时的内存占用。由于在我的测试数据中，url本身是由数字来表示的，所以在<a href="#opt1">优化一</a>V2代码的基础上再将<code>links</code>的定义改为如下代码，我们将该版本称之为V3:<br><figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">...</span><br><span class="line"><span class="keyword">val</span> lines = sc.textFile(inputPath)</span><br><span class="line"><span class="keyword">val</span> links = lines.map &#123; s =&gt;</span><br><span class="line">  <span class="keyword">val</span> parts = s.split(<span class="string">"\\s+"</span>)</span><br><span class="line">  (parts(<span class="number">0</span>).trim.toLong, parts(<span class="number">1</span>).trim.toLong)</span><br><span class="line">&#125;.distinct().groupByKey()</span><br><span class="line">links.persist(storageLevel).setName(<span class="string">"links"</span>)</span><br><span class="line">...</span><br></pre></td></tr></table></figure></p>
<p>经过测试发现，url改成<code>Long</code>型后，使用<code>MEMORY_ONLY</code>缓存时，如下图所示，<code>links</code>仅占用2.5G，相比为<code>String</code>类型时的6.6G，缩小了一半多。此外，url改成<code>Long</code>型后，运行3次迭代的时间为278s，相比为<code>String</code>类型时的333s，性能提升了17%左右。</p>
<p><img src="/images/links-long-cache.png" alt="links-long-cache | center"></p>
<p>使用带压缩的<code>MEMORY_ONLY_SER</code>缓存时，如下图所示，<code>links</code>仅占用549.5M，相比为<code>String</code>类型时的861.8M，也缩小了近一半。此外，url改成<code>Long</code>型后，运行3次迭代的时间为306s，相比为<code>String</code>类型时的391s，性能提升了21%左右。</p>
<p><img src="/images/links-long-cache-compress.png" alt="links-long-cache-compress | center"></p>
<blockquote>
<p>Tips: 实际开发中，尽可能使用原生类型，尤其是Numeric的原生类型(<code>Int</code>, <code>Long</code>等)</p>
</blockquote>
<h2 id="优化三-数据倾斜"><a href="#优化三-数据倾斜" class="headerlink" title="优化三(数据倾斜)"></a>优化三(数据倾斜)</h2><p>经过前面两个优化后，基本可以应用到线上跑了，但是，可能还不够，如果我们的数据集中有少数url链接的urls特别多，那么在使用<code>groupByKey</code>初始化<code>links</code>时，少数记录的value(urls)可能会有溢出风险，由于<code>groupByKey</code>底层是用一个<code>Array</code>保存value，如果一个节点链接了数十万个节点，那么要开一个超大的数组，即使不溢出，很可能因为没有足够大的连续内存，导致频繁GC，进而引发OOM等致命性错误，通常我们把这类问题称之为数据倾斜问题。此外，在后续迭代循环中<code>links</code>和<code>ranks</code>的<code>join</code>也可能因为数据倾斜导致部分task非常慢甚至引发OOM，下图是<code>groupByKey</code>和<code>join</code>的示意图，左边是<code>groupByKey</code>后得到每个url链接的urls，底层用数组保存，在<code>join</code>时，shuffle阶段会将来自两个RDD相同key的记录通过网络拉到一个partition中，右边显示对url1的shuffle read，如果url1对应的urls特别多，join过程将会非常慢。<br><img src="/images/pagerank-shuffle-origin.png" alt="shuffle-origin | center"></p>
<h3 id="对key进行分桶"><a href="#对key进行分桶" class="headerlink" title="对key进行分桶"></a>对key进行分桶</h3><p>首先我们应该考虑避免使用<code>groupByKey</code>，这是导致后续数据倾斜的源头。既然可能存在单个key对应的value(urls)特别多，那么可以将key做一个随机化处理，例如将具有相同key的记录随机分配到10个桶中，这样就相当于把数据倾斜的记录给打散了，其大概原理如下图所示。</p>
<p><img src="/images/pagerank-random-int-skew.png" alt="random-int-skew | center"></p>
<p>基于上面的理论基础，我们先得到不用<code>groupByKey</code>的<code>links</code>:<br><figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> lines = sc.textFile(inputPath)</span><br><span class="line"><span class="keyword">val</span> links = lines.map &#123; s =&gt;</span><br><span class="line">  <span class="keyword">val</span> parts = s.split(<span class="string">"\\s+"</span>)</span><br><span class="line">  (parts(<span class="number">0</span>).trim.toLong, parts(<span class="number">1</span>).trim.toLong)</span><br><span class="line">&#125;.distinct()</span><br><span class="line">links.persist(storageLevel).setName(<span class="string">"links"</span>)</span><br></pre></td></tr></table></figure></p>
<p>再分析前面代码里的迭代循环，发现我们之前使用<code>groupByKey</code>很大一部分原因是想要得到每个key对应的urls size，我们可以单独通过<code>reduceByKey</code>来得到，<code>reduceByKey</code>会做本地combine，这个操作shuffle开销很小的:<br><figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Count of each url's outs</span></span><br><span class="line"><span class="keyword">val</span> outCnts = links.mapValues(_ =&gt; <span class="number">1</span>).reduceByKey(_ + _)</span><br><span class="line">outCnts.persist(storageLevel).setName(<span class="string">"out-counts"</span>)</span><br></pre></td></tr></table></figure></p>
<p>现在我们就可以使用<code>cogroup</code>将<code>links</code>、<code>outCnts</code>以及<code>ranks</code>三者join起来了，很快我们会想到使用如下代码:<br><figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> contribs = links.cogroup(outCnts, ranks).values.flatMap &#123; pair =&gt;</span><br><span class="line">    <span class="keyword">for</span> (u &lt;- pair._1.iterator; v &lt;- pair._2.iterator; w &lt;- pair._3.iterator)</span><br><span class="line">      <span class="keyword">yield</span> (u, w/v)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>但是！但是！但是！这样做还是会跟之前一样出现数据倾斜，因为<code>cogroup</code>执行过程中，在shuffle阶段还是会把<code>links</code>中相同key的记录分到同一个partition，也就说上面代码<code>pair._1.iterator</code>也可能非常大，这个<code>iterator</code>底层也是<code>Array</code>，面临的问题基本没解决。</p>
<p>所以我们就要考虑使用前面介绍的分桶方法了，对<code>links</code>中的每条记录都随机打散到10个桶中，那么相同key的记录就会被随机分到不同桶中了:<br><figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">keyWithRandomInt</span></span>[<span class="type">K</span>, <span class="type">V</span>](rdd: <span class="type">RDD</span>[(<span class="type">K</span>, <span class="type">V</span>)]): <span class="type">RDD</span>[((<span class="type">K</span>, <span class="type">Int</span>), <span class="type">V</span>)] = &#123;</span><br><span class="line">  rdd.map(x =&gt; ((x._1, <span class="type">Random</span>.nextInt(<span class="number">10</span>)), x._2))</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>然而，cogroup是按照key进行join的，就是说它把来自多个RDD具有相同key的记录汇聚到一起计算，既然<code>links</code>的key已经被我们改变了，那么<code>outCnts</code>和<code>ranks</code>也要变成跟<code>links</code>相同的形式，才能join到一起去计算:<br><figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">expandKeyWithRandomInt</span></span>[<span class="type">K</span>, <span class="type">V</span>](rdd: <span class="type">RDD</span>[(<span class="type">K</span>, <span class="type">V</span>)])</span><br><span class="line">  : <span class="type">RDD</span>[((<span class="type">K</span>, <span class="type">Int</span>), <span class="type">V</span>)] = &#123;</span><br><span class="line">  rdd.flatMap &#123; x =&gt;</span><br><span class="line">    <span class="keyword">for</span> (i &lt;- <span class="number">0</span> until <span class="number">10</span>)</span><br><span class="line">      <span class="keyword">yield</span> ((x._1, i), x._2)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>有了这个基础后，我们就可以将前面的<code>cogroup</code>逻辑修改一下，让他们能够顺利join到一块儿去:<br><figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> contribs = keyWithRandomInt(links).cogroup(</span><br><span class="line">  expandKeyWithRandomInt(outCnts),</span><br><span class="line">  expandKeyWithRandomInt(ranks)</span><br><span class="line">).values.flatMap &#123; pair =&gt;</span><br><span class="line">  <span class="keyword">for</span> (u &lt;- pair._1.iterator; v &lt;- pair._2.iterator; w &lt;- pair._3.iterator)</span><br><span class="line">    <span class="keyword">yield</span> (u, w/v)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>我们将该版本称之为V4，将上述逻辑整理成如下图，可以看到，其实我们对<code>outCnts</code>和<code>ranks</code>做了膨胀处理，才能保证<code>cogroup</code>shuffle阶段对于<code>links</code>中的每条记录，都能找到与之对应的<code>outCnts</code>和<code>ranks</code>记录。</p>
<p><img src="/images/pagerank-shuffle-skewed-process.png" alt="shuffle-skewed-process | center"></p>
<p>其实这种做法会极大地损失性能，虽然这样做可能把之前OOM的问题搞定，能够不出错的跑完，但是由于数据膨胀，实际跑起来是非常慢的，不建议采用这种方法处理数据倾斜问题。这里仅仅引出一些问题让我们更多地去思考。</p>
<h3 id="拆分发生倾斜的key"><a href="#拆分发生倾斜的key" class="headerlink" title="拆分发生倾斜的key"></a>拆分发生倾斜的key</h3><p>有了前面的分析基础，我们知道对key分桶的方法，是不加区分地对所有key都一股脑地处理了，把不倾斜的key也当做倾斜来处理了，其实大部分实际情况下，只有少数key有倾斜，如果大部分key都倾斜那就不是数据倾斜了，那叫数据量特别大。所以我们可以考虑对倾斜的key和不倾斜的key分别用不同的处理逻辑，对不倾斜的key，还是用原来<code>groupByKey</code>和<code>join</code>方式来处理，对倾斜的key可以考虑使用<code>broadcast</code>来实现map join，因为倾斜的key一般来说是可数的，其对应的<code>outCnts</code>和<code>ranks</code>信息在我们PageRank场景里也不会很大，所以可以使用广播。</p>
<p>首先我们把链接的urls个数超过1000000的key定义为倾斜key，使用下面代码将<code>links</code>切分为两部分:<br><figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> lines = sc.textFile(path)</span><br><span class="line"><span class="keyword">val</span> links = lines.map &#123; s =&gt;</span><br><span class="line">  <span class="keyword">val</span> parts = s.split(<span class="string">"\\s+"</span>)</span><br><span class="line">  (parts(<span class="number">0</span>).trim.toLong, parts(<span class="number">1</span>).trim.toLong)</span><br><span class="line">&#125;.distinct()</span><br><span class="line">links.persist(storageLevel).setName(<span class="string">"links"</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">// Count of each url's outs</span></span><br><span class="line"><span class="keyword">val</span> outCnts = links.mapValues(_ =&gt; <span class="number">1</span>L).reduceByKey(_ + _)</span><br><span class="line">  .persist(storageLevel).setName(<span class="string">"out-counts"</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">// Init ranks</span></span><br><span class="line"><span class="keyword">var</span> ranks = outCnts.mapValues(_ =&gt; <span class="number">1.0</span>)</span><br><span class="line">  .persist(storageLevel).setName(<span class="string">"init-ranks"</span>)</span><br><span class="line"><span class="comment">// Force action, just for trigger calculation</span></span><br><span class="line">ranks.foreach(_ =&gt; <span class="type">Unit</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">val</span> skewedOutCnts = outCnts.filter(_._2 &gt;= <span class="number">1000000</span>).collectAsMap()</span><br><span class="line"><span class="keyword">val</span> bcSkewedOutCnts = sc.broadcast(skewedOutCnts)</span><br><span class="line"></span><br><span class="line"><span class="keyword">val</span> skewed = links.filter &#123; link =&gt;</span><br><span class="line">  <span class="keyword">val</span> cnts = bcSkewedOutCnts.value</span><br><span class="line">  cnts.contains(link._1)</span><br><span class="line">&#125;.persist(storageLevel).setName(<span class="string">"skewed-links"</span>)</span><br><span class="line"><span class="comment">// Force action, just for trigger calculation</span></span><br><span class="line">skewed.foreach(_ =&gt; <span class="type">Unit</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">val</span> noSkewed = links.filter &#123; link =&gt;</span><br><span class="line">  <span class="keyword">val</span> cnts = bcSkewedOutCnts.value</span><br><span class="line">  !cnts.contains(link._1)</span><br><span class="line">&#125;.groupByKey().persist(storageLevel).setName(<span class="string">"no-skewed-links"</span>)</span><br><span class="line"><span class="comment">// Force action, just for trigger calculation</span></span><br><span class="line">noSkewed.foreach(_ =&gt; <span class="type">Unit</span>)</span><br><span class="line"></span><br><span class="line">links.unpersist(blocking = <span class="literal">false</span>)</span><br></pre></td></tr></table></figure></p>
<p>首先统计出链接数超过1000000的key，广播到每个计算节点，然后过滤<code>links</code>，如果key在广播变量中则为倾斜的数据，否则为非倾斜的数据，过滤完毕后原始<code>links</code>被销毁。下面就可以在迭代循环中分别处理倾斜的数据<code>skewed</code>和非倾斜的数据<code>noSkewed</code>了。</p>
<p>对<code>noSkewed</code>使用原来的方法:<br><figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> noSkewedPart = noSkewed.join(ranks).values.flatMap &#123;</span><br><span class="line">  <span class="keyword">case</span> (urls, rank) =&gt;</span><br><span class="line">    <span class="keyword">val</span> size = urls.size</span><br><span class="line">    urls.map(url =&gt; (url, rank / size))</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>对<code>skewed</code>使用<code>broadcast</code>方式实现map join，类似地，要把倾斜的key对应的rank收集起来广播，之前的<code>cogroup</code>中的<code>outCnts</code>和<code>ranks</code>在这里就都被广播了，所以可以直接在<code>map</code>操作里完成对<code>skewed</code>中的数据处理:<br><figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> skewedRanks = ranks.filter &#123; rank =&gt;</span><br><span class="line">  <span class="keyword">val</span> cnts = bcSkewedOutCnts.value</span><br><span class="line">  cnts.contains(rank._1)</span><br><span class="line">&#125;.collectAsMap()</span><br><span class="line"><span class="keyword">val</span> bcSkewedRanks = sc.broadcast(skewedRanks)</span><br><span class="line"><span class="keyword">val</span> skewedPart = skewed.map &#123; link =&gt;</span><br><span class="line">  <span class="keyword">val</span> cnts = bcSkewedOutCnts.value</span><br><span class="line">  <span class="keyword">val</span> ranks = bcSkewedRanks.value</span><br><span class="line">  (link._2, ranks(link._1)/cnts(link._1))</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>最后将两部分的处理结果<code>union</code>一下:<br><figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> contribs = noSkewedPart.union(skewedPart)</span><br></pre></td></tr></table></figure></p>
<p>后面的逻辑就跟前面一样了，我们将该版本称之为V5。分别测V3和V5版本代码，迭代3次，在没有数据倾斜的情况下，相同数据、资源和参数下V3运行时间306s，V5运行时间311s，但是在有数据倾斜的情况下，相同数据、资源和参数下V3运行时间722s并伴有严重的GC，V5运行时间472s。可以发现V5版本在不牺牲性能的情况可以解决数据倾斜问题，同时还能以V3相同的性能处理不倾斜的数据集，所以说V5版本更具通用性。</p>
<blockquote>
<p>Tips: 对有倾斜的数据集，将倾斜的记录和非倾斜的记录切分，对倾斜的记录使用map join来解决由于数据倾斜导致少数task非常慢的问题</p>
</blockquote>
<h2 id="优化四-资源利用最大化"><a href="#优化四-资源利用最大化" class="headerlink" title="优化四(资源利用最大化)"></a>优化四(资源利用最大化)</h2><p>通过前面几个优化操作后，V5版本基本可以用于线上例行化跑作业了，但是部署到线上集群，面临如何给资源的困扰。为了测试方便，测试数据集中没有数据倾斜，下面就拿V5来测试并监控资源利用情况。</p>
<p>原始测试数据(使用带压缩的<code>MEMORY_ONLY_SER</code>缓存方式)情况如下表:</p>
<table>
<thead>
<tr>
<th style="text-align:center">磁盘中大小</th>
<th style="text-align:center"><code>links</code>缓存大小</th>
<th style="text-align:center">分区数</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">1.5g</td>
<td style="text-align:center">549.5M</td>
<td style="text-align:center">20</td>
</tr>
</tbody>
</table>
<p>运行3次迭代，一开始大概估计使用如下资源，使用5个executor，每个executor配2个core，一次并行运行10个partition，20个partition 2轮task就可以跑完:</p>
<table>
<thead>
<tr>
<th style="text-align:center">driver_mem</th>
<th style="text-align:center">num_executor</th>
<th style="text-align:center">executor_mem</th>
<th style="text-align:center">executor_cores</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">4g</td>
<td style="text-align:center">5</td>
<td style="text-align:center">2g</td>
<td style="text-align:center">2</td>
</tr>
</tbody>
</table>
<p>在提交参数中加上如下额外JVM参数，表示分别对driver和executor在运行期间开启<a href="https://docs.oracle.com/javacomponents/index.html" target="_blank" rel="external">Java Flight Recorder</a>:<br><figure class="highlight vim"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">spark.driver.extraJavaOptions -XX:+UnlockCommercialFeatures -XX:+FlightRecorder -XX:StartFlightRecording=filename=<span class="symbol">&lt;LOG_DIR&gt;</span>/driver.jfr,dumponexit=true</span><br><span class="line">spark.executor.extraJavaOptions -XX:+UnlockCommercialFeatures -XX:+FlightRecorder -XX:StartFlightRecording=filename=<span class="symbol">&lt;LOG_DIR&gt;</span>/excutor.jfr,dumponexit=true</span><br></pre></td></tr></table></figure></p>
<p>运行完毕后，统计运行时间为439s，将<code>driver.jfr</code>和<code>excutor.jfr</code>拿到开发机上来，打开jmc分析工具(位于java安装目录<code>bin/</code>下面)，首先我们看driver的监控信息，主页如下图所示，可以看到driver的cpu占用是很小的:</p>
<p><img src="/images/driver-control.png" alt="driver-control | center"></p>
<p>切到内存tab，把物理内存的两个勾选去掉，可以看到driver的内存使用曲线，我们给了4g，但是实际上最大也就用了差不多1g，看下图中的GC统计信息，没有什么瓶颈。</p>
<p><img src="/images/driver-heap.png" alt="driver-heap | center"></p>
<p>所以给driver分配4g是浪费的，我们把它调到2g，虽然实际上只用了大概1g，这里多给driver留点余地，其他配置不变，重新提交程序，统计运行时间为443s，跟4g时运行时间439s差不多。</p>
<p>再来看executor的监控信息，主页如下图所示，可以看到executor的cpu利用明显比driver多，因为要做序列化、压缩以及排序等。</p>
<p><img src="/images/executor-control.png" alt="executor-control | center"></p>
<p>再切到内存tab，可以看到executor的内存使用波动较大，最大内存使用差不多1.75g，我们给了2g，还是相当合适的。但是看下面的GC统计信息，发现最长暂停4s多，而且垃圾回收次数也较多。</p>
<p><img src="/images/executor-heap.png" alt="executor-heap | center"></p>
<p>为此，我们切到”GC时间”tab，可以看到，GC还是比较频繁的，还有一次持续4s多的GC，看右边GC类型，对最长暂停时间从大到小排序，居然有几个SerialOld类型的GC，其他一部分是ParNew类型GC，一部分是CMS类型的GC，没有出现FULL GC，下面先分析内存使用，回过头来再分析这里出现的诡异SerialOld。</p>
<p><img src="/images/executor-gc.png" alt="executor-gc | center"></p>
<p>我们再看下堆内存大对象占用情况，大对象主要是在<code>ExternalAppendOnlyMap</code>和<code>ExternalSorter</code>中，<code>ExternalAppendOnlyMap</code>用于存放shuffle read的数据，<code>ExternalSorter</code>用于存放shuffle write前的数据，用于对记录排序，这两个数据结构底层使用<code>Array</code>存储数据，所以这里表现为大对象。</p>
<p><img src="/images/executor-heap-info.png" alt="executor-heap-info | center"></p>
<p>切换到TLAB，再细化到小对象，可以看到大部分是<code>Long</code>型(url)，展开堆栈跟踪，大部分是用在shuffle阶段，因为在<code>join</code>时，一方面会读取<code>groupByKey</code>后的<code>links</code>，用于做shuffle write，一方面在shuffle read阶段，将相同key的<code>links</code>和<code>ranks</code>拉到一起做<code>join</code>计算。</p>
<p><img src="/images/executor-heap-info2.png" alt="executor-heap-info2 | center"></p>
<p>所以总体来说，内存情况是符合业务逻辑的，没有出现莫名其妙的内存占用。让人有点摸不清头脑的是，GC信息中有SerialOld这玩意儿，我明明用了CMS垃圾回收方式，经过一番Google查阅资料，”Concurrent Mode Failure”可能导致Serial Old的出现，查阅”Concurrent Mode Failure”发生的原因: 当CMS GC正进行时，此时有新的对象要进入老年代，但是老年代空间不足。仔细分析，个人觉得可能是因为CMS GC后存在较多的内存碎片，而我们的程序在shuffle阶段底层使用<code>Array</code>，需要连续内存，导致CMS GC过程中出现了”Concurrent Mode Failure”，才退化到Serial Old，Serial Old是采用标记整理回收算法，回收过程中会整理内存碎片。这样看来，应该是CMS GC过程中，老年代空间不足导致的，从两个方面考虑优化下，一是增加老年代内存占比，二是减小参数<code>-XX:CMSInitiatingOccupancyFraction</code>，降低触发CMS GC的阈值，让CMS GC及早回收老年代。</p>
<p>首先我们增加老年代内存占比，也就是降低新生代内存占比，默认<code>-XX:NewRatio=2</code>，我们把它改成<code>-XX:NewRatio=3</code>，将老年代内存占比由2/3提升到3/4，重新提交程序，得到<code>executor.jfr</code>，打开GC监控信息，发现有很大的改善，不在出现Serial Old类型的GC了，最长暂停时间从原来的4s降低到600ms左右，整体运行时间从448s降低到436s。</p>
<p><img src="/images/executor-gc2.png" alt="executor-gc2 | center"></p>
<p>把上述<code>-XX:NewRatio=3</code>去掉，设置参数<code>-XX:CMSInitiatingOccupancyFraction=60</code>，重新提交程序，得到executor GC的监控信息，发现GC最大暂停时间也降下来了，但是由于老年代GC的频率加大了，整体运行时间为498s，比原来的436s还要长。</p>
<p><img src="/images/executor-gc3.png" alt="executor-gc3 | center"></p>
<p>综合考虑以上信息，增加executor的jvm启动参数<code>-XX:NewRatio=3</code>，能把GC状态调整到一个较优的状态。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>Spark给我们提供了一种简单灵活的大数据编程框架，但是对于很多实际问题的处理，还应该多思考下如何让我们写出来的应用程序更高效更节约，以上几个调优点是可以推广到其他应用的，在我们编写spark应用程序时，通过这种思考也可以加速我们对spark的理解。</p>
<p><span style="color:red"><em>转载请注明出处，本文永久链接：<a href="http://sharkdtu.com/posts/spark-app-optimize.html">http://sharkdtu.com/posts/spark-app-optimize.html</a></em></span></p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[Spark Cache性能测试]]></title>
      <url>http://sharkdtu.com/posts/spark-cache-benchmark.html</url>
      <content type="html"><![CDATA[<p>采用Spark自带的Kmeans算法作为测试基准（Spark版本为2.1），该算法Shuffle数据量较小，对于这类迭代型任务，又需要多次加载训练数据，此测试的目的在于评判各种Cache IO的性能，并总结其Spark内部原理作分析，作为Spark用户的参考。<a id="more"></a></p>
<h2 id="测试准备"><a href="#测试准备" class="headerlink" title="测试准备"></a>测试准备</h2><p>训练数据是通过<a href="http://prof.ict.ac.cn/BigDataBench/dowloads/" target="_blank" rel="external">Facebook SNS公开数据集生成器</a>得到，在HDFS上大小为9.3G，100个文件，添加如下两个参数以保证所有资源全部到位后才启动task，训练时间为加载数据到训练完毕这期间的耗时。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">--conf spark.scheduler.minRegisteredResourcesRatio=1</span><br><span class="line">--conf spark.scheduler.maxRegisteredResourcesWaitingTime=100000000</span><br></pre></td></tr></table></figure>
<p>测试集群为3个节点的TS5机器搭建而成，其中一台作为RM，并运行着Alluxio Master，两个NM上同时运行着Alluxio Worker。除以上配置外，其他配置全部保持Spark默认状态。公共资源配置、分区设置以及算法参数如下表所示，executor_memory视不同的测试用例不同:</p>
<table>
<thead>
<tr>
<th style="text-align:center">driver_memory</th>
<th style="text-align:center">num_executor</th>
<th style="text-align:center">executor_cores</th>
<th style="text-align:center">分区数</th>
<th style="text-align:center">聚类个数</th>
<th style="text-align:center">迭代次数</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">4g</td>
<td style="text-align:center">10</td>
<td style="text-align:center">2</td>
<td style="text-align:center">100</td>
<td style="text-align:center">6</td>
<td style="text-align:center">10</td>
</tr>
</tbody>
</table>
<h2 id="测试用例"><a href="#测试用例" class="headerlink" title="测试用例"></a>测试用例</h2><h3 id="测试用例1-不使用Cache"><a href="#测试用例1-不使用Cache" class="headerlink" title="测试用例1:  不使用Cache"></a>测试用例1:  不使用Cache</h3><p>在不使用Cache的情况下，测试Spark-Kmeans算法的训练时间以及GC时间占比。这里分别使用两种方式加载数据，一种是直接从HDFS加载数据，另一种是透过ALLUXIO加载数据，相关测试指标数据如下表所示：</p>
<table>
<thead>
<tr>
<th style="text-align:center">\</th>
<th style="text-align:center">说明</th>
<th style="text-align:center">内存使用总量</th>
<th style="text-align:center">训练时间</th>
<th style="text-align:center">GC时间占比</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">case 1-1</td>
<td style="text-align:center">从hdfs直接加载训练数据</td>
<td style="text-align:center">20g</td>
<td style="text-align:center">1064s</td>
<td style="text-align:center">3.1%</td>
</tr>
<tr>
<td style="text-align:center">case 1-2</td>
<td style="text-align:center">透过alluxio加载训练数据</td>
<td style="text-align:center">20g + 9.3g</td>
<td style="text-align:center">689s</td>
<td style="text-align:center">3.4%</td>
</tr>
</tbody>
</table>
<p>不使用cache时，以上两种情形GC均不是瓶颈，主要差别表现在：</p>
<ul>
<li>从hdfs直接加载训练数据：在每次迭代时均要读一遍hdfs，访问hdfs有较大的开销；</li>
<li>透过alluxio加载训练数据：只需第一次加载读一遍hdfs，后续迭代直接从alluxio中读取，不过alluxio额外消耗9.3G内存，整体性能提升35%+。</li>
</ul>
<h3 id="测试用例2-使用Cache"><a href="#测试用例2-使用Cache" class="headerlink" title="测试用例2:  使用Cache"></a>测试用例2:  使用Cache</h3><p>在使用Cache的情况下，从HDFS加载数据后先做cache，分别采用不同的Cache方式，相关测试指标数据如下表所示：</p>
<table>
<thead>
<tr>
<th style="text-align:center">\</th>
<th style="text-align:center">缓存方式</th>
<th style="text-align:center">executor_memory</th>
<th style="text-align:center">内存使用总量</th>
<th style="text-align:center">cache比例</th>
<th style="text-align:center">训练时间</th>
<th style="text-align:center">GC时间占比</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">case 2-1</td>
<td style="text-align:center">MEMORY_ONLY</td>
<td style="text-align:center">2g</td>
<td style="text-align:center">20g</td>
<td style="text-align:center">33%</td>
<td style="text-align:center">1558s</td>
<td style="text-align:center">12%</td>
</tr>
<tr>
<td style="text-align:center">case 2-2</td>
<td style="text-align:center">MEMORY_ONLY</td>
<td style="text-align:center">4g</td>
<td style="text-align:center">40g</td>
<td style="text-align:center">90%</td>
<td style="text-align:center">986s</td>
<td style="text-align:center">7%</td>
</tr>
<tr>
<td style="text-align:center">case 2-3</td>
<td style="text-align:center">MEMORY_ONLY</td>
<td style="text-align:center">6g</td>
<td style="text-align:center">60g</td>
<td style="text-align:center">100%</td>
<td style="text-align:center">463s</td>
<td style="text-align:center">4.7%</td>
</tr>
<tr>
<td style="text-align:center">case 2-4</td>
<td style="text-align:center">MEMORY_AND_DISK</td>
<td style="text-align:center">2g</td>
<td style="text-align:center">20g</td>
<td style="text-align:center">100%</td>
<td style="text-align:center">1182s</td>
<td style="text-align:center">16.9%</td>
</tr>
<tr>
<td style="text-align:center">case 2-5</td>
<td style="text-align:center">DISK_ONLY</td>
<td style="text-align:center">2g</td>
<td style="text-align:center">20g</td>
<td style="text-align:center">100%</td>
<td style="text-align:center">514s</td>
<td style="text-align:center">3.2%</td>
</tr>
<tr>
<td style="text-align:center">case 2-6</td>
<td style="text-align:center">ALLUXIO</td>
<td style="text-align:center">2g</td>
<td style="text-align:center">20g + 9.3g</td>
<td style="text-align:center">100%</td>
<td style="text-align:center">687s</td>
<td style="text-align:center">4.5%</td>
</tr>
</tbody>
</table>
<blockquote>
<p><a href="https://alluxio.com/blog/alluxiospark-rdd" target="_blank" rel="external">采用Alluxio的Cache实现方式</a>为:<br><figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">data.saveAsTextFile(path)</span><br><span class="line"><span class="keyword">val</span> data = sc.textFile(path)</span><br></pre></td></tr></table></figure></p>
</blockquote>
<p>从以上测试数据看来，让人有点出乎意料，一开始有点不太相信，但是多次测试后数据并没有多大的抖动，所以说Spark的性能受多方面因素的影响，单单Cache这块不同的Cache方式以及不同的资源情况下，其性能差别就相差较大，下面分析其内在原因。</p>
<p>从HDFS加载训练数据后直接采用Spark原生的Cache，当executor_memory为2g时，不足以Cache住原始训练数据，从UI上看到Cache的比例只有33%左右，导致频繁的 rdd-block 剔除重建，同时由于内存吃紧，可能引发频发的Spill以及较重的GC，从UI上看到GC时间占到总的task运行时间的12%左右，已经成为瓶颈，其整体性能还不如不使用Cache的case1-1；当executor_memory为4g时，也不足以Cache住原始训练数据，但是其Cache的比例有90%左右，同样存在 rdd-block 剔除重建，频发Spill以及较重的GC，GC时间占总的task运行时间的7%左右，虽然比executor_memory为2g的情况有所好转，但是仍然不理想，只比不做Cache的case1-1好7%左右，但是内存却多用了20g，并不是特别划算；当executor_memory为6g时，可以全部Cache住原始训练数据，性能较优，GC占比较小，但是比不用Cache的case1-1要多用40g内存，有些代价。</p>
<p>一般来说，当我们内存不够时，可以选择MEMORY_AND_DISK的缓存方式，但是测试发现MEMORY_AND_DISK的缓存效果并不是特别好，从测试数据来看，还不如直接使用DISK_ONLY的缓存方式，MEMORY_AND_DISK的缓存方式带来的GC开销非常大，可能是因为每次都尽可能地Cache数据到内存，不够再刷到磁盘，造成JVM频繁GC。</p>
<p>另外测试了使用Alluxio作缓存的Case，发现并没有<a href="https://alluxio.com/blog/alluxiospark-rdd" target="_blank" rel="external">官方描述</a>的那样会提升Cache的性能，还不如直接使用Spark DISK_ONLY缓存，感觉官方给的测试对比数据存在一定的水分，值得一提的是在多个Application之间Alluxio能起到加速作用。</p>
<h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>Spark的Cache并不是总是会加速任务运行，Cache的方式不同，对任务产生的影响不同。并不是能用内存Cache就用内存，而是要考虑是否有充足的内存Cache住你的数据，否则可能适得其反。</p>
<p><span style="color:red"><em>转载请注明出处，本文永久链接：<a href="http://sharkdtu.com/posts/spark-cache-benchmark.html">http://sharkdtu.com/posts/spark-cache-benchmark.html</a></em></span></p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[Spark 底层网络模块]]></title>
      <url>http://sharkdtu.com/posts/spark-network.html</url>
      <content type="html"><![CDATA[<p>对于分布式系统来说，网络是最基本的一环，其设计的好坏直接影响到整个分布式系统的稳定性及可用性。为此，Spark专门独立出基础网络模块spark-network，为上层RPC、Shuffle数据传输、RDD Block同步以及资源文件传输等提供可靠的网络服务。在spark-1.6以前，RPC是单独通过akka实现，数据以及文件传输是通过netty实现，然而akka实质上底层也是采用netty实现，对于一个优雅的工程师来说，不会在系统中同时使用具有重复功能的框架，否则会使得系统越来越重，所以自spark-1.6开始，通过netty封装了一套简洁的类似于akka actor模式的RPC接口，逐步抛弃akka这个大框架。从spark-2.0起，所有的网络功能都是通过netty来实现。<a id="more"></a></p>
<h2 id="系统抽象"><a href="#系统抽象" class="headerlink" title="系统抽象"></a>系统抽象</h2><p>在介绍spark网络模块前，我们先温习下netty的基本工作流程。无论是服务器还是客户端都会关联一个channel(socket)，channel上会绑定一个pipeline，pipeline绑定若干个handler，用来专门用来处理和业务有关的东西，handler有DownHandler和UpHandler两种，DownHandler用来处理发包，UpHandler用来处理收包，大致过程如下图所示。</p>
<p><img src="/images/spark-network-netty-overview.png" width="500" height="500" alt="spark-network-netty-overview" align="center"></p>
<p>Spark的底层网络实现也是遵循上图所示流程，其总体实现流程如下图所示。客户端发送请求消息，经过Encoder(一种DownHandler)编码，加上包头信息，再通过网络发给服务端，服务端收到消息后，首先经过TransportFrameDecoder(一种UpHandler)处理粘包拆包，得到消息类型和消息体，然后经过Decoder解析消息类型，得到一个个具体的请求消息，最后由TransportChannelHandler处理具体的请求消息，并根据具体的消息类型判断是否返回一个响应。类似地，响应消息传给客户端也是先经过Encoder编码，客户端先通过TransportFrameDecoder、Decoder解包消息，再通过TransportChannelHandler处理具体的响应消息。</p>
<p><img src="/images/spark-network-basic.png" width="500" height="500" alt="spark-network-basic" align="center"></p>
<p>整个网络模型非常清晰简单，最核心的当属消息抽象以及如何定义消息传输和处理，即上图中的Message的定义以及编解码传输等，下面详细介绍spark网络模块的消息抽象以及相关handler的定义。</p>
<h3 id="消息抽象"><a href="#消息抽象" class="headerlink" title="消息抽象"></a>消息抽象</h3><p>总结起来，Spark中定义三种类型的消息：RPC消息、ChunkFetch消息以及Stream消息。Message是这些消息的抽象接口，它定义了三个关键接口，分别得到消息类型、消息体以及判断消息体是否编码在header中，消息体统一由ManagedBuffer表示，ManagedBuffer抽象了JAVA NIO ByteBuffer、Netty ByteBuf以及File Segment，所以无论是ByteBuffer、ByteBuf还是File Segment，都可以表示为ManagedBuffer。如下图列出所有spark中涉及到的具体消息，下面分别详细阐述各种消息。</p>
<p><img src="/images/spark-network-protocol.png" width="500" height="400" alt="spark-network-protocol" align="center"></p>
<p>RPC消息用于抽象所有spark中涉及到RPC操作时需要传输的消息，通常这类消息很小，一般都是些控制类消息，在spark-1.6以前，RPC都是通过akka来实现的，自spark-1.6开始逐渐把akka剔除，通过netty实现，所以在spark-network公共模块中定义该类消息，其包括RpcRequest、OneWayMessage、RpcResponse以及RpcFailure四种消息。RpcRequest封装RPC请求消息，这类RPC请求是需要得到一个RPC响应的，RpcRequest除了消息体外，还包括一个requestId字段，用于唯一标识一个RPC请求，与RpcRequest对应地，有两种RPC响应消息，RpcResponse是RPC调用正常返回的响应消息，RpcFailure是RPC调用异常返回的响应消息，同样地，它们除了消息体外也包括requestId字段，该字段用于对应RpcRequest。OneWayMessage作为另一种RPC请求消息，但是这类RPC请求是不需要响应的，所以它只包含消息体，不需要诸如requestId等字段来唯一标识，该消息被发送后可不用管它。</p>
<p>ChunkFetch消息用于抽象所有spark中涉及到数据拉取操作时需要传输的消息，它用于shuffle数据以及RDD Block数据传输。在shuffle阶段，reduce task会去拉取map task结果中的对应partition数据，这需要发起一个ChunkFetch；另外，当RDD被缓存后，如果节点上没有所需的RDD Block，则会发起一个ChunkFetch拉取其他节点上的RDD Block。ChunkFetch消息包括ChunkFetchRequest、ChunkFetchSuccess以及ChunkFetchFailure三种消息。ChunkFetchRequest封装ChunkFetch请求消息，其只包括StreamChunkId字段，没有消息体，StreamChunkId包括streamId和chunkIndex两个字段，streamId标识这次chunk fetch，chunkIndex标识fetch的数据块，通常一次fetch可能会fetch多个chunk。ChunkFetchSuccess是成功Fetch后的响应消息，ChunkFetchFailure是Fetch失败的响应消息，它们包含了Fetch的消息体外，还包括StreamChunkId，以对应ChunkFetchRequest。</p>
<p>Stream消息很简单，主要用于driver到executor传输jar、file文件等。Stream消息包括StreamRequest、StreamResponse以及StreamFailure三种消息，其中StreamRequest表示Stream请求消息，只包含一个streamId，标识这个请求。StreamResponse表示Stream成功响应消息，包含streamId以及响应的字节数，并后面跟数据内容，实际使用时，客户端会根据响应中的字节数进一步获取实际内容。StreamFailure表示Stream失败的响应消息，包含streamId以及异常信息。executor需要获取相关jar包或file文件时，会发起一个StreamRequest消息给driver，driver会返回一个StreamResponse，executor根据响应中的字节数来进一步去截获后续数据内容。</p>
<h3 id="Handler定义"><a href="#Handler定义" class="headerlink" title="Handler定义"></a>Handler定义</h3><p>一个message从被发送到被接收需要经过”MessageEncoder-&gt;网络-&gt;TransportFrameDecoder-&gt;MessageDecoder”，下面按照这一过程详细阐述各handler的作用。</p>
<p>一个message进入网络前，需要经过MessageEncoder编码，加上包头信息，以便后续收包时正确粘包拆包。头信息主要是包括三部分：</p>
<ul>
<li>整个包的长度</li>
<li>消息类型</li>
<li>除消息体外的消息元数据，例如RpcRequest消息的元数据信息包括requestId和消息长度bodysize</li>
</ul>
<p><img src="/images/spark-network-messagewithheader.png" width="400" height="200" alt="message-with-header" align="center"></p>
<p>经过上述编码后，一个个Message被编码成一个个MessageWithHeader发送到网络中，接收端收到数据后，首先通过TransportFrameDecoder和MessageDecoder来解码出一个个具体的Message，这里就涉及到粘包拆包问题，这也是为什么在编码阶段在头部加上frame length的原因。TransportFrameDecoder在解码过程中，首先读取8字节的frame length(Long型)，用frame length减去8就是除frame length外其他部分的长度，即为message type、message meta、message body三部分的长度，循环读取直到这个长度，把读到的Bytebuf交给MessagerDecoder，MessagerDecoder首先解析出message type，根据message type去反序列化(实例化)出具体的Message，例如message type如果是RpcRequest，那么则继续解析requestId和body size，根据body size解析后续字节得到body，并构造出RpcRequest对象。反序列化得到的message对象会交给TransportChannelHandler，TransportChannelHandler里封装了TransportRequestHandler和TransportResponseHandler，分别处理RequestMessage和ResponseMessage，在服务端，TransportChannelHandler一般处理RequestMessage，在客户端，TransportChannelHandler一般处理ResponseMessage。</p>
<p><img src="/images/spark-network-handler.png" width="600" height="300" alt="spark-network-handler" align="center"></p>
<h2 id="系统消息流程"><a href="#系统消息流程" class="headerlink" title="系统消息流程"></a>系统消息流程</h2><p>根据上述系统抽象可以看出，spark-network将RPC、ChunkFetch以及Stream统一抽象出来，其中任意一种功能都依赖于spark-network的实现，下面分别详细阐述这三种功能的一般使用流程。</p>
<h3 id="RPC消息处理"><a href="#RPC消息处理" class="headerlink" title="RPC消息处理"></a>RPC消息处理</h3><p>客户端发送一个RPC请求消息(RpcRequest或OneWayMessage)，经过编码到网络，解码到服务端的TransportChannelHandler，RPC请求会被交给TransportRequestHandler处理，而TransportRequestHandler中包括了一个RpcHandler专门用来处理RPC请求消息，RpcHandler中有两个关键receive接口，带callback和不带callback参数分别处理RpcRequest和OneWayMessage。</p>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">public <span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">RpcHandler</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">  public <span class="keyword">abstract</span> void receive(</span><br><span class="line">      <span class="type">TransportClient</span> client,</span><br><span class="line">      <span class="type">ByteBuffer</span> message,</span><br><span class="line">      <span class="type">RpcResponseCallback</span> callback);</span><br><span class="line"></span><br><span class="line">  public void receive(<span class="type">TransportClient</span> client, <span class="type">ByteBuffer</span> message) &#123;</span><br><span class="line">    receive(client, message, <span class="type">ONE_WAY_CALLBACK</span>);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>当收到RpcRequest时，处理后会在callback中发送响应消息，成功则发送RpcResponse，失败则发送RpcFailure。当收到OneWayMessage时，处理后则直接不用管，客户端也不用关心是否被处理了。</p>
<p>类似地，服务端发送RPC响应消息(RpcResponse或RpcFailure)，也经过编码到网络，解码到客户端的TransportChannelHandler，RPC响应会被交给TransportResponseHandler处理，在客户端发送RpcRequest的时候，会注册一个RpcResponseCallback，通过requestId来标识，这样在收到响应消息的时候，根据响应消息中的requestId就可以取出对应的RpcResponseCallback对响应消息进行处理。</p>
<h3 id="ChunkFetch消息处理"><a href="#ChunkFetch消息处理" class="headerlink" title="ChunkFetch消息处理"></a>ChunkFetch消息处理</h3><p>对于ChunkFetch请求，客户端一般需要首先发送一个RPC请求，告诉服务端需要拉取哪些数据，服务端收到这个RPC请求后，会为客户端准备好需要的数据。上一节也提到，RPC请求会通过RpcHandler来处理，当RpcHandler接收到ChunkFetch的RPC请求消息时，则会为客户端准备好它需要的数据，这些即将要被fetch的数据是通过一个StreamManager来管理的，所以RpcHandler中有一个接口专门获取StreamManager，StreamManager为后续到来的ChunkFetchRequest服务。</p>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">public <span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">RpcHandler</span> </span>&#123;</span><br><span class="line">  ...</span><br><span class="line"></span><br><span class="line">  public <span class="keyword">abstract</span> <span class="type">StreamManager</span> getStreamManager();</span><br><span class="line"></span><br><span class="line">  ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>RPC请求成功后，服务端表示数据准备好，客户端发送ChunkFetchRequest消息，服务端收到该消息后，最后会交给TransportRequestHandler处理，TransportRequestHandler则根据请求消息中的StreamChunkId，从前面准备好的StreamManager中拿到对应的数据，封装成ChunkFetchSuccess返回给客户端，如果出错或找不到对应的数据，则返回ChunkFetchFailure。</p>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">public <span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">StreamManager</span> </span>&#123;</span><br><span class="line">  ...</span><br><span class="line"></span><br><span class="line">  public <span class="keyword">abstract</span> <span class="type">ManagedBuffer</span> getChunk(long streamId, int chunkIndex);</span><br><span class="line"></span><br><span class="line">  ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>响应消息到达客户端后，最后会被交给TransportResponseHandler处理，在客户端发送ChunkFetchRequest的时候，会注册一个ChunkReceivedCallback，通过StreamChunkId来标识，这样在收到响应消息的时候，根据响应消息中的StreamChunkId就可以取出对应的ChunkReceivedCallback对响应消息进行处理。</p>
<h3 id="Stream消息处理"><a href="#Stream消息处理" class="headerlink" title="Stream消息处理"></a>Stream消息处理</h3><p>Stream类似于ChunkFetch，主要用于文件服务。客户端一般也需要首先发送一个RPC请求，告诉服务端需要打开一个stream，服务端收到这个RPC请求后，会为客户端打开所需的文件流。</p>
<p>RPC请求成功后，服务端表示数据准备好，客户端发送StreamRequest消息，服务端收到该消息后，最后会交给TransportRequestHandler处理，TransportRequestHandler则根据请求消息中的streamId，从准备好的StreamManager中打开对应的文件流，同时返回StreamResponse给客户端，如果出错或找不到对应的流，则返回ChunkFetchFailure。</p>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">public <span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">StreamManager</span> </span>&#123;</span><br><span class="line">  ...</span><br><span class="line"></span><br><span class="line">  public <span class="type">ManagedBuffer</span> openStream(<span class="type">String</span> streamId)；</span><br><span class="line"></span><br><span class="line">  ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>响应消息到达客户端后，最后会被交给TransportResponseHandler处理，在客户端发送StreamRequest的时候，会注册一个StreamCallback，同时维护一个StreamCallback的队列，这样在收到响应消息的时候，就会从队列中取出StreamCallback去处理截获的数据。注意这里说的是截获的数据，这块有点不一样的是，收到响应消息后，会根据响应消息中数据大小，在TransportFrameDecoder对象中设置截获器Interceptor对象，TransportFrameDecoder在接收数据的时候会被这个截获器Interceptor截取它想要的数据。虽然代码看懂了，但是这里却不知道为啥通过截获的方式去拉取文件流数据。</p>
<h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>本文主要阐述spark-network公共模块，详细分析spark底层网络编解码以及消息处理的抽象，在后续文章中会更加详细地介绍具体spark中的RPC、ShuffleService、BlockTransformService以及FileServer的实现，这些功能服务都是基于spark-network公共模块来实现的。</p>
<p><span style="color:red"><em>转载请注明出处，本文永久链接：<a href="http://sharkdtu.com/posts/spark-network.html">http://sharkdtu.com/posts/spark-network.html</a></em></span></p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[Spark Shuffle原理及相关调优]]></title>
      <url>http://sharkdtu.com/posts/spark-shuffle.html</url>
      <content type="html"><![CDATA[<p>通过文章<a href="/posts/spark-scheduler.html">“Spark Scheduler原理及资源配置”</a>我们知道，Spark在DAG调度阶段会将一个Job划分为多个Stage，上游Stage做map工作，下游Stage做reduce工作，其本质上还是MapReduce计算框架。Shuffle是连接map和reduce之间的桥梁，它将map的输出对应到reduce输入中，这期间涉及到序列化反序列化、跨节点网络IO以及磁盘读写IO等，所以说Shuffle是整个应用程序运行过程中非常昂贵的一个阶段，理解Spark Shuffle原理有助于优化Spark应用程序。<a id="more"></a></p>
<h2 id="Spark-Shuffle的基本原理与特性"><a href="#Spark-Shuffle的基本原理与特性" class="headerlink" title="Spark Shuffle的基本原理与特性"></a>Spark Shuffle的基本原理与特性</h2><p>与MapReduce计算框架一样，Spark的Shuffle实现大致如下图所示，在DAG阶段以shuffle为界，划分stage，上游stage做map task，每个map task将计算结果数据分成多份，每一份对应到下游stage的每个partition中，并将其临时写到磁盘，该过程叫做shuffle write；下游stage做reduce task，每个reduce task通过网络拉取上游stage中所有map task的指定分区结果数据，该过程叫做shuffle read，最后完成reduce的业务逻辑。举个栗子，假如上游stage有100个map task，下游stage有1000个reduce task，那么这100个map task中每个map task都会得到1000份数据，而1000个reduce task中的每个reduce task都会拉取上游100个map task对应的那份数据，即第一个reduce task会拉取所有map task结果数据的第一份，以此类推。</p>
<p><img src="/images/spark-shuffle-overview.png" width="600" height="400" alt="spark-shuffle-overview" align="center"></p>
<p>在map阶段，除了map的业务逻辑外，还有shuffle write的过程，这个过程涉及到序列化、磁盘IO等耗时操作；在reduce阶段，除了reduce的业务逻辑外，还有前面shuffle read过程，这个过程涉及到网络IO、反序列化等耗时操作。所以整个shuffle过程是极其昂贵的，spark在shuffle的实现上也做了很多优化改进，随着版本的迭代发布，spark shuffle的实现也逐步得到改进。下面详细介绍spark shuffle的实现演进过程。</p>
<h2 id="Spark-Shuffle实现演进"><a href="#Spark-Shuffle实现演进" class="headerlink" title="Spark Shuffle实现演进"></a>Spark Shuffle实现演进</h2><p>Spark在1.1以前的版本一直是采用Hash Shuffle的实现的方式，到1.1版本时参考Hadoop MapReduce的实现开始引入Sort Shuffle，在1.5版本时开始Tungsten钨丝计划，引入UnSafe Shuffle优化内存及CPU的使用，在1.6中将Tungsten统一到Sort Shuffle中，实现自我感知选择最佳Shuffle方式，到最近的2.0版本，Hash Shuffle已被删除，所有Shuffle方式全部统一到Sort Shuffle一个实现中。下图是spark shuffle实现的一个版本演进。</p>
<p><img src="/images/spark-shuffle-evolution.png" width="400" height="230" alt="spark-shuffle-evolution" align="center"></p>
<h3 id="Hash-Shuffle-v1"><a href="#Hash-Shuffle-v1" class="headerlink" title="Hash Shuffle v1"></a>Hash Shuffle v1</h3><p>在spark-1.1版本以前，spark内部实现的是Hash Shuffle，其大致原理与前面基本原理介绍中提到的基本一样，如下图所示。</p>
<p><img src="/images/spark-shuffle-v1.png" width="600" height="400" alt="spark-shuffle-v1" align="center"></p>
<p>在map阶段(shuffle write)，每个map都会为下游stage的每个partition写一个临时文件，假如下游stage有1000个partition，那么每个map都会生成1000个临时文件，一般来说一个executor上会运行多个map task，这样下来，一个executor上会有非常多的临时文件，假如一个executor上运行M个map task，下游stage有N个partition，那么一个executor上会生成M<em>N个文件。另一方面，如果一个executor上有K个core，那么executor同时可运行K个task，这样一来，就会同时申请K</em>N个文件描述符，一旦partition数较多，势必会耗尽executor上的文件描述符，同时生成K*N个write handler也会带来大量内存的消耗。</p>
<p>在reduce阶段(shuffle read)，每个reduce task都会拉取所有map对应的那部分partition数据，那么executor会打开所有临时文件准备网络传输，这里又涉及到大量文件描述符，另外，如果reduce阶段有combiner操作，那么它会把网络中拉到的数据保存在一个<code>HashMap</code>中进行合并操作，如果数据量较大，很容易引发OOM操作。</p>
<p>综上所述，Hash Shuffle实现简单但是特别naive，在小数据量下运行比较快，一旦数据量较大，基本就垮了。当然这个版本的shuffle也是在spark早期版本中，随着版本迭代的进行，shuffle的实现也越来越成熟。</p>
<h3 id="Hash-Shuffle-v2"><a href="#Hash-Shuffle-v2" class="headerlink" title="Hash Shuffle v2"></a>Hash Shuffle v2</h3><p>在上一节讲到每个map task都要生成N个partition文件，为了减少文件数，后面引进了，目的是减少单个executor上的文件数。如下图所示，一个executor上所有的map task生成的分区文件只有一份，即将所有的map task相同的分区文件合并，这样每个executor上最多只生成N个分区文件。</p>
<p><img src="/images/spark-shuffle-v2.png" width="600" height="400" alt="spark-shuffle-v2" align="center"></p>
<p>表面上看是减少了文件数，但是假如下游stage的分区数N很大，还是会在每个executor上生成N个文件，同样，如果一个executor上有K个core，还是会开K*N个writer handler，总体上来说基本没太解决问题。对于shuffle read阶段跟v1版一样没改进，仍然容易导致OOM。</p>
<h3 id="Sort-Shuffle-v1"><a href="#Sort-Shuffle-v1" class="headerlink" title="Sort Shuffle v1"></a>Sort Shuffle v1</h3><p>针对上述Hash Shuffle的弊端，在spark 1.1.0版本中引入Sort Shuffle，它参考了Hadoop MapReduce中的shuffle实现，对记录进行排序来做shuffle，如下图所示。</p>
<p><img src="/images/spark-shuffle-v3.png" width="600" height="400" alt="spark-shuffle-v3" align="center"></p>
<p>在map阶段(shuffle write)，会按照partition id以及key对记录进行排序，将所有partition的数据写在同一个文件中，该文件中的记录首先是按照partition id排序一个一个分区的顺序排列，每个partition内部是按照key进行排序存放，map task运行期间会顺序写每个partition的数据，并通过一个索引文件记录每个partition的大小和偏移量。这样一来，每个map task一次只开两个文件描述符，一个写数据，一个写索引，大大减轻了Hash Shuffle大量文件描述符的问题，即使一个executor有K个core，那么最多一次性开K*2个文件描述符。</p>
<p>在reduce阶段(shuffle read)，reduce task拉取数据做combine时不再是采用<code>HashMap</code>，而是采用<code>ExternalAppendOnlyMap</code>，该数据结构在做combine时，如果内存不足，会刷写磁盘，很大程度的保证了鲁棒性，避免大数据情况下的OOM。</p>
<p>总体上看来Sort Shuffle解决了Hash Shuffle的所有弊端，但是因为需要其shuffle过程需要对记录进行排序，所以在性能上有所损失。</p>
<h3 id="Unsafe-Shuffle"><a href="#Unsafe-Shuffle" class="headerlink" title="Unsafe Shuffle"></a>Unsafe Shuffle</h3><p>从spark 1.5.0开始，spark开始了钨丝计划(Tungsten)，目的是优化内存和CPU的使用，进一步提升spark的性能。为此，引入Unsafe Shuffle，它的做法是将数据记录用二进制的方式存储，直接在序列化的二进制数据上sort而不是在java 对象上，这样一方面可以减少memory的使用和GC的开销，另一方面避免shuffle过程中频繁的序列化以及反序列化。在排序过程中，它提供cache-efficient sorter，使用一个8 bytes的指针，把排序转化成了一个指针数组的排序，极大的优化了排序性能。更多Tungsten详细介绍请移步<a href="https://databricks.com/blog/2015/04/28/project-tungsten-bringing-spark-closer-to-bare-metal.html" target="_blank" rel="external">databricks博客</a>。</p>
<p>但是使用Unsafe Shuffle有几个限制，shuffle阶段不能有aggregate操作，分区数不能超过一定大小($2^{24}-1$，这是可编码的最大parition id)，所以像reduceByKey这类有aggregate操作的算子是不能使用Unsafe Shuffle，它会退化采用Sort Shuffle。</p>
<h3 id="Sort-Shuffle-v2"><a href="#Sort-Shuffle-v2" class="headerlink" title="Sort Shuffle v2"></a>Sort Shuffle v2</h3><p>从spark-1.6.0开始，把Sort Shuffle和Unsafe Shuffle全部统一到Sort Shuffle中，如果检测到满足Unsafe Shuffle条件会自动采用Unsafe Shuffle，否则采用Sort Shuffle。从spark-2.0.0开始，spark把Hash Shuffle移除，可以说目前spark-2.0中只有一种Shuffle，即为Sort Shuffle。</p>
<h2 id="Spark-Shuffle相关调优"><a href="#Spark-Shuffle相关调优" class="headerlink" title="Spark Shuffle相关调优"></a>Spark Shuffle相关调优</h2><p>从上述shuffle的原理介绍可以知道，shuffle是一个涉及到CPU(序列化反序列化)、网络IO(跨节点数据传输)以及磁盘IO(shuffle中间结果落地)的操作，用户在编写spark应用程序的时候应当尽可能考虑shuffle相关的优化，提升spark应用程序的性能。下面简单列举几点关于spark shuffle调优的参考。</p>
<ul>
<li>尽量减少shuffle次数</li>
</ul>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 两次shuffle</span></span><br><span class="line">rdd.map(...).repartition(<span class="number">1000</span>).reduceByKey(_ + _, <span class="number">3000</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 一次shuffle</span></span><br><span class="line">rdd.map(...).repartition(<span class="number">3000</span>).reduceByKey(_ + _)</span><br></pre></td></tr></table></figure>
<ul>
<li>必要时主动shuffle，通常用于改变并行度，提高后续分布式运行速度</li>
</ul>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">rdd.repartiton(largerNumPartition).map(...)...</span><br></pre></td></tr></table></figure>
<ul>
<li>使用treeReduce &amp; treeAggregate替换reduce &amp; aggregate。数据量较大时，reduce &amp; aggregate一次性聚合，shuffle量太大，而treeReduce &amp; treeAggregate是分批聚合，更为保险。</li>
</ul>
<h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>本文详细阐述了spark shuffle的原理以及实现演进，清楚地知道shuffle原理有助于调优应用程序，并了解应用程序执行的每个过程。</p>
<p><span style="color:red"><em>转载请注明出处，本文永久链接：<a href="http://sharkdtu.com/posts/spark-shuffle.html">http://sharkdtu.com/posts/spark-shuffle.html</a></em></span></p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[Spark Scheduler内部原理剖析]]></title>
      <url>http://sharkdtu.com/posts/spark-scheduler.html</url>
      <content type="html"><![CDATA[<p>通过文章<a href="/posts/spark-rdd.html">“Spark核心概念RDD”</a>我们知道，Spark的核心是根据RDD来实现的，Spark Scheduler则为Spark核心实现的重要一环，其作用就是任务调度。Spark的任务调度就是如何组织任务去处理RDD中每个分区的数据，根据RDD的依赖关系构建DAG，基于DAG划分Stage，将每个Stage中的任务发到指定节点运行。基于Spark的任务调度原理，我们可以合理规划资源利用，做到尽可能用最少的资源高效地完成任务计算。<a id="more"></a></p>
<h2 id="分布式运行框架"><a href="#分布式运行框架" class="headerlink" title="分布式运行框架"></a>分布式运行框架</h2><p>Spark可以部署在多种资源管理平台，例如Yarn、Mesos等，Spark本身也实现了一个简易的资源管理机制，称之为Standalone模式。由于工作中接触较多的是Saprk on Yarn，不做特别说明，以下所述均表示Spark-on-Yarn。Spark部署在Yarn上有两种运行模式，分别为yarn-client和yarn-cluster模式，它们的区别仅仅在于Spark Driver是运行在Client端还是ApplicationMater端。如下图所示为Spark部署在Yarn上，以yarn-cluster模式运行的分布式计算框架。</p>
<p><img src="/images/spark-distribution-framework.png" width="600" height="430" alt="spark-on-yarn-distribution-framework" align="center"></p>
<p>其中蓝色部分是Spark里的概念，包括Client、ApplicationMaster、Driver和Executor，其中Client和ApplicationMaster主要是负责与Yarn进行交互；Driver作为Spark应用程序的总控，负责分发任务以及监控任务运行状态；Executor负责执行任务，并上报状态信息给Driver，从逻辑上来看Executor是进程，运行在其中的任务是线程，所以说Spark的任务是线程级别的。通过下面的时序图可以更清晰地理解一个Spark应用程序从提交到运行的完整流程。</p>
<p><img src="/images/spark-submit-time.png" width="600" height="430" alt="spark-submit-time" align="center"></p>
<p>提交一个Spark应用程序，首先通过Client向ResourceManager请求启动一个Application，同时检查是否有足够的资源满足Application的需求，如果资源条件满足，则准备ApplicationMaster的启动上下文，交给ResourceManager，并循环监控Application状态。</p>
<p>当提交的资源队列中有资源时，ResourceManager会在某个NodeManager上启动ApplicationMaster进程，ApplicationMaster会单独启动Driver后台线程，当Driver启动后，ApplicationMaster会通过本地的RPC连接Driver，并开始向ResourceManager申请Container资源运行Executor进程（一个Executor对应与一个Container），当ResourceManager返回Container资源，则在对应的Container上启动Executor。</p>
<p>Driver线程主要是初始化SparkContext对象，准备运行所需的上下文，然后一方面保持与ApplicationMaster的RPC连接，通过ApplicationMaster申请资源，另一方面根据用户业务逻辑开始调度任务，将任务下发到已有的空闲Executor上。</p>
<p>当ResourceManager向ApplicationMaster返回Container资源时，ApplicationMaster就尝试在对应的Container上启动Executor进程，Executor进程起来后，会向Driver注册，注册成功后保持与Driver的心跳，同时等待Driver分发任务，当分发的任务执行完毕后，将任务状态上报给Driver。</p>
<blockquote>
<p>Driver把资源申请的逻辑给抽象出来，以适配不同的资源管理系统，所以才间接地通过ApplicationMaster去和Yarn打交道。</p>
</blockquote>
<p>从上述时序图可知，Client只管提交Application并监控Application的状态。对于Spark的任务调度主要是集中在两个方面: 资源申请和任务分发，其主要是通过ApplicationMaster、Driver以及Executor之间来完成，下面详细剖析Spark任务调度每个细节。</p>
<h2 id="Spark任务调度总览"><a href="#Spark任务调度总览" class="headerlink" title="Spark任务调度总览"></a>Spark任务调度总览</h2><p>当Driver起来后，Driver则会根据用户程序逻辑准备任务，并根据Executor资源情况逐步分发任务。在详细阐述任务调度前，首先说明下Spark里的几个概念。一个Spark应用程序包括Job、Stage以及Task三个概念：</p>
<ul>
<li>Job是以Action方法为界，遇到一个Action方法则触发一个Job；</li>
<li>Stage是Job的子集，以RDD宽依赖(即Shuffle)为界，遇到Shuffle做一次划分；</li>
<li>Task是Stage的子集，以并行度(分区数)来衡量，分区数是多少，则有多少个task。</li>
</ul>
<p>Spark的任务调度总体来说分两路进行，一路是Stage级的调度，一路是Task级的调度，总体调度流程如下图所示。</p>
<p><img src="/images/spark-scheduler-overview.png" width="600" height="430" alt="spark-scheduler-overview" align="center"></p>
<p>Spark RDD通过其Transactions操作，形成了RDD血缘关系图，即DAG，最后通过Action的调用，触发Job并调度执行。DAGScheduler负责Stage级的调度，主要是将DAG切分成若干Stages，并将每个Stage打包成TaskSet交给TaskScheduler调度。TaskScheduler负责Task级的调度，将DAGScheduler给过来的TaskSet按照指定的调度策略分发到Executor上执行，调度过程中SchedulerBackend负责提供可用资源，其中SchedulerBackend有多种实现，分别对接不同的资源管理系统。有了上述感性的认识后，下面这张图描述了Spark-On-Yarn模式下在任务调度期间，ApplicationMaster、Driver以及Executor内部模块的交互过程。</p>
<p><img src="/images/spark-scheduler-detail.png" width="600" height="430" alt="spark-scheduler-detail" align="center"></p>
<p>Driver初始化SparkContext过程中，会分别初始化DAGScheduler、TaskScheduler、SchedulerBackend以及HeartbeatReceiver，并启动SchedulerBackend以及HeartbeatReceiver。SchedulerBackend通过ApplicationMaster申请资源，并不断从TaskScheduler中拿到合适的Task分发到Executor执行。HeartbeatReceiver负责接收Executor的心跳信息，监控Executor的存活状况，并通知到TaskScheduler。下面着重剖析DAGScheduler负责的Stage调度以及TaskScheduler负责的Task调度。</p>
<h3 id="Stage级的调度"><a href="#Stage级的调度" class="headerlink" title="Stage级的调度"></a>Stage级的调度</h3><p>Spark的任务调度是从DAG切割开始，主要是由DAGScheduler来完成。当遇到一个Action操作后就会触发一个Job的计算，并交给DAGScheduler来提交，下图是涉及到Job提交的相关方法调用流程图。</p>
<p><img src="/images/spark-scheduler-dag-process.png" width="600" height="430" alt="spark-scheduler-dag-process" align="center"></p>
<p>Job由最终的RDD和Action方法封装而成，SparkContext将Job交给DAGScheduler提交，它会根据RDD的血缘关系构成的DAG进行切分，将一个Job划分为若干Stages，具体划分策略是，由最终的RDD不断通过依赖回溯判断父依赖是否是款依赖，即以Shuffle为界，划分Stage，窄依赖的RDD之间被划分到同一个Stage中，可以进行pipeline式的计算，如上图紫色流程部分。划分的Stages分两类，一类叫做ResultStage，为DAG最下游的Stage，由Action方法决定，另一类叫做ShuffleMapStage，为下游Stage准备数据，下面看一个简单的例子WordCount。</p>
<p><img src="/images/spark-scheduler-dag-wordcount.png" width="600" height="430" alt="spark-scheduler-dag-wordcount" align="center"></p>
<p>Job由<code>saveAsTextFile</code>触发，该Job由RDD-3和<code>saveAsTextFile</code>方法组成，根据RDD之间的依赖关系从RDD-3开始回溯搜索，直到没有依赖的RDD-0，在回溯搜索过程中，RDD-3依赖RDD-2，并且是宽依赖，所以在RDD-2和RDD-3之间划分Stage，RDD-3被划到最后一个Stage，即ResultStage中，RDD-2依赖RDD-1，RDD-1依赖RDD-0，这些依赖都是窄依赖，所以将RDD-0、RDD-1和RDD-2划分到同一个Stage，即ShuffleMapStage中，实际执行的时候，数据记录会一气呵成地执行RDD-0到RDD-2的转化。不难看出，其本质上是一个深度优先搜索算法。</p>
<p>一个Stage是否被提交，需要判断它的父Stage是否执行，只有在父Stage执行完毕才能提交当前Stage，如果一个Stage没有父Stage，那么从该Stage开始提交。Stage提交时会将Task信息（分区信息以及方法等）序列化并被打包成TaskSet交给TaskScheduler，一个Partition对应一个Task，另一方面监控Stage的运行状态，只有Executor丢失或者Task由于Fetch失败才需要重新提交失败的Stage以调度运行失败的任务，其他类型的Task失败会在TaskScheduler的调度过程中重试。</p>
<p>相对来说DAGScheduler做的事情较为简单，仅仅是在Stage层面上划分DAG，提交Stage并监控相关状态信息。TaskScheduler则相对较为复杂，下面详细阐述其细节。</p>
<h3 id="Task级的调度"><a href="#Task级的调度" class="headerlink" title="Task级的调度"></a>Task级的调度</h3><p>Spark Task的调度是由TaskScheduler来完成，由前文可知，DAGScheduler将Stage打包到TaskSet交给TaskScheduler，TaskScheduler会将其封装为TaskSetManager加入到调度队列中，TaskSetManager负责监控管理同一个Stage中的Tasks，TaskScheduler就是以TaskSetManager为单元来调度任务。前面也提到，TaskScheduler初始化后会启动SchedulerBackend，它负责跟外界打交道，接收Executor的注册信息，并维护Executor的状态，所以说SchedulerBackend是管“粮食”的，同时它在启动后会定期地去“询问”TaskScheduler有没有任务要运行，也就是说，它会定期地“问”TaskScheduler“我有这么余量，你要不要啊”，TaskScheduler在SchedulerBackend“问”它的时候，会从调度队列中按照指定的调度策略选择TaskSetManager去调度运行，大致方法调用流程如下图所示。</p>
<p><img src="/images/spark-scheduler-task-process.png" width="600" height="430" alt="spark-scheduler-task-process" align="center"></p>
<h4 id="调度策略"><a href="#调度策略" class="headerlink" title="调度策略"></a>调度策略</h4><p>前面讲到，TaskScheduler会先把DAGScheduler给过来的TaskSet封装成TaskSetManager扔到任务队列里，然后再从任务队列里按照一定的规则把它们取出来在SchedulerBackend给过来的Executor上运行。这个调度过程实际上还是比较粗粒度的，是面向TaskSetManager的。</p>
<p>TaskScheduler是以树的方式来管理任务队列，树中的节点类型为Schdulable，叶子节点为TaskSetManager，非叶子节点为Pool，下图是它们之间的继承关系。</p>
<p><img src="/images/spark-scheduler-pool.png" width="600" height="430" alt="spark-scheduler-pool" align="center"></p>
<p>TaskScheduler支持两种调度策略，一种是FIFO，也是默认的调度策略，另一种是FAIR。在TaskScheduler初始化过程中会实例化rootPool，表示树的根节点，是Pool类型。如果是采用FIFO调度策略，则直接简单地将TaskSetManager按照先来先到的方式入队，出队时直接拿出最先进队的TaskSetManager，其树结构大致如下图所示，TaskSetManager保存在一个FIFO队列中。</p>
<p><img src="/images/spark-scheduler-fifo-tree.png" width="600" height="430" alt="spark-scheduler-fifo-tree" align="center"></p>
<p>在阐述FAIR调度策略前，先贴一段使用FAIR调度策略的应用程序代码，后面针对该代码逻辑来详细阐述FAIR调度的实现细节。</p>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">object</span> <span class="title">MultiJobTest</span> </span>&#123;</span><br><span class="line">  <span class="comment">// spark.scheduler.mode=FAIR</span></span><br><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">main</span></span>(args: <span class="type">Array</span>[<span class="type">String</span>]): <span class="type">Unit</span> = &#123;</span><br><span class="line">    <span class="keyword">val</span> spark = <span class="type">SparkSession</span>.builder().getOrCreate()</span><br><span class="line"></span><br><span class="line">    <span class="keyword">val</span> rdd = spark.sparkContext.textFile(...)</span><br><span class="line">      .map(_.split(<span class="string">"\\s+"</span>))</span><br><span class="line">      .map(x =&gt; (x(<span class="number">0</span>), x(<span class="number">1</span>)))</span><br><span class="line"></span><br><span class="line">    <span class="keyword">val</span> jobExecutor = <span class="type">Executors</span>.newFixedThreadPool(<span class="number">2</span>)</span><br><span class="line"></span><br><span class="line">    jobExecutor.execute(<span class="keyword">new</span> <span class="type">Runnable</span> &#123;</span><br><span class="line">      <span class="keyword">override</span> <span class="function"><span class="keyword">def</span> <span class="title">run</span></span>(): <span class="type">Unit</span> = &#123;</span><br><span class="line">        spark.sparkContext.setLocalProperty(<span class="string">"spark.scheduler.pool"</span>, <span class="string">"count-pool"</span>)</span><br><span class="line">        <span class="keyword">val</span> cnt = rdd.groupByKey().count()</span><br><span class="line">        println(<span class="string">s"Count: <span class="subst">$cnt</span>"</span>)</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;)</span><br><span class="line"></span><br><span class="line">    jobExecutor.execute(<span class="keyword">new</span> <span class="type">Runnable</span> &#123;</span><br><span class="line">      <span class="keyword">override</span> <span class="function"><span class="keyword">def</span> <span class="title">run</span></span>(): <span class="type">Unit</span> = &#123;</span><br><span class="line">        spark.sparkContext.setLocalProperty(<span class="string">"spark.scheduler.pool"</span>, <span class="string">"take-pool"</span>)</span><br><span class="line">        <span class="keyword">val</span> data = rdd.sortByKey().take(<span class="number">10</span>)</span><br><span class="line">        println(<span class="string">s"Data Samples: "</span>)</span><br><span class="line">        data.foreach &#123; x =&gt; println(x.mkString(<span class="string">", "</span>)) &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;)</span><br><span class="line"></span><br><span class="line">    jobExecutor.shutdown()</span><br><span class="line">    <span class="keyword">while</span> (!jobExecutor.isTerminated) &#123;&#125;</span><br><span class="line">    println(<span class="string">"Done!"</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>上述应用程序中使用两个线程分别调用了Action方法，即有两个Job会并发提交，但是不管怎样，这两个Job被切分成若干TaskSet后终究会被交到TaskScheduler这里统一管理，其调度树大致如下图所示。</p>
<p><img src="/images/spark-scheduler-fair-tree.png" width="600" height="430" alt="spark-scheduler-fair-tree" align="center"></p>
<p>在出队时，则会对所有TaskSetManager排序，具体排序过程是从根节点<code>rootPool</code>开始，递归地去排序子节点，最后合并到一个<code>ArrayBuffer</code>里，代码逻辑如下。</p>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">getSortedTaskSetQueue</span></span>: <span class="type">ArrayBuffer</span>[<span class="type">TaskSetManager</span>] = &#123;</span><br><span class="line">    <span class="keyword">var</span> sortedTaskSetQueue = <span class="keyword">new</span> <span class="type">ArrayBuffer</span>[<span class="type">TaskSetManager</span>]</span><br><span class="line">    <span class="keyword">val</span> sortedSchedulableQueue = schedulableQueue.asScala.toSeq.sortWith(taskSetSchedulingAlgorithm.comparator)</span><br><span class="line">    <span class="keyword">for</span> (schedulable &lt;- sortedSchedulableQueue) &#123;</span><br><span class="line">      sortedTaskSetQueue ++= schedulable.getSortedTaskSetQueue</span><br><span class="line">    &#125;</span><br><span class="line">    sortedTaskSetQueue</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>
<p>使用FAIR调度策略时，上面代码中的<code>taskSetSchedulingAlgorithm</code>的类型为<code>FairSchedulingAlgorithm</code>，排序过程的比较是基于Fair-share来比较的，每个要排序的对象包含三个属性: <code>runningTasks</code>值（正在运行的Task数）、<code>minShare</code>值、<code>weight</code>值，比较时会综合考量<code>runningTasks</code>值，<code>minShare</code>以及<code>weight</code>值。如果A对象的<code>runningTasks</code>大于它的<code>minShare</code>，B对象的<code>runningTasks</code>小于它的<code>minShare</code>，那么B排在A前面；如果A、B对象的<code>runningTasks</code>都小于它们的<code>minShare</code>，那么就比较<code>runningTasks</code>与<code>minShare</code>的比值，谁小谁排前面；如果A、B对象的<code>runningTasks</code>都大于它们的<code>minShare</code>，那么就比较<code>runningTasks</code>与<code>weight</code>的比值，谁小谁排前面。整体上来说就是通过<code>minShare</code>和<code>weight</code>这两个参数控制比较过程，可以做到不让资源被某些长时间Task给一直占了。</p>
<p>从调度队列中拿到TaskSetManager后，那么接下来的工作就是TaskSetManager按照一定的规则一个个取出Task给TaskScheduler，TaskScheduler再交给SchedulerBackend去发到Executor上执行。前面也提到，TaskSetManager封装了一个Stage的所有Task，并负责管理调度这些Task。</p>
<h4 id="本地化调度"><a href="#本地化调度" class="headerlink" title="本地化调度"></a>本地化调度</h4><p>从调度队列中拿到TaskSetManager后，那么接下来的工作就是TaskSetManager按照一定的规则一个个取出Task给TaskScheduler，TaskScheduler再交给SchedulerBackend去发到Executor上执行。前面也提到，TaskSetManager封装了一个Stage的所有Task，并负责管理调度这些Task。</p>
<p><img src="/images/spark-scheduler-taskset-process.png" width="600" height="430" alt="spark-scheduler-taskset-process" align="center"></p>
<p>在TaskSetManager初始化过程中，会对Tasks按照Locality级别进行分类，Task的Locality有五种，优先级由高到低顺序：PROCESS_LOCAL(指定的Executor)，NODE_LOCAL(指定的主机节点)，NO_PREF(无所谓)，RACK_LOCAL(指定的机架)，ANY(满足不了Task的Locality就随便调度)。这五种Locality级别存在包含关系，RACK_LOCAL包含NODE_LOCAL，NODE_LOCAL包含PROCESS_LOCAL，然而ANY包含其他所有四种。初始化阶段在对Task分类时，根据Task的preferredLocations判断它属于哪个Locality级别，属于PROCESS_LOCAL的Task同时也会被加入到NODE_LOCAL、RACK_LOCAL类别中，比如，一个Task的preferredLocations指定了在Executor-2上执行，那么它属于Executor-2对应的PROCESS_LOCAL类别，同时也把他加入到Executor-2所在的主机对应的NODE_LOCAL类别，Executor-2所在的主机的机架对应的RACK_LOCAL类别中，以及ANY类别，这样在调度执行时，满足不了PROCESS_LOCAL，就逐步退化到NODE_LOCAL，RACK_LOCAL，ANY。</p>
<p>TaskSetManager在决定调度哪些Task时，是通过上面流程图中的resourceOffer方法来实现，为了尽可能地将Task调度到它的preferredLocations上，它采用一种延迟调度算法。resourceOffer方法原型如下，参数包括要调度任务的Executor Id、主机地址以及最大可容忍的Locality级别。</p>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">resourceOffer</span></span>(</span><br><span class="line">      execId: <span class="type">String</span>,</span><br><span class="line">      host: <span class="type">String</span>,</span><br><span class="line">      maxLocality: <span class="type">TaskLocality</span>.<span class="type">TaskLocality</span>)</span><br><span class="line">    : <span class="type">Option</span>[<span class="type">TaskDescription</span>]</span><br></pre></td></tr></table></figure>
<p>延迟调度算法的大致流程如下图所示。</p>
<p><img src="/images/spark-scheduler-task-locatity-process.png" width="600" height="430" alt="spark-scheduler-task-locatity-process" align="center"></p>
<p>首先看是否存在execId对应的PROCESS_LOCAL类别的任务，如果存在，取出来调度，否则根据当前时间，判断是否超过了PROCESS_LOCAL类别最大容忍的延迟，如果超过，则退化到下一个级别NODE_LOCAL，否则等待不调度。退化到下一个级别NODE_LOCAL后调度流程也类似，看是否存在host对应的NODE_LOCAL类别的任务，如果存在，取出来调度，否则根据当前时间，判断是否超过了NODE_LOCAL类别最大容忍的延迟，如果超过，则退化到下一个级别RACK_LOCAL，否则等待不调度，以此类推…..。当不满足Locatity类别会选择等待，直到下一轮调度重复上述流程，如果你比较激进，可以调大每个类别的最大容忍延迟时间，如果不满足Locatity时就会等待多个调度周期，直到满足或者超过延迟时间退化到下一个级别为止。</p>
<h4 id="失败重试与黑名单机制"><a href="#失败重试与黑名单机制" class="headerlink" title="失败重试与黑名单机制"></a>失败重试与黑名单机制</h4><p>除了选择合适的Task调度运行外，还需要监控Task的执行状态，前面也提到，与外部打交道的是SchedulerBackend，Task被提交到Executor启动执行后，Executor会将执行状态上报给SchedulerBackend，SchedulerBackend则告诉TaskScheduler，TaskScheduler找到该Task对应的TaskSetManager，并通知到该TaskSetManager，这样TaskSetManager就知道Task的失败与成功状态，对于失败的Task，会记录它失败的次数，如果失败次数还没有超过最大重试次数，那么就把它放回待调度的Task池子中，否则整个Application失败。</p>
<p>在记录Task失败次数过程中，会记录它上一次失败所在的Executor Id和Host，这样下次再调度这个Task时，会使用黑名单机制，避免它被调度到上一次失败的节点上，起到一定的容错作用。黑名单记录Task上一次失败所在的Executor Id和Host，以及其对应的“黑暗”时间，“黑暗”时间是指这段时间内不要再往这个节点上调度这个Task了。</p>
<h4 id="推测式执行"><a href="#推测式执行" class="headerlink" title="推测式执行"></a>推测式执行</h4><p>TaskScheduler在启动SchedulerBackend后，还会启动一个后台线程专门负责推测任务的调度，推测任务是指对一个Task在不同的Executor上启动多个实例，如果有Task实例运行成功，则会干掉其他Executor上运行的实例。推测调度线程会每隔固定时间检查是否有Task需要推测执行，如果有，则会调用SchedulerBackend的reviveOffers去尝试拿资源运行推测任务。</p>
<p><img src="/images/spark-scheduler-speculation-process.png" width="600" height="430" alt="spark-scheduler-speculation-process" align="center"></p>
<p>检查是否有Task需要推测执行的逻辑最后会交到TaskSetManager，TaskSetManager采用基于统计的算法，检查Task是否需要推测执行，算法流程大致如下图所示。</p>
<p><img src="/images/spark-scheduler-speculation-check.png" width="600" height="430" alt="spark-scheduler-speculation-check" align="center"></p>
<p>TaskSetManager首先会统计成功的Task数，当成功的Task数超过75%(可通过参数<code>spark.speculation.quantile</code>控制)时，再统计所有成功的Tasks的运行时间，得到一个中位数，用这个中位数乘以1.5(可通过参数<code>spark.speculation.multiplier</code>控制)得到运行时间门限，如果在运行的Tasks的运行时间超过这个门限，则对它启用推测。算法逻辑较为简单，其实就是对那些拖慢整体进度的Tasks启用推测，以加速整个TaskSet即Stage的运行。</p>
<h2 id="资源申请机制"><a href="#资源申请机制" class="headerlink" title="资源申请机制"></a>资源申请机制</h2><p>在前文已经提过，ApplicationMaster和SchedulerBackend起来后，SchedulerBackend通过ApplicationMaster申请资源，ApplicationMaster就是用来专门适配YARN申请Container资源的，当申请到Container，会在相应Container上启动Executor进程，其他事情就交给SchedulerBackend。Spark早期版本只支持静态资源申请，即一开始就指定用多少资源，在整个Spark应用程序运行过程中资源都不能改变，后来支持动态Executor申请，用户不需要指定确切的Executor数量，Spark会动态调整Executor的数量以达到资源利用的最大化。</p>
<h3 id="静态资源申请"><a href="#静态资源申请" class="headerlink" title="静态资源申请"></a>静态资源申请</h3><p>静态资源申请是用户在提交Spark应用程序时，就要提前估计应用程序需要使用的资源，包括Executor数(num_executor)、每个Executor上的core数(executor_cores)、每个Executor的内存(executor_memory)以及Driver的内存(driver_memory)。</p>
<p>在估计资源使用时，应当首先了解这些资源是怎么用的。任务的并行度由分区数(Partitions)决定，一个Stage有多少分区，就会有多少Task。每个Task默认占用一个Core，一个Executor上的所有core共享Executor上的内存，一次并行运行的Task数等于num_executor*executor_cores，如果分区数超过该值，则需要运行多个轮次，一般来说建议运行3～5轮较为合适，否则考虑增加num_executor或executor_cores。由于一个Executor的所有tasks会共享内存executor_memory，所以建议executor_cores不宜过大。executor_memory的设置则需要综合每个分区的数据量以及是否有缓存等逻辑。下图描绘了一个应用程序内部资源利用情况。</p>
<p><img src="/images/spark-scheduler-resource.png" width="600" height="430" alt="spark-scheduler-resource" align="center"></p>
<h3 id="动态资源申请"><a href="#动态资源申请" class="headerlink" title="动态资源申请"></a>动态资源申请</h3><p>动态资源申请目前只支持到Executor，即可以不用指定num_executor，通过参数spark.dynamicAllocation.enabled来控制。由于许多Spark应用程序一开始可能不需要那么多Executor或者其本身就不需要太多Executor，所以不必一次性申请那么多Executor，根据具体的任务数动态调整Executor的数量，尽可能做到资源的不浪费。由于动态Executor的调整会导致Executor动态的添加与删除，如果删除Executor，其上面的中间Shuffle结果可能会丢失，这就需要借助第三方的ShuffleService了，如果Spark是部署在Yarn上，则可以在Yarn上配置Spark的ShuffleService，具体操作仅需做两点:</p>
<ol>
<li><p>首先在yarn-site.xml中加上如下配置：</p>
<figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">property</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">name</span>&gt;</span>yarn.nodemanager.aux-services<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">value</span>&gt;</span>mapreduce_shuffle,spark_shuffle<span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">property</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">name</span>&gt;</span>yarn.nodemanager.aux-services.spark_shuffle.class<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">value</span>&gt;</span>org.apache.spark.network.yarn.YarnShuffleService<span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">property</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">name</span>&gt;</span>spark.shuffle.service.port<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">value</span>&gt;</span>7337<span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>将Spark ShuffleService jar包\$SPARK_HOME/lib/spark-*-yarn-shuffle.jar拷贝到每台NodeManager的\$HADOOP_HOME/share/hadoop/yarn/lib/下，并重启所有的NodeManager。</p>
</li>
</ol>
<p>当启用动态Executor申请时，在SparkContext初始化过程中会实例化ExecutorAllocationManager，它是被用来专门控制动态Executor申请逻辑的，动态Executor申请是一种基于当前Task负载压力实现动态增删Executor的机制。一开始会按照参数spark.dynamicAllocation.initialExecutors设置的初始Executor数申请，然后根据当前积压的Task数量，逐步增长申请的Executor数，如果当前有积压的Task，那么取积压的Task数和spark.dynamicAllocation.maxExecutors中的最小值作为Executor数上限，每次新增加申请的Executor为2的次方，即第一次增加1，第二次增加2，第三次增加4，…。另一方面，如果一个Executor在一段时间内都没有Task运行，则将其回收，但是在Remove Executor时，要保证最少的Executor数，该值通过参数spark.dynamicAllocation.minExecutors来控制，如果Executor上有Cache的数据，则永远不会被Remove，以保证中间数据不丢失。</p>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>本文详细阐述了Spark的任务调度，着重讨论Spark on Yarn的部署调度，剖析了从应用程序提交到运行的全过程。Spark Schedule算是Spark中的一个大模块，它负责任务下发与监控等，基本上扮演了Spark大脑的角色。了解Spark Schedule有助于帮助我们清楚地认识Spark应用程序的运行轨迹，同时在我们实现其他系统时，也可以借鉴Spark的实现。</p>
<p><span style="color:red"><em>转载请注明出处，本文永久链接：<a href="http://sharkdtu.com/posts/spark-scheduler.html">http://sharkdtu.com/posts/spark-scheduler.html</a></em></span></p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[Spark核心概念RDD]]></title>
      <url>http://sharkdtu.com/posts/spark-rdd.html</url>
      <content type="html"><![CDATA[<p>RDD全称叫做弹性分布式数据集(Resilient Distributed Datasets)，它是一种分布式的内存抽象，表示一个只读的记录分区的集合，它只能通过其他RDD转换而创建，为此，RDD支持丰富的转换操作(如map, join, filter, groupBy等)，通过这种转换操作，新的RDD则包含了如何从其他RDDs衍生所必需的信息，所以说RDDs之间是有依赖关系的。基于RDDs之间的依赖，RDDs会形成一个有向无环图DAG，该DAG描述了整个流式计算的流程，实际执行的时候，RDD是通过血缘关系(Lineage)一气呵成的，即使出现数据分区丢失，也可以通过血缘关系重建分区，总结起来，基于RDD的流式计算任务可描述为：从稳定的物理存储(如分布式文件系统)中加载记录，记录被传入由一组确定性操作构成的DAG，然后写回稳定存储。<a id="more"></a>另外RDD还可以将数据集缓存到内存中，使得在多个操作之间可以重用数据集，基于这个特点可以很方便地构建迭代型应用(图计算、机器学习等)或者交互式数据分析应用。可以说Spark最初也就是实现RDD的一个分布式系统，后面通过不断发展壮大成为现在较为完善的大数据生态系统，简单来讲，Spark-RDD的关系类似于Hadoop-MapReduce关系。</p>
<h2 id="RDD特点"><a href="#RDD特点" class="headerlink" title="RDD特点"></a>RDD特点</h2><p>RDD表示只读的分区的数据集，对RDD进行改动，只能通过RDD的转换操作，由一个RDD得到一个新的RDD，新的RDD包含了从其他RDD衍生所必需的信息。RDDs之间存在依赖，RDD的执行是按照血缘关系延时计算的。如果血缘关系较长，可以通过持久化RDD来切断血缘关系。</p>
<h3 id="分区"><a href="#分区" class="headerlink" title="分区"></a>分区</h3><p>如下图所示，RDD逻辑上是分区的，每个分区的数据是抽象存在的，计算的时候会通过一个compute函数得到每个分区的数据。如果RDD是通过已有的文件系统构建，则compute函数是读取指定文件系统中的数据，如果RDD是通过其他RDD转换而来，则compute函数是执行转换逻辑将其他RDD的数据进行转换。</p>
<p><img src="/images/rdd-partition.png" width="400" height="230" alt="rdd-partition" align="center"></p>
<h3 id="只读"><a href="#只读" class="headerlink" title="只读"></a>只读</h3><p>如下图所示，RDD是只读的，要想改变RDD中的数据，只能在现有的RDD基础上创建新的RDD。</p>
<p><img src="/images/rdd-readonly.png" width="400" height="230" alt="rdd-readonly" align="center"></p>
<p>由一个RDD转换到另一个RDD，可以通过丰富的操作算子实现，不再像MapReduce那样只能写map和reduce了，如下图所示。</p>
<p><img src="/images/rdd-transform.png" width="400" height="230" alt="rdd-transform" align="center"></p>
<p>RDD的操作算子包括两类，一类叫做transformations，它是用来将RDD进行转化，构建RDD的血缘关系；另一类叫做actions，它是用来触发RDD的计算，得到RDD的相关计算结果或者将RDD保存的文件系统中。下图是RDD所支持的操作算子列表。</p>
<p><img src="/images/rdd-transformations-actions.png" width="400" height="230" alt="rdd-transforms-actions" align="center"></p>
<h3 id="依赖"><a href="#依赖" class="headerlink" title="依赖"></a>依赖</h3><p>RDDs通过操作算子进行转换，转换得到的新RDD包含了从其他RDDs衍生所必需的信息，RDDs之间维护着这种血缘关系，也称之为依赖。如下图所示，依赖包括两种，一种是窄依赖，RDDs之间分区是一一对应的，另一种是宽依赖，下游RDD的每个分区与上游RDD(也称之为父RDD)的每个分区都有关，是多对多的关系。</p>
<p><img src="/images/rdd-dependency.png" width="400" height="230" alt="rdd-dependency" align="center"></p>
<p>通过RDDs之间的这种依赖关系，一个任务流可以描述为DAG(有向无环图)，如下图所示，在实际执行过程中宽依赖对应于Shuffle(图中的reduceByKey和join)，窄依赖中的所有转换操作可以通过类似于管道的方式一气呵成执行(图中map和union可以一起执行)。</p>
<p><img src="/images/rdd-dag.png" width="500" height="300" alt="rdd-dag" align="center"></p>
<h3 id="缓存"><a href="#缓存" class="headerlink" title="缓存"></a>缓存</h3><p>如果在应用程序中多次使用同一个RDD，可以将该RDD缓存起来，该RDD只有在第一次计算的时候会根据血缘关系得到分区的数据，在后续其他地方用到该RDD的时候，会直接从缓存处取而不用再根据血缘关系计算，这样就加速后期的重用。如下图所示，RDD-1经过一系列的转换后得到RDD-n并保存到hdfs，RDD-1在这一过程中会有个中间结果，如果将其缓存到内存，那么在随后的RDD-1转换到RDD-m这一过程中，就不会计算其之前的RDD-0了。</p>
<p><img src="/images/rdd-cache.png" width="500" height="300" alt="rdd-cache" align="center"></p>
<h3 id="checkpoint"><a href="#checkpoint" class="headerlink" title="checkpoint"></a>checkpoint</h3><p>虽然RDD的血缘关系天然地可以实现容错，当RDD的某个分区数据失败或丢失，可以通过血缘关系重建。但是对于长时间迭代型应用来说，随着迭代的进行，RDDs之间的血缘关系会越来越长，一旦在后续迭代过程中出错，则需要通过非常长的血缘关系去重建，势必影响性能。为此，RDD支持checkpoint将数据保存到持久化的存储中，这样就可以切断之前的血缘关系，因为checkpoint后的RDD不需要知道它的父RDDs了，它可以从checkpoint处拿到数据。</p>
<h3 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h3><p>总结起来，给定一个RDD我们至少可以知道如下几点信息：1、分区数以及分区方式；2、由父RDDs衍生而来的相关依赖信息；3、计算每个分区的数据，计算步骤为：1）如果被缓存，则从缓存中取的分区的数据；2）如果被checkpoint，则从checkpoint处恢复数据；3）根据血缘关系计算分区的数据。</p>
<h2 id="编程模型"><a href="#编程模型" class="headerlink" title="编程模型"></a>编程模型</h2><p>在Spark中，RDD被表示为对象，通过对象上的方法调用来对RDD进行转换。经过一系列的transformations定义RDD之后，就可以调用actions触发RDD的计算，action可以是向应用程序返回结果(count, collect等)，或者是向存储系统保存数据(saveAsTextFile等)。在Spark中，只有遇到action，才会执行RDD的计算(即延迟计算)，这样在运行时可以通过管道的方式传输多个转换。</p>
<p>要使用Spark，开发者需要编写一个Driver程序，它被提交到集群以调度运行Worker，如下图所示。Driver中定义了一个或多个RDD，并调用RDD上的action，Worker则执行RDD分区计算任务。</p>
<p><img src="/images/spark-runtime.png" width="400" height="230" alt="spark-runtime" align="center"></p>
<h3 id="应用举例"><a href="#应用举例" class="headerlink" title="应用举例"></a>应用举例</h3><p>下面介绍一个简单的spark应用程序实例WordCount，统计一个数据集中每个单词出现的次数，首先将从hdfs中加载数据得到原始RDD-0，其中每条记录为数据中的一行句子，经过一个flatMap操作，将一行句子切分为多个独立的词，得到RDD-1，再通过map操作将每个词映射为key-value形式，其中key为词本身，value为初始计数值1，得到RDD-2，将RDD-2中的所有记录归并，统计每个词的计数，得到RDD-3，最后将其保存到hdfs。</p>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> org.apache.spark._</span><br><span class="line"><span class="keyword">import</span> <span class="type">SparkContext</span>._</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">object</span> <span class="title">WordCount</span> </span>&#123;</span><br><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">main</span></span>(args: <span class="type">Array</span>[<span class="type">String</span>]) &#123;</span><br><span class="line">    <span class="keyword">if</span> (args.length &lt; <span class="number">2</span>) &#123;</span><br><span class="line">      <span class="type">System</span>.err.println(<span class="string">"Usage: WordCount &lt;inputfile&gt; &lt;outputfile&gt;"</span>);</span><br><span class="line">      <span class="type">System</span>.exit(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">val</span> conf = <span class="keyword">new</span> <span class="type">SparkConf</span>().setAppName(<span class="string">"WordCount"</span>)</span><br><span class="line">    <span class="keyword">val</span> sc = <span class="keyword">new</span> <span class="type">SparkContext</span>(conf)</span><br><span class="line">    <span class="keyword">val</span> result = sc.textFile(args(<span class="number">0</span>))</span><br><span class="line">                   .flatMap(line =&gt; line.split(<span class="string">" "</span>))</span><br><span class="line">                   .map(word =&gt; (word, <span class="number">1</span>))</span><br><span class="line">                   .reduceByKey(_ + _)</span><br><span class="line">    result.saveAsTextFile(args(<span class="number">1</span>))</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/images/spark-wordcount.png" width="100%" height="100%" alt="spark-wordcount" align="center"></p>
<h3 id="小结-1"><a href="#小结-1" class="headerlink" title="小结"></a>小结</h3><p>基于RDD实现的Spark相比于传统的Hadoop MapReduce有什么优势呢？总结起来应该至少有三点：1）RDD提供了丰富的操作算子，不再是只有map和reduce两个操作了，对于描述应用程序来说更加方便；2）通过RDDs之间的转换构建DAG，中间结果不用落地；3）RDD支持缓存，可以在内存中快速完成计算。</p>
<p><span style="color:red"><em>转载请注明出处，本文永久链接：<a href="http://sharkdtu.com/posts/spark-rdd.html">http://sharkdtu.com/posts/spark-rdd.html</a></em></span></p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[Maven 使用本地jar包]]></title>
      <url>http://sharkdtu.com/posts/maven-assembly-native-jar.html</url>
      <content type="html"><![CDATA[<p>通常maven管理的项目中的依赖都是在远程仓库中的，假如我需要在maven项目中添加一个本地的jar包依赖，该jar包在仓库中是不存在的，可能是项目组前人开发的一个库，但是没发布到maven仓库中。遇到这种情况我们可以通过在pom中指定本地的依赖<a id="more"></a>，如：<br><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.hello.world.tools<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>tools<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>0.0.1<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">scope</span>&gt;</span>system<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">systemPath</span>&gt;</span>$&#123;project.basedir&#125;/lib/tools-0.0.1.jar<span class="tag">&lt;/<span class="name">systemPath</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>通过以上方法仅仅可以让你的代码编译通过，如果要把这个本地jar包打进assembly jar包，那么需要在pom中添加如下配置，其中jar包文件tools-0.0.1.jar放在根目录下的lib目录下：<br><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">build</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">plugins</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">plugin</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.apache.maven.plugins<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>maven-assembly-plugin<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">version</span>&gt;</span>2.5.3<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">configuration</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">descriptorRefs</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;<span class="name">descriptorRef</span>&gt;</span>jar-with-dependencies<span class="tag">&lt;/<span class="name">descriptorRef</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">descriptorRefs</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;/<span class="name">configuration</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">executions</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">execution</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;<span class="name">phase</span>&gt;</span>package<span class="tag">&lt;/<span class="name">phase</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;<span class="name">goals</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">goal</span>&gt;</span>single<span class="tag">&lt;/<span class="name">goal</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;/<span class="name">goals</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">execution</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;/<span class="name">executions</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">plugin</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">plugins</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">resources</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">resource</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">directory</span>&gt;</span>lib/<span class="tag">&lt;/<span class="name">directory</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">includes</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">include</span>&gt;</span>tools-0.0.1.jar<span class="tag">&lt;/<span class="name">include</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;/<span class="name">includes</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">resource</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">resources</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">build</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<p><span style="color:red"><em>转载请注明出处，本文永久链接：<a href="http://sharkdtu.com/posts/maven-assembly-native-jar.html">http://sharkdtu.com/posts/maven-assembly-native-jar.html</a></em></span></p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[运用TensorFlow处理简单的NLP问题]]></title>
      <url>http://sharkdtu.com/posts/nn-nlp.html</url>
      <content type="html"><![CDATA[<p>当前“人工智能”是继“大数据”后又一个即将被毁的词，每家公司都宣称要发力人工智能，就跟4-5年前大数据一样，业界叫的都非常响亮，不禁想到之前一个老外说过的话：</p>
<blockquote>
<p><em>Big Data is like teenage sex: Everyone talks about it, nobody really knows how to do it, everyone thinks everyone else is doing it, so everyone claims.</em></p>
</blockquote>
<p>现在看来，上面的”Big Data”可以换成”AI”了，在大家还没搞明白大数据的时候，人工智能就开始引领下一个潮流了。本着跟风的态度，我也尝试去窥探个究竟。<a id="more"></a></p>
<hr>
<h2 id="引言"><a href="#引言" class="headerlink" title="引言"></a>引言</h2><p>当前无论是学术界还是工业界，深度学习都受到极大的追捧，尤其是在Google开源深度学习平台TensorFlow之后，更是给深度学习火上浇油。目前在开源社区Github上所有开源项目中，TensorFlow最为活跃，从推出到现在，经历了几个版本的演进，可以说能够灵活高效地解决大量实际问题。本文主要尝试阐述TensorFlow在自然语言处理(NLP)领域的简单应用，让大家伙儿更加感性地认识TensorFlow。</p>
<p>说到NLP，其实我对它并不是很熟悉，之前也未曾有过NLP的相关经验，本文是我最近学习TensorFlow的一些积累，就当抛砖引玉了。当前互联网每天都在产生大量的文本和音频数据，通过挖掘这些数据，我们可以做一些更加便捷的应用，例如机器翻译、语音识别、词性标注以及信息检索等，这些都属于NLP范畴。而在NLP领域中，语言模型是最基本的一个环节，本文主要围绕语言模型展开，首先介绍其基本原理，进而引出词向量(word2vec)、循环神经网络(RNN)、长短时记忆网络(LSTM)等深度学习相关模型，并详细介绍如何利用 TensorFlow 实现上述模型。</p>
<h2 id="语言模型"><a href="#语言模型" class="headerlink" title="语言模型"></a>语言模型</h2><p>语言模型是一种概率模型，它是基于一个语料库创建，得到每个句子出现的概率，通俗一点讲就是看一句话是不是正常人说出来的，数学上表示为:</p>
<p>$$ P(W) = P(w_1 w_2 … w_t) = P(w_1) P(w_2|w_1) P(w_3|w_1 w_2) \cdots P(w_t|w_1 w_2 \cdots  w_{t-1}) \tag{2-1}$$</p>
<p>上述公式的意义是：一个句子出现的概率等于给定前面的词情况下，紧接着后面的词出现的概率。它是通过条件概率公式展开得到。其中条件概率 $P({w_2|w_1}), P({w_3|w_1 w_2}), \cdots, P({w_t|w_1 w_2 \cdots w_{t-1}})$ 就是创建语言模型所需要的参数，每个条件概率的意义解释为:根据前面的词预测下一个词的概率。有了这些条件概率参数，给定一个句子，就可以通过以上公式得到一个句子出现的概率。例如有一句话“php是最好的语言”（我不确定这是不是自然语言），假设已经分词为“php”、“是”、“最好的”、“语言”，那么它出现的概率为P(“php”，“是”，“最好的”，“语言”)=P(“php”)P(“是”|“php”)P(“最好的”|“php”，“是”)P(“语言”|“php”，“是”，“最好的”)，如果这个概率较大，那么判断为正常的一句话。以上这些条件概率通过如下贝叶斯公式得到：</p>
<p>$$ P(w_t|w_1 w_2 \cdots w_{t-1}) = \frac {P(w_1 w_2 \cdots w_t)} {P(w_1 w_2 \cdots w_{t-1})} \tag{2-2}$$</p>
<p>根据大数定理上述公式又可以近似为：</p>
<p>$$ P(w_t|w_1 w_2 \cdots w_{t-1}) = \frac {count(w_1 w_2 \cdots w_t)} {count(w_1, w_2, \cdots w_{t-1})} \tag{2-3}$$</p>
<p>假如语料库里有 $N$ 个词，一个句子长度为 $T$ ，那么就有 $N^T$ 种可能，每一种可能都要计算 $T$ 个条件概率参数，最后要计算 $TN^T$ 个参数并保存，不仅计算量大，对于内存要求也是惊人。那么如何避免这个问题呢，之前穷举的方法行不通，那么换个思路，采用一种偷懒的处理方法，就是将上述公式中条件概率做个如下近似：</p>
<p>$$ P(w_t|w_1 w_2 \cdots w_{t-1}) \approx P(w_t|w_{t-n+1} \cdots w_{t-1}) \tag{2-4}$$</p>
<p>这意思就是说一个词出现的概率只与它前面 $n-1$ 个词有关，而不是与它前面所有的词有关，这样极大的减少了统计的可能性，提高了计算效率，这种处理方法称之为 n-gram 模型，通常 $n$ 取2~3就能得到不错的效果。总结起来，n-gram 模型就是统计语料库中词串出现的次数，一次性计算得到词串的概率并将其保存起来，在预测一个句子时，直接通过前面所述的条件概率公式得到句子出现的概率。</p>
<p>近年也流行起神经网络语言模型，从机器学习的角度来看，一开始不全部计算这些词串的概率值，而是通过一个模型对词串的概率进行建模，然后构造一个目标函数，不断优化这个目标，得到一组优化的参数，当需要哪个词串概率时，利用这组优化的参数直接计算得到对应的词串概率。将词串概率 $P(w|{context(w)})$ 看做是 $w$ 和 $context(w)$ 的函数，其中 $context(w)$ 表示此 $w$ 的上下文，即相当于前面所述的 n-gram 模型的前 $n-1$ 个词，那么就有如下数学表示。</p>
<p>$$ P(w|{context(w)}) = F(w, {context(w)}, \Theta) \tag{2-5}$$</p>
<p>目标函数采用对数似然函数，表示如下(其中 $N$ 代表语料库中词典的大小)：</p>
<p>$$ Obj = \frac{1}{N} \sum \limits_{i = 1}^N log P({w_i}|{context_i}) \tag{2-6}$$</p>
<p>通过优化算法不断最小化目标函数得到一组优化的参数 $\Theta$ ，在神经网络中参数 $\Theta$ 则为网络层与层间的权值与偏置。那么在用神经网络学习语言模型[1]时，如何表示一个词呢？通常，在机器学习领域，是将一个样本对象抽象为一个向量，所以类似地，神经网络语言模型中是将词(或短语)表示为向量，通常叫做word2vec。那么神经网络语言模型就可以表示如下示意图。</p>
<p><img src="/images/nlp-nn.jpg" width="400" height="230" alt="nlp-nn" align="center"></p>
<p>上述神经网络包括输入层、投影层、隐藏层以及输出层，其中投影层只是对输入层做了一个预处理，将输入的所有词进行一个连接操作，假如一个词表示为 $m$ 维向量，那么由 $n-1$ 个词连接后则为 $(n-1)m$ 维向量，将连接后的向量作为神经网络的输入，经过隐藏层再到输出层，其中 $W$ 、$U$ 分别为投影层到隐藏层、隐藏层到输出层的权值参数，$p$ 、$q$ 分别为投影层到隐藏层、隐藏层到输出层的偏置参数，整个过程数学表达如下：</p>
<p>$$<br>\begin{split}<br>Z &amp;= \sigma(WX + p)\\<br>Y &amp;= UZ + q<br>\end{split} \tag{2-7}<br>$$</p>
<p>其中 $\sigma$ 为sigmoid函数，作为隐藏层的激活函数，输出层的输出向量为 $N$ 维，对应于语料库中词典的大小。一般需要再经过softmax归一化为概率形式，得到预测语料库中每个词的概率。以上神经网络语言模型看似很简单，但是词向量怎么来呢，如何将一个词转化为向量的形式呢？下面作详细阐述。</p>
<h2 id="词向量-word2vec"><a href="#词向量-word2vec" class="headerlink" title="词向量(word2vec)"></a>词向量(word2vec)</h2><p>词向量要做的事就是将语言数学化表示，以往的做法是采用 One-hot Representation 表示一个词，即语料库词典中有 $N$ 个词，那么向量的维度则为 $N$ ，给每个词编号，对于第 $i$ 个词，其向量表示除了第 $i$ 个单元为1，其他单元都为0的 $N$ 维向量，这种词向量的缺点显而易见，一般来说语料库的词典规模都特别大，那么词向量的维数就非常大，并且词与词之间没有关联性，并不能真实地刻画语言本身的性质，例如“腾讯”、“小马哥”这两个词通过One-hot向量表示，没有任何关联。为了克服One-hot Representation 的缺点，Mikolov大神提出了一种 Distributed Representation[2]，说个题外话，在大家都在如火如荼的用CNN做图像识别的时候，这哥们却在研究如何用神经网络处理NLP问题，最后发了大量关于神经网络NLP的高水平论文，成为这一领域的灵魂人物之一。顾名思义，Distributed Representation 就是把词的信息分布到向量不同的分量上，而不是像 One-hot Representation 那样所有信息集中在一个分量上，它的做法是将词映射到 $m$ 维空间，表示为 $m$ 维向量，也称之为 Word Embedding，这样一方面可以减小词向量的维度，另一方面，可以将有关联的词映射为空间中相邻的点，词与词之间的关联性通过空间距离来刻画，如下图所示。</p>
<p><img src="/images/nlp-word2vec-example.jpg" width="400" height="230" alt="nlp-word2vec-example" align="center"></p>
<p>词被映射到3维空间，每个词表示为一个3维向量，相近的词离的较近，可以看到两组差不多关系的词，他们之间的词向量距离也差不多。</p>
<p>要想得到词向量，需要借助语言模型训练得到，本质上来说，词向量是在训练语言模型过程中得到的副产品。解决word2vec问题有两种模型，即 CBOW 和 Skip-Gram 模型[3]，如下图所示：</p>
<p><img src="/images/nlp-word2vec-model.jpg" width="400" height="230" alt="nlp-word2vec-model" align="center"></p>
<p>CBOW 模型是根据词的上下文预测当前词，这里的上下文是由待预测词的前后 $c$ 个词组成。而 Skip-Gram 模型则相反，是通过当前词去预测上下文。给定一个语料库作为训练集，就可以通过以上模型训练出每个词的向量表示。从实验结果来看，CBOW 模型会平滑掉一些分布信息，因为它将词的上下文作为单个样本，而 Skip-Gram 模型将词上下文拆分为多个样本，训练得到的结果更为精确，为此，TensorFlow 中 word2vec 采用的是 Skip-Gram 模型，对应于文[2]中所提出的一种更为优化的 Skip-Gram 模型，下面着重介绍其原理，更多关于 CBOW 和 Skip-Gram 模型细节可以参阅文[3]。</p>
<h3 id="Skip-Gram-模型"><a href="#Skip-Gram-模型" class="headerlink" title="Skip-Gram 模型"></a>Skip-Gram 模型</h3><p>前面也提到， Skip-Gram 模型是根据当前词去预测上下文，例如有如下语句：</p>
<p><em>“php 是 世界上 最好的 语言”</em></p>
<p>假定上下文是由待预测词的前后2个词组成，那么由以上句子可以得到如下正样本：</p>
<p><em>(世界上, 是), (世界上, php), (世界上, 最好的), (世界上, 语言), (最好的, 世界上), …</em></p>
<p>训练目标为最大化以下对数似然函数：</p>
<p>$$ Obj = \frac {1} {N} \sum_{i=1}^{N} \sum_{-c \leqslant j \leqslant c , j \neq 0} log \ p(w_{i+j}|w_i) \tag{3-1}$$</p>
<p>其中 $c$ 为上下文的距离限定，即仅取词 $w_t$ 的前后 $c$ 个词进行预测。$c$ 越大，训练结果更精确，但是计算复杂度加大，训练成本相应也更大，一般取 $c$ 为2～3就能训练出不错的结果。基本的 Skip-Gram 模型采用softmax方法将以上目标函数中概率 $p(w_{i+j}|w_i)$ 定义为：</p>
<p>$$ p(w_O|w_I) = \frac {exp({\theta_{w_O}}^T v_{w_I})} {\sum_{w\in W} exp({\theta_{w}}^T v_{w_I})} \tag{3-2}$$</p>
<p>其中 $v_w$ 表示输入词 $w$ 的向量，$\theta_{w}$ 表示预测结果为 $w$ 的权值参数，二者都是待训练的参数。不难发现，通过以上公式，计算每个词的损失函数都要用到词典中的所有词，而一般词典的量级都非常大，所以这种方式是不切实际的。对于一个样本，例如(“世界上”, “php”)，无非是根据词“世界上”去预测词“php”，那么就可以看成一个二分类问题，对于输入词“世界上”，预测“php”为正，预测其他则为负，其他词可能是除“php”以外的所有词，为了简化计算，可以通过采样的方式，每次随机从所有除“php”以外的词中取 $k$ 个词作为负样本对象，那么训练目标则可以转化为类似于逻辑回归目标函数：</p>
<p>$$ Obj = log \sigma ({\theta_{w_O}}^{T} v_{w_I}) + \sum_{j=1}^{k} E_{w_j\sim {P_n}(w)} [log \sigma (-{\theta_{w_j}}^{T} v_{w_I})] \tag{3-3}$$</p>
<p>以上表达式称之为 NCE(Noise-contrastive estimation)[4]目标函数，其中等号右边第二项表示通过一个服从 $P_{n}(w)$ 分布的采样算法取得 $k$ 个负样本的期望损失。文[2]中采用了一个简单的一元分布采样，简化了计算，称之为负采样(Negative Sampling)，下面详细介绍负采样算法。</p>
<h3 id="负采样算法"><a href="#负采样算法" class="headerlink" title="负采样算法"></a>负采样算法</h3><p>词典中的每个词在语料库中出现的频次有高有低，理论上来说，对于那些高频词，被选为负样本的概率较大，对于那些低频词，被选为负样本的概率较小。基于这个基本事实，可以通过带权采样方法来实现，假设每个词的词频表示为单位线段上的一小分段，对于词典大小为 $N$ 的语料库，可以将词典中所有的词表示为单位线段上的一点，再在单位线段上等距离划分 $M$ 个等分， $M&gt;&gt;N$ ， 具体采样过程就是随机得到一个数 $i&lt;M$，通过映射找到其对应的词，如下如所示。</p>
<p><img src="/images/nlp-word2vec-negative-sampling.jpg" width="400" height="230" alt="nlp-word2vec-negative-sampling" align="center"></p>
<p>文[2]中在实际负采样计算词频时，做了一点修正，不是简单的统计词的出现次数，而是对词的出现次数做了 $\alpha$ 次幂处理，最后词频公式为：</p>
<p>$$ freq(w) = \frac {[counter(w)]^{3/4}} {\sum_{u \in W} [counter(u)]^{3/4}} \tag{3-4}$$</p>
<h3 id="高频词二次采样"><a href="#高频词二次采样" class="headerlink" title="高频词二次采样"></a>高频词二次采样</h3><p>在一个大语料库中，很多常见的词大量出现，如“的”、“是”等。这些词虽然词频较高，但是能提供的有用信息却很少。一般来说，这些高频词的词向量在训练几百万样本后基本不会有太大的变化，为了提高训练速度，平衡低频词和高频词，文[2]中提出一种针对高频词二次采样的技巧，对于每个词，按如下概率丢弃而不做训练。</p>
<p>$$ P(w_i) = 1 - \sqrt{\frac{t}{f(w_i)}} \tag{3-5}$$</p>
<p>其中$f(w_i)$表示词频，从上述公式中不难发现，二次采样仅针对那些满足 $f(w_i) &gt; t$ 所谓的高频词有效，参数 $t$ 根据语料库的大小而设置，一般设置为 $10^{-5}$ 左右。</p>
<h3 id="TensorFlow实现"><a href="#TensorFlow实现" class="headerlink" title="TensorFlow实现"></a>TensorFlow实现</h3><p>根据以上实现原理，下面结合代码阐述利用TensorFlow实现一个简易的word2vec模型[5]，借助TensorFlow丰富的api以及强大的计算引擎，我们可以非常方便地表达模型。给定语料库作为训练数据，首先扫描语料库建立字典，为每个词编号，同时将那些词频低于min_count的词过滤掉，即不对那些陌生词生成词向量。对于一个样本(“世界上”, “php”)，利用负采样得到若干负实例，分别计算输入词为“世界上”到“php”以及若干负样本的logit值，最后通过交叉熵公式得到目标函数(3-3)。</p>
<p><img src="/images/nlp-word2vec-forward.jpg" width="400" height="230" alt="nlp-word2vec-forward" align="center"></p>
<h4 id="构建计算流图"><a href="#构建计算流图" class="headerlink" title="构建计算流图"></a>构建计算流图</h4><p>首先定义词向量矩阵，也称为 embedding matrix，这个是我们需要通过训练得到的词向量，其中<code>vocabulary_size</code>表示词典大小，<code>embedding_size</code>表示词向量的维度，那么词向量矩阵为 vocabulary_size $\times$ embedding_size，利用均匀分布对它进行随机初始化：<br><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">embeddings = tf.Variable(</span><br><span class="line">    tf.random_uniform([vocabulary_size, embedding_size], <span class="number">-1.0</span>, <span class="number">1.0</span>))</span><br></pre></td></tr></table></figure></p>
<p>定义权值矩阵和偏置向量（对应于3-3式中的 $\theta$），并初始化为0：<br><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">weights = tf.Variable(</span><br><span class="line">  tf.truncated_normal([vocabulary_size, embedding_size],</span><br><span class="line">                      stddev=<span class="number">1.0</span> / math.sqrt(embedding_size)))</span><br><span class="line">biases = tf.Variable(tf.zeros([vocabulary_size]))</span><br></pre></td></tr></table></figure></p>
<p>给定一个batch的输入，从词向量矩阵中找到对应的向量表示，以及从权值矩阵和偏置向量中找到对应正确输出的参数，其中<code>examples</code>是输入词，<code>labels</code>为对应的正确输出，一维向量表示，每个元素为词在字典中编号：<br><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Embeddings for examples: [batch_size, embedding_size]</span></span><br><span class="line">example_emb = tf.nn.embedding_lookup(embeddings, examples)</span><br><span class="line"><span class="comment"># Weights for labels: [batch_size, embedding_size]</span></span><br><span class="line">true_w = tf.nn.embedding_lookup(weights, labels)</span><br><span class="line"><span class="comment"># Biases for labels: [batch_size, 1]</span></span><br><span class="line">true_b = tf.nn.embedding_lookup(biases, labels)</span><br></pre></td></tr></table></figure></p>
<p>负采样得到若干非正确的输出，其中<code>labels_matrix</code>为正确的输出词，采样的时候会跳过这些词，<code>num_sampled</code>为采样个数，<code>distortion</code>即为公式(3-4)中的幂指数：<br><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">labels_matrix = tf.reshape(</span><br><span class="line">    tf.cast(labels,</span><br><span class="line">            dtype=tf.int64),</span><br><span class="line">    [batch_size, <span class="number">1</span>])</span><br><span class="line"><span class="comment"># Negative sampling.</span></span><br><span class="line">sampled_ids, _, _ = tf.nn.fixed_unigram_candidate_sampler(</span><br><span class="line">     true_classes=labels_matrix,</span><br><span class="line">     num_true=<span class="number">1</span>,</span><br><span class="line">     num_sampled=num_samples,</span><br><span class="line">     unique=<span class="keyword">True</span>,</span><br><span class="line">     range_max=vocab_size,</span><br><span class="line">     distortion=<span class="number">0.75</span>,</span><br><span class="line">     unigrams=vocab_counts.tolist())</span><br></pre></td></tr></table></figure></p>
<p>找到采样样本对应的权值和偏置参数：<br><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Weights for sampled ids: [num_sampled, embedding_size]</span></span><br><span class="line">sampled_w = tf.nn.embedding_lookup(weights, sampled_ids)</span><br><span class="line"><span class="comment"># Biases for sampled ids: [num_sampled, 1]</span></span><br><span class="line">sampled_b = tf.nn.embedding_lookup(biases, sampled_ids)</span><br></pre></td></tr></table></figure></p>
<p>分别计算正确输出和非正确输出的logit值，即计算 $WX + b$，并通过交叉熵得到目标函数(3-3)：<br><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># True logits: [batch_size, 1]</span></span><br><span class="line">true_logits = tf.reduce_sum(tf.mul(example_emb, true_w), <span class="number">1</span>) + true_b</span><br><span class="line"><span class="comment"># Sampled logits: [batch_size, num_sampled]</span></span><br><span class="line"><span class="comment"># We replicate sampled noise lables for all examples in the batch</span></span><br><span class="line"><span class="comment"># using the matmul.</span></span><br><span class="line">sampled_b_vec = tf.reshape(sampled_b, [num_samples])</span><br><span class="line">sampled_logits = tf.matmul(example_emb,</span><br><span class="line">                           sampled_w,</span><br><span class="line">                           transpose_b=<span class="keyword">True</span>) + sampled_b_vec</span><br><span class="line"><span class="comment"># cross-entropy(logits, labels)</span></span><br><span class="line">true_xent = tf.nn.sigmoid_cross_entropy_with_logits(</span><br><span class="line">   true_logits, tf.ones_like(true_logits))</span><br><span class="line">sampled_xent = tf.nn.sigmoid_cross_entropy_with_logits(</span><br><span class="line">   sampled_logits, tf.zeros_like(sampled_logits))</span><br><span class="line"><span class="comment"># NCE-loss is the sum of the true and noise (sampled words)</span></span><br><span class="line"><span class="comment"># contributions, averaged over the batch.</span></span><br><span class="line">loss = (tf.reduce_sum(true_xent) +</span><br><span class="line">        tf.reduce_sum(sampled_xent)) / batch_size</span><br></pre></td></tr></table></figure></p>
<h4 id="训练模型"><a href="#训练模型" class="headerlink" title="训练模型"></a>训练模型</h4><p>计算流图构建完毕后，我们需要去优化目标函数。采用梯度下降逐步更新参数，首先需要确定学习步长，随着迭代进行，逐步减少学习步长，其中<code>trained_words</code>为已训练的词数量，<code>words_to_train</code>为所有待训练的词数量：<br><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">lr = init_learning_rate * tf.maximum(</span><br><span class="line">     <span class="number">0.0001</span>, <span class="number">1.0</span> - tf.cast(trained_words, tf.float32) / words_to_train)</span><br></pre></td></tr></table></figure></p>
<p>定义优化算子，使用梯度下降训练模型：<br><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">optimizer = tf.train.GradientDescentOptimizer(lr)</span><br><span class="line">train = optimizer.minimize(loss,</span><br><span class="line">                           global_step=global_step,</span><br><span class="line">                           gate_gradients=optimizer.GATE_NONE)</span><br><span class="line">session.run(train)</span><br></pre></td></tr></table></figure></p>
<h4 id="验证词向量"><a href="#验证词向量" class="headerlink" title="验证词向量"></a>验证词向量</h4><p>经过以上步骤后，即可得到词向量矩阵，即上述代码中的变量<code>embeddings</code>，那么如何验证得到的词向量矩阵的好坏呢，Mikolov等人发现[2]，如果一对关系差不多的词，其词向量在空间中的连线近乎平行，如下图所示。</p>
<p><img src="/images/nlp-word2vec-analogical.jpg" width="400" height="230" alt="nlp-word2vec-analogical" align="center"></p>
<p>为此，给定基准测试集，其每行包含4个词组成一个四元组 $(w_1, w_2, w_3, w_4)$ ，对于一个较好的词向量结果，每个四元组大致会有如下关系：</p>
<p>$$ Vector(w_1) - Vector(w_2) + Vector(w_4) = Vector(w_3) $$</p>
<h2 id="循环神经网络-RNN"><a href="#循环神经网络-RNN" class="headerlink" title="循环神经网络(RNN)"></a>循环神经网络(RNN)</h2><p>人类不是从脑子一片空白开始思考，当你读一篇文章的时候，你会根据前文去理解下文，而不是每次看到一个词后就忘掉它，理解下一个词的时候又从头开始。传统的神经网络模型是从输入层到隐藏层再到输出层，每层之间的节点是无连接的，这种普通的神经网络不具备记忆功能，而循环神经网络(Recurrent Neural Network，RNN)就是来解决这类问题，它具备记忆性，通常用于处理时间序列问题，在众多NLP问题中，RNN取得了巨大成功以及广泛应用。</p>
<p>在RNN网络中，一个序列当前的输出除了与当前输入有关以外，还与前面的输出也有关，下图为RNN中一个单元的结构示意图，图片来源于文[7]。</p>
<p><img src="/images/nlp-rnn-cell.jpg" width="400" height="230" alt="nlp-rnn-cell" align="center"></p>
<p>上图理解起来可能还不是很形象，根据时间序列将上图平铺展开得到如下图，其链式的特征揭示了 RNN 本质上是与序列相关的，所以 RNN 对于这类数据来说是最自然的神经网络架构。</p>
<p><img src="/images/nlp-rnn-unrolled.jpg" width="400" height="230" alt="nlp-rnn-unrolled" align="center"></p>
<p>然而 RNN 有一个缺点，虽然它可以将之前的信息连接到当前的输入上，但是如果当前输入与之前的信息时间跨度很大，由于梯度衰减等原因，RNN 学习如此远的信息的能力会下降，这个问题称之为长时间依赖（Long-Term Dependencies）问题。例如预测一句话“飞机在天上”下一个词，可能不需要太多的上下文就可以预测到下一个词为“飞”，这种情况下，相关信息与要预测的词之间的时间跨度很小，RNN 可以很容易学到之前的信息。再比如预测“他来自法国，…，他会讲”的下一个词，从当前的信息来看，下一个词可能是一种语言，但是要想准确预测哪种语言，就需要再去前文找信息了，由于前文的“法国”离当前位置的时间跨度较大，RNN很难学到如此远的信息。更多长时间依赖细节参考文[8]。幸运的是，有一种 RNN 变种，叫做长短时记忆网络(Long Short Term Memory networks, LSTM)，可以解决这个问题。</p>
<h2 id="长短时记忆网络-LSTM"><a href="#长短时记忆网络-LSTM" class="headerlink" title="长短时记忆网络(LSTM)"></a>长短时记忆网络(LSTM)</h2><p>LSTM 是一种带有选择性记忆功能的 RNN，它可以有效的解决长时间依赖问题，并能学习到之前的关键信息。如下图所示为 LSTM 展开后的示意图。</p>
<p><img src="/images/nlp-lstm-unrolled.jpg" width="400" height="230" alt="nlp-lstm-unrolled" align="center"></p>
<p>相对于 RNN , LSTM 只是在每个单元结构上做了改进，在 RNN 中，每个单元结构只有单个激活函数，而 LSTM 中每个单元结构更为复杂，它增加了一条状态线（图中最上面的水平线），以记住从之前的输入学到的信息，另外增加三个门(gate)来控制其该状态，分别为忘记门、输入门和输出门。忘记门的作用是选择性地将之前不重要的信息丢掉，以便存储新信息；输入门是根据当前输入学习到新信息然后更新当前状态；输出门则是结合当前输入和当前状态得到一个输出，该输出除了作为基本的输出外，还会作为下一个时刻的输入。下面用数学的方式表达每个门的意思。</p>
<p>忘记门，要丢掉的信息如下：</p>
<p>$$ f_t = \sigma (W_f[h_{t-1}, x_t] + b_f) \tag{5-1}$$</p>
<p>输入门，要增加的信息如下：<br>$$<br>\begin{split}<br>i_t &amp;= \sigma (W_i[h_{t-1}, x_t] + b_i)\\<br>\tilde{C_t} &amp;= tanh(W_C[h_{t-1}, x_t] + b_C)<br>\end{split} \tag{5-2}<br>$$</p>
<p>那么根据忘记门和输入门，状态更新如下：</p>
<p>$$ C_t = f_t * C_{t-1} + i_t * \tilde{C_t} \tag{5-3}$$</p>
<p>输出门，得到输出信息如下：</p>
<p>$$<br>\begin{split}<br>o_t &amp;= \sigma (W_o[h_{t-1}, x_t] + b_o)\\<br>h_t &amp;= o_t * tanh(C_t)<br>\end{split} \tag{5-4}<br>$$</p>
<p>LSTM 单元输入都是上一个时刻的输出与当前时刻的输入通过向量concat连接而得到，基于这个输入，利用sigmoid函数作为三个门的筛选器，分别得到 $f_t$ 、$i_t$ 、$o_t$，这三个筛选器分别选择部分分量对状态进行选择性忘记、对输入进行选择性输入、对输出进行选择性输出。以上是 LSTM 基本结构原理，在这基础上，根据不同的实际应用场景，演变出很多 LSTM 的变体，更多关于 LSTM 的详细解释请参考文[7]。下面介绍一种深层 LSTM 网络[9]，该结构也是 TensorFlow 中 LSTM 所实现的根据[10]。</p>
<h3 id="深层LSTM网络"><a href="#深层LSTM网络" class="headerlink" title="深层LSTM网络"></a>深层LSTM网络</h3><p>深度学习，其特点在于深，前面已经讲述单层 LSTM 网络结构，深层 LSTM 网络其实就是将多层 LSTM 叠加，形成多个隐藏层，如下图所示。</p>
<p><img src="/images/nlp-lstm-multilayer.jpg" width="400" height="230" alt="nlp-lstm-multilayer" align="center"></p>
<p>上图中每个 LSTM 单元内部结构如下图所示，对于 $l$ 层 $t$ 时刻来说，$h_{t-1}^l$ 为 $l$ 层 $t-1$ 时刻（即上一个时刻）的输出，$h_t^{l-1}$ 为 $l-1$ 层（即上一层） $t$ 时刻的输出，这两个输出叠加作为 $l$ 层 $t$ 时刻的输入。</p>
<p><img src="/images/nlp-lstm-multilayer-cell.jpg" width="400" height="230" alt="nlp-lstm-multilayer-cell" align="center"></p>
<p>根据上面的结构，可以得到 $l$ 层 LSTM 数学表达, $h_t^{l-1}, h_{t-1}^l, c_{t-1}^l \rightarrow h_t^l, c_t^l$：</p>
<p>$$<br>\begin{split}<br>f &amp;= \sigma (W_f[h_t^{l-1}, h_{t-1}^l] + b_f) \\<br>i &amp;= \sigma (W_i[h_t^{l-1}, h_{t-1}^l] + b_i) \\<br>o &amp;= \sigma (W_o[h_t^{l-1}, h_{t-1}^l] + b_o) \\<br>g &amp;= tanh(W_g[h_t^{l-1}, h_{t-1}^l] + b_g) \\<br>\ \notag \\<br>c_t^l &amp;= f * c_{t-1}^l + i * g \\<br>h_t^l &amp;= o * tanh(c_t^l)<br>\end{split} \tag{5-5}<br>$$</p>
<p>其中 $c_{t-1}^l$ 表示上一时刻的状态，$c_t^l$ 表示由当前输入更新后的状态。</p>
<h3 id="正则化"><a href="#正则化" class="headerlink" title="正则化"></a>正则化</h3><p>然而，实践证明大规模的 LSTM 网络很容易过拟合，实际应用中，需要采取正则化方法来避免过拟合，神经网络中常见的正则化方法是Dropout方法[11]，文[12]提出一种简单高效的Dropout方法运用于 RNN/LTSM 网络。如下图所示，Dropout仅应用于虚线方向的输入，即仅针对于上一层的输出做Dropout。</p>
<p><img src="/images/nlp-lstm-multilayer-dropout.jpg" width="400" height="230" alt="nlp-lstm-multilayer-dropout" align="center"></p>
<p>根据上图的Dropout策略，公式(5-5)可以改写成如下形式：</p>
<p>$$<br>\begin{split}<br>f &amp;= \sigma (W_f[D(h_t^{l-1}), h_{t-1}^l] + b_f) \\<br>i &amp;= \sigma (W_i[D(h_t^{l-1}), h_{t-1}^l] + b_i) \\<br>o &amp;= \sigma (W_o[D(h_t^{l-1}), h_{t-1}^l] + b_o) \\<br>g &amp;= tanh(W_g[D(h_t^{l-1}), h_{t-1}^l] + b_g) \\<br>\ \notag \\<br>c_t^l &amp;= f * c_{t-1}^l + i * g \\<br>h_t^l &amp;= o * tanh(c_t^l)<br>\end{split} \tag{5-6}<br>$$</p>
<p>其中 $D$ 表示Dropout操作符，会随机地将 $h_t^{l-1}$ 的中的分量设置为零。如下图所示，黑色粗实线表示从 $t-2$ 时刻的信息流向 $t+2$ 时刻作为其预测的参考，它经历了 $L+1$ 次的Dropout，其中 $L$ 表示网络的层数。</p>
<p><img src="/images/nlp-lstm-multilayer-dropout-event-flow.jpg" width="400" height="230" alt="nlp-lstm-multilayer-dropout-event-flow" align="center"></p>
<h3 id="TensorFlow实现-1"><a href="#TensorFlow实现-1" class="headerlink" title="TensorFlow实现"></a>TensorFlow实现</h3><p>根据前面所述的 LSTM 模型原理，实现之前提到的语言模型，即根据前文预测下一个词，例如输入“飞机在天上”预测下一个词“飞”，使用 TensorFlow 来实现 LSTM 非常的方便，因为 TensorFlow 已经提供了基本的 LSTM 单元结构的Operation，其实现原理就是基于文[12]提出的带Dropout的 LSTM 模型。完整代码请参考<a href="https://github.com/tensorflow/tensorflow/blob/master/tensorflow/models/rnn/ptb/ptb_word_lm.py" target="_blank" rel="external">ptb_word_lm.py</a></p>
<h4 id="构建LSTM模型"><a href="#构建LSTM模型" class="headerlink" title="构建LSTM模型"></a>构建LSTM模型</h4><p>利用TensorFlow提供的Operation，实现 LSTM 网络很简单，首先定义一个基本的 LSTM 单元，其中<code>size</code>为 LSTM 单元的输出维度，再对其添加Dropout，根据 LSTM 的层数<code>num_layers</code>得到多层的 RNN 结构单元。<br><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">lstm_cell = tf.nn.rnn_cell.BasicLSTMCell(size, forget_bias=<span class="number">0.0</span>)</span><br><span class="line">lstm_cell = tf.nn.rnn_cell.DropoutWrapper(</span><br><span class="line">    lstm_cell, output_keep_prob=keep_prob)</span><br><span class="line">cell = tf.nn.rnn_cell.MultiRNNCell([lstm_cell] * num_layers)</span><br></pre></td></tr></table></figure></p>
<p>每次给定一个batch的输入，将 LSTM 网络的状态初始化为0。词的输入由词向量表示，所以先定义一个embedding矩阵，这里可以不要关心它一开始有没有，它会在训练过程中的慢慢得到的，仅作为训练的副产品。假设LSTM网络展开<code>num_steps</code>步，每一步给定一个batch的词作为输入，经过 LSTM 单元处理后，状态更新并得到输出，并通过softmax归一化后计算损失函数。</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line">initial_state = cell.zero_state(batch_size, tf.float32)</span><br><span class="line">embedding = tf.get_variable(<span class="string">"embedding"</span>, [vocab_size, size])</span><br><span class="line"><span class="comment"># input_data: [batch_size, num_steps]</span></span><br><span class="line"><span class="comment"># targets： [batch_size, num_steps]</span></span><br><span class="line">input_data = tf.placeholder(tf.int32, [batch_size, num_steps])</span><br><span class="line">targets = tf.placeholder(tf.int32, [batch_size, num_steps])</span><br><span class="line">inputs = tf.nn.embedding_lookup(embedding, input_data)</span><br><span class="line">outputs = []</span><br><span class="line"><span class="keyword">for</span> time_step <span class="keyword">in</span> range(num_steps):</span><br><span class="line">  (cell_output, state) = cell(inputs[:, time_step, :], state)</span><br><span class="line">  outputs.append(cell_output)</span><br><span class="line"></span><br><span class="line">output = tf.reshape(tf.concat(<span class="number">1</span>, outputs), [<span class="number">-1</span>, size])</span><br><span class="line">softmax_w = tf.get_variable(<span class="string">"softmax_w"</span>, [size, vocab_size])</span><br><span class="line">softmax_b = tf.get_variable(<span class="string">"softmax_b"</span>, [vocab_size])</span><br><span class="line">logits = tf.matmul(output, softmax_w) + softmax_b</span><br><span class="line"></span><br><span class="line">loss = tf.nn.seq2seq.sequence_loss_by_example(</span><br><span class="line">    [logits],</span><br><span class="line">    [tf.reshape(targets, [<span class="number">-1</span>])],</span><br><span class="line">    [tf.ones([batch_size * num_steps])])</span><br></pre></td></tr></table></figure>
<h4 id="训练模型-1"><a href="#训练模型-1" class="headerlink" title="训练模型"></a>训练模型</h4><p>简单采用梯度下降优化上述损失函数，逐步迭代，直至最大迭代次数，得到<code>final_state</code>，即为LSTM所要学习的参数。<br><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">optimizer = tf.train.GradientDescentOptimizer(lr)</span><br><span class="line">train_op = optimizer.minimize(loss)</span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(max_epoch):</span><br><span class="line">  _, final_state = session.run([train_op, state],</span><br><span class="line">                               &#123;input_data: x,</span><br><span class="line">                                targets: y&#125;)</span><br></pre></td></tr></table></figure></p>
<h4 id="验证测试模型"><a href="#验证测试模型" class="headerlink" title="验证测试模型"></a>验证测试模型</h4><p>模型训练完毕后，我们已经得到LSTM网络的状态，给定输入，经过LSTM网络后即可得到输出了。<br><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">(cell_output, _) = cell(inputs, state)</span><br><span class="line">session.run(cell_output)</span><br></pre></td></tr></table></figure></p>
<h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>在使用TensorFlow处理深度学习相关问题时，我们不需要太关注其内部实现细节，只需把精力放到模型的构建上，利用TensorFlow已经提供的抽象单元结构就可以构建灵活的模型。也恰恰正是因为TensorFlow的高度抽象化，有时让人理解起来颇费劲。所以在我们使用TensorFlow的过程中，不要把问题细化的太深，一切数据看成Tensor即可，利用Tensor的操作符对其进行运算，不要在脑海里想如何如何的运算细节等等，不然就会身陷囹圄。</p>
<h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2><p>[1]. Bengio Y, Schwenk H, Senécal J S, et al. Neural probabilistic language models[M]//Innovations in Machine Learning. Springer Berlin Heidelberg, 2006: 137-186.MLA.<br>[2]. Mikolov T, Sutskever I, Chen K, et al. Distributed representations of words and phrases and their compositionality[C]//Advances in neural information processing systems. 2013: 3111-3119.<br>[3]. Mikolov T, Le Q V, Sutskever I. Exploiting similarities among languages for machine translation[J]. arXiv preprint arXiv:1309.4168, 2013.<br>[4]. Gutmann M U, Hyvärinen A. Noise-contrastive estimation of unnormalized statistical models, with applications to natural image statistics[J]. The Journal of Machine Learning Research, 2012, 13(1): 307-361.<br>[5]. Vector Representations of Words. <a href="https://www.tensorflow.org/versions/r0.8/tutorials/word2vec/index.html#vector-representations-of-words" target="_blank" rel="external">https://www.tensorflow.org/versions/r0.8/tutorials/word2vec/index.html#vector-representations-of-words</a><br>[6]. word2vec 中的数学原理详解. <a href="http://www.cnblogs.com/peghoty/p/3857839.html" target="_blank" rel="external">http://www.cnblogs.com/peghoty/p/3857839.html</a><br>[7]. Understanding LSTM Networks. <a href="http://colah.github.io/posts/2015-08-Understanding-LSTMs/" target="_blank" rel="external">http://colah.github.io/posts/2015-08-Understanding-LSTMs/</a><br>[8]. Bengio Y, Simard P, Frasconi P. Learning long-term dependencies with gradient descent is difficult[J]. Neural Networks, IEEE Transactions on, 1994, 5(2): 157-166.<br>[9]. Graves A. Generating sequences with recurrent neural networks[J]. arXiv preprint arXiv:1308.0850, 2013.<br>[10]. Recurrent Neural Networks. <a href="https://www.tensorflow.org/versions/r0.8/tutorials/recurrent/index.html#recurrent-neural-networks" target="_blank" rel="external">https://www.tensorflow.org/versions/r0.8/tutorials/recurrent/index.html#recurrent-neural-networks</a><br>[11]. Srivastava N. Improving neural networks with dropout[D]. University of Toronto, 2013.<br>[12]. Zaremba W, Sutskever I, Vinyals O. Recurrent neural network regularization[J]. arXiv preprint arXiv:1409.2329, 2014.                                                                                    </p>
<p><span style="color:red"><em>转载请注明出处，本文永久链接：<a href="http://sharkdtu.com/posts/nn-nlp.html">http://sharkdtu.com/posts/nn-nlp.html</a></em></span></p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[临时更换hadoop-ugi]]></title>
      <url>http://sharkdtu.com/posts/tmp-hadoop-ugi-using.html</url>
      <content type="html"><![CDATA[<p>在用spark读写hdfs数据时，有时候当前用户对要读写的hdfs路径没有权限，需要临时改变用户去读写hdfs，操作完后回到原来的用户。我们的hdfs是没有权限认证的，一开始通过下面代码的方式来实现。<a id="more"></a></p>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> conf = <span class="keyword">new</span> <span class="type">JobConf</span>(rdd.context.hadoopConfiguration)</span><br><span class="line">conf.set(<span class="string">"hadoop.job.ugi"</span>, <span class="string">"newuser,newgroup"</span>)</span><br><span class="line">rdd.saveAsHadoopFile(path, keyClass, valueClass, outputFormatClass, conf)</span><br></pre></td></tr></table></figure>
<p>以上代码是在现有的 hadoop conf 上复制出一个 conf，然后修改ugi，利用这个新的 conf 去写数据，表面上看并没有什么错，没有改变原来的 conf，然而这种方式偶尔会不生效。经过排查发现，自从hdfs-2.0开始，ugi操作需要通过<code>UserGroupInformation</code>这个类来实现，在获取用户名时有个缓存，所以之前偶尔不生效的情况是因为它用了缓存里的用户名而还没来得及更新，为了规避这个问题，改成下面的实现就万无一失了，所有临时的操作在<code>doAs</code>方法里实现，出了这个方法就回到原来用户下。</p>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> conf = <span class="keyword">new</span> <span class="type">JobConf</span>(rdd.context.hadoopConfiguration)</span><br><span class="line">conf.set(<span class="string">"hadoop.job.ugi"</span>, <span class="string">"newuser,newgroup"</span>)</span><br><span class="line"><span class="keyword">val</span> ugi = <span class="type">UserGroupInformation</span>.getCurrentUser(conf)</span><br><span class="line">ugi.doAs(<span class="keyword">new</span> <span class="type">PrivilegeAction</span>[unit] &#123;</span><br><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">run</span></span>()&#123;</span><br><span class="line">    rdd.saveAsHadoopFile(path, keyClass, valueClass, outputFormatClass, conf)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>
<p><span style="color:red"><em>转载请注明出处，本文永久链接：<a href="http://sharkdtu.com/posts/tmp-hadoop-ugi-using.html">http://sharkdtu.com/posts/tmp-hadoop-ugi-using.html</a></em></span></p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[初识Scala反射]]></title>
      <url>http://sharkdtu.com/posts/scala-reflect.html</url>
      <content type="html"><![CDATA[<p>我们知道，scala编译器会将scala代码编译成JVM字节码，编译过程中会擦除scala特有的一些类型信息，在scala-2.10以前，只能在scala中利用java的反射机制，但是通过java反射机制得到的是只是擦除后的类型信息，并不包括scala的一些特定类型信息。从scala-2.10起，scala实现了自己的反射机制，我们可以通过scala的反射机制得到scala的类型信息。scala反射包括运行时反射和编译时反射，本文主要阐述运行时反射的一些用法，方便scala开发人员参考，具体原理细节请查看官方文档。<a id="more"></a>本文涉及到的代码示例是基于scala-2.10.4，如有不同请勿对号入座。</p>
<p>给定类型或者对象实例，通过scala运行时反射，可以做到：1）获取运行时类型信息；2）通过类型信息实例化新对象；3）访问或调用对象的方法和属性等。下面分别举例阐述运行时反射的功能。</p>
<h2 id="获取运行时类型信息"><a href="#获取运行时类型信息" class="headerlink" title="获取运行时类型信息"></a>获取运行时类型信息</h2><p>scala运行时类型信息是保存在<code>TypeTag</code>对象中，编译器在编译过程中将类型信息保存到<code>TypeTag</code>中，并将其携带到运行期。我们可以通过<code>typeTag</code>方法获取<code>TypeTag</code>类型信息。</p>
<pre><code>scala&gt; import scala.reflect.runtime.universe._
import scala.reflect.runtime.universe._

scala&gt; typeTag[List[Int]]
res0: reflect.runtime.universe.TypeTag[List[Int]] = TypeTag[scala.List[Int]]

scala&gt; res0.tpe
res1: reflect.runtime.universe.Type = scala.List[Int]
</code></pre><p>如上述scala REPL显示，通过<code>typeTag</code>方法获取<code>List[Int]</code>类型的<code>TypeTag</code>对象，该对象包含了<code>List[Int]</code>的详细类型信息，通过<code>TypeTag</code>对象的<code>tpe</code>方法得到由<code>Type</code>对象封装具体的类型信息，可以看到该<code>Type</code>对象的类型信息精确到了类型参数<code>Int</code>。如果仅仅是获取类型信息，还有一个更简便的方法，那就是通过<code>typeOf</code>方法。</p>
<pre><code>scala&gt; import scala.reflect.runtime.universe._
import scala.reflect.runtime.universe._

scala&gt; typeOf[List[Int]]
res0: reflect.runtime.universe.Type = scala.List[Int]
</code></pre><p>这时有人就会问，<code>typeTag</code>方法需要传一个具体的类型，事先知道类型还要<code>TypeTag</code>有啥用啊。我们不妨写个方法，获取任意对象的类型信息。</p>
<pre><code>scala&gt; val ru = scala.reflect.runtime.universe
ru: scala.reflect.api.JavaUniverse = scala.reflect.runtime.JavaUniverse@6dae22e7

scala&gt; def getTypeTag[T: ru.TypeTag](obj: T) = ru.typeTag[T]
getTypeTag: [T](obj: T)(implicit evidence$1: ru.TypeTag[T])ru.TypeTag[T]

scala&gt; val list = List(1, 2, 3)
list: List[Int] = List(1, 2, 3)

scala&gt; val theType = getTypeTag(list).tpe
theType: ru.Type = List[Int]
</code></pre><p>如上scala REPL显示，方法<code>getTypeTag</code>可以获取任意对象的类型信息，注意方法中的上下文界定<code>T: ru.TypeTag</code> ,它表示存在一个从<code>T</code>到<code>TypeTag[T]</code>的隐式转换，前面已经讲到，<code>TypeTag</code>对象是在编译期间由编译器生成的，如果不加这个上下文界定，编译器就不会为T生成<code>TypeTag</code>对象。当然也可以通过隐式参数替代上下文界定，就如同REPL中显示的<code>implicit evidence$1: ru.TypeTag[T]</code>那样。一旦我们获取到了类型信息(Type instance)，我们就可以通过该<code>Type</code>对象查询更详尽的类型信息。</p>
<pre><code>scala&gt; val decls = theType.declarations.take(10)
decls: Iterable[ru.Symbol] = List(constructor List, method companion, method isEmpty, method head, method tail, method ::, method :::, method reverse_:::, method mapConserve, method ++)
</code></pre><p>到这里我们知道<code>TypeTag</code>对象封装了<code>Type</code>对象，通过<code>Type</code>对象可以获取详尽的类型信息，包括方法和属性等，通过<code>typeTag</code>方法可以得到<code>TypeTag</code>对象，通过<code>typeOf</code>方法可以得到<code>Type</code>对象，它包含没有被编译器擦除的含完整的scala类型信息，与之对应地，如果想获取擦除后的类型信息，传统的方法可以通过java的反射机制来实现，但是scala也提供了该功能，通过<code>classTag</code>方法可以获取<code>ClassTag</code>对象，<code>ClassTag</code>封装了擦除后的类型信息，通过<code>classOf</code>方法可以获取<code>Class</code>对象，这与java反射中的<code>Class</code>对象一致。</p>
<pre><code>scala&gt; import scala.reflect._
import scala.reflect._

scala&gt; val clsTag = classTag[List[Int]]
clsTag: scala.reflect.ClassTag[List[Int]] = scala.collection.immutable.List

scala&gt; clsTag.runtimeClass
res0: Class[_] = class scala.collection.immutable.List

scala&gt; val cls = classOf[List[Int]]
cls: Class[List[Int]] = class scala.collection.immutable.List

scala&gt; cls.[tab键补全]
asInstanceOf              asSubclass          cast
desiredAssertionStatus    getAnnotation
getAnnotations            getCanonicalName    ...   
</code></pre><p>从上述scala REPL中可以看到，<code>ClassTag</code>对象包含了<code>Class</code>对象，通过<code>Class</code>对象仅仅可以获取擦除后的类型信息，通过在scala REPL中用tab补全可以看到通过<code>Class</code>对象可以获取的信息。</p>
<h2 id="运行时类型实例化"><a href="#运行时类型实例化" class="headerlink" title="运行时类型实例化"></a>运行时类型实例化</h2><p>我们已经知道通过<code>Type</code>对象可以获取未擦除的详尽的类型信息，下面我们通过<code>Type</code>对象中的信息找到构造方法并实例化类型的一个对象。</p>
<pre><code>scala&gt; case class Person(id: Int, name: String)
defined class Person

scala&gt; val ru = scala.reflect.runtime.universe
ru: scala.reflect.api.JavaUniverse = scala.reflect.runtime.JavaUniverse@3e9ed70d

scala&gt; val m = ru.runtimeMirror(getClass.getClassLoader)
m: ru.Mirror = JavaMirror with scala.tools.nsc.interpreter.IMain$TranslatingClassLoader@a57fc5f ...

scala&gt; val classPerson = ru.typeOf[Person].typeSymbol.asClass
classPerson: ru.ClassSymbol = class Person

scala&gt; val cm = m.reflectClass(classPerson)
cm: ru.ClassMirror = class mirror for Person (bound to null)

scala&gt; val ctor = ru.typeOf[Person].declaration(ru.nme.CONSTRUCTOR).asMethod
ctor: ru.MethodSymbol = constructor Person

scala&gt; val ctorm = cm.reflectConstructor(ctor)
ctorm: ru.MethodMirror = constructor mirror for Person.&lt;init&gt;(id: scala.Int, name: String): Person (bound to null)

scala&gt; val p = ctorm(1, &quot;Mike&quot;)
p: Any = Person(1,Mike)
</code></pre><p>如上scala REPL代码，要想通过<code>Type</code>对象获取相关信息，必须借助<code>Mirror</code>，<code>Mirror</code>是按层级划分的，有<code>ClassLoaderMirror</code>, <code>ClassMirror</code>, <code>InstanceMirror</code>, <code>ModuleMirror</code>, <code>MethodMirror</code>, <code>FieldMirror</code>。通过<code>ClassLoaderMirror</code>可以创建<code>ClassMirror</code>, <code>InstanceMirror</code>, <code>ModuleMirror</code>,  <code>MethodMirror</code>, <code>FieldMirror</code>。通过<code>ClassMirror</code>, <code>InstanceMirror</code>可以创建<code>MethodMirror</code>,  <code>FieldMirror</code>。<code>ModuleMirror</code>用于处理单例对象，通常是由<code>object</code>定义的。从上述代码中可以发现，首先获取一个<code>ClassLoaderMirror</code>，然后通过该<code>Mirror</code>创建一个<code>ClassMirror</code>，继续创建<code>MethodMirror</code>，通过该<code>MethodMirror</code>调用构造函数。从一个<code>Mirror</code>创建另一个<code>Mirror</code>，需要指定一个<code>Symbol</code>，<code>Symbol</code>其实就是绑定名字和一个实体，有<code>ClassSymbol</code>、 <code>MethodSymbol</code>、 <code>FieldSymbol</code>等，<code>Symbol</code>的获取是通过<code>Type</code>对象方法去查询，例如上述代码中通过<code>declaration</code>方法查询构造函数的<code>Symbol</code>。</p>
<h2 id="运行时类成员访问"><a href="#运行时类成员访问" class="headerlink" title="运行时类成员访问"></a>运行时类成员访问</h2><p>下面举例阐述访问运行时类成员，同理，我们只需逐步创建<code>FieldMirror</code>来访问类成员。</p>
<pre><code>scala&gt; case class Person(id: Int, name: String)
defined class Person

scala&gt; val ru = scala.reflect.runtime.universe
ru: scala.reflect.api.JavaUniverse = scala.reflect.runtime.JavaUniverse@3e9ed70d

scala&gt; val m = ru.runtimeMirror(getClass.getClassLoader)
m: ru.Mirror = JavaMirror with scala.tools.nsc.interpreter.IMain$TranslatingClassLoader@a57fc5f ...

scala&gt; val p = Person(1, &quot;Mike&quot;)
p: Person = Person(1,Mike)

scala&gt; val nameTermSymb = ru.typeOf[Person].declaration(ru.newTermName(&quot;name&quot;)).asTerm
nameTermSymb: ru.TermSymbol = value name

scala&gt; val im = m.reflect(p)
im: ru.InstanceMirror = instance mirror for Person(1,Mike)

scala&gt; val nameFieldMirror = im.reflectField(nameTermSymb)
nameFieldMirror: ru.FieldMirror = field mirror for Person.name (bound to Person(1,Mike))

scala&gt; nameFieldMirror.get
res0: Any = Mike

scala&gt; nameFieldMirror.set(&quot;Jim&quot;)

scala&gt; p.name
res2: String = Jim
</code></pre><p>如上代码所示，通过层级<code>ClassLoaderMirror</code>-&gt;<code>InstanceMirror</code>-&gt;<code>FieldMirror</code>得到<code>FieldMirror</code>，通过<code>Type</code>对象调用方法<code>declaration(ru.newTermName(&quot;name&quot;))</code>获取name字段的<code>Symbol</code>，通过<code>FieldMirror</code>的<code>get</code>和<code>set</code>方法去访问和修改成员变量。</p>
<p>说了这么多，貌似利用java的反射机制也可以实现上述功能，还不用这么的费劲。关键还是在于你要访问编译器擦除后的类型信息还是擦除前的类型信息，如果是访问擦除后的类型信息，使用java和scala的反射都可以，但是访问擦除前的类型信息，那就必须要使用scala的反射，因为java的反射并不知道擦除前的信息。</p>
<p>举个栗子，一步步剖析，首先定义一个基类<code>A</code>，它包含一个抽象类型成员<code>T</code>，然后分别派生出两个子类<code>B</code>和<code>C</code>。</p>
<pre><code>scala&gt; class A {
     | type T
     | val x: Option[T] = None
     | }
defined class A

scala&gt; class B extends A
defined class B

scala&gt; class C extends B
defined class C
</code></pre><p>现在分别实例化<code>B</code>和<code>C</code>的一个对象，并将抽象类型<code>T</code>具体化为<code>String</code>类型。</p>
<pre><code>scala&gt; val b = new B { type T = String }
b: B{type T = String} = $anon$1@446344a8

scala&gt; val c = new C { type T = String }
c: C{type T = String} = $anon$1@195bc0a4
</code></pre><p>现在通过java的反射机制判断对象<code>b</code>和<code>c</code>的运行时类型是否是父子关系。</p>
<pre><code>scala&gt; b.getClass.isAssignableFrom(c.getClass)
res3: Boolean = false
</code></pre><p>可以看到通过java的反射判断对象<code>c</code>的运行时类型并不是对象<code>b</code>的运行时类型的子类。然而从我们的定义来看，对象<code>c</code>的类型本应该是对象<code>b</code>的类型的子类，到这里我们就会想到编译器，在实例化对象<code>b</code>和<code>c</code>时实际上是通过匿名类来实例化的，一开始定义类型信息在编译的时候被擦除了，转为匿名类了。下面通过scala的反射机制判断对象<code>b</code>和<code>c</code>的运行时类型是否是父子关系。</p>
<pre><code>scala&gt; val ru = scala.reflect.runtime.universe
ru: scala.reflect.api.JavaUniverse = scala.reflect.runtime.JavaUniverse@3e9ed70d

scala&gt; def isSubClass[T: ru.TypeTag, S: ru.TypeTag](x: T, y: S): Boolean = {
     | val leftTag = ru.typeTag[T]
     | val rightTag = ru.typeTag[S]
     | leftTag.tpe &lt;:&lt; rightTag.tpe
     | }
isSubClass: [T, S](x: T, y: S)(implicit evidence$1: ru.TypeTag[T], implicit evidence$2: ru.TypeTag[S])Boolean

scala&gt; isSubClass(c, b)
res5: Boolean = true
</code></pre><p>从上述代码中可以看到，通过scala的反射得到的类型信息符合我们一开始定义。所以在scala中最好是使用scala的反射而不要使用java的反射，因为很有可能编译后通过java的反射得到的结果并不是想象的那样。</p>
<p><span style="color:red"><em>转载请注明出处，本文永久链接：<a href="http://sharkdtu.com/posts/scala-reflect.html">http://sharkdtu.com/posts/scala-reflect.html</a></em></span></p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[博客重生]]></title>
      <url>http://sharkdtu.com/posts/blog-reborn.html</url>
      <content type="html"><![CDATA[<p>之前用wordpress搭建博客，买vps以及域名，自己管理维护主机，写了一段时间博客后，发现主机经常受到一些诸如垃圾邮件的攻击，然后被vps厂商强制关机，要求其开机要经过一系列的邮件申诉，后面机器恢复后，因为内存不够用(也可以说是银子不够)的原因，mysql经常挂，总是要去重启，管理起来特别费心。中途由于一段时间的懒惰，没码字了，vps空间到期忘了续费，机器被停了，数据也被清理了，所有的文章都没来得及备份，想想都特别惋惜，所幸通过外链引用找回部分文章，在这篇文章之前的所有文章都是以前wordpress站点搬过来的。<a id="more"></a>最近想起重新把博客搭起来，在考虑为博客选型，虽然wordpress特别好用，功能丰富，操作简单，但是花钱买vps，同时还要自己管理维护并保障安全，对于只想写写博客的我来说，真的已经力不从心了。经过调研，发现可以借助github pages的功能就可以搭建博客，而且流量和主机都免费，居然有这么好的事，这不就免去了自己管理机器的烦恼吗，有github强大的后台支撑，再也不担心被攻击内存不够等的烦心问题了。</p>
<p>通过一番调研，在网上看了很多关于在github上搭建博客的文章，github官网推荐用jekyll来生成静态网页，自己也尝试了一番，由于对html、js、css等前端知识不是很了解，所以找了一些现成的主题玩玩，总是找不到称心如意的，想办法自定义的话又太费事了。突然在网上看到一篇文章“<a href="http://sunwhut.com/2015/10/30/buildBlog/" target="_blank" rel="external">如何利用GitHub Pages和Hexo快速搭建个人博客</a>”，发现<a href="https://hexo.io/" target="_blank" rel="external">hexo</a>＋github搭建博客是如此的简单，而且<a href="https://github.com/iissnan/hexo-theme-next" target="_blank" rel="external">next主题</a>正合我的口味，所以按照上面提到的这篇文章开始着手起来搭建本博客了，整个过程非常的简单，只需改改配置就可以建立功能完善界面简约的博客了。接下来希望自己能好好沉淀，努力码字，妈妈再也不担心我的博客数据被清理掉了。</p>
<p><span style="color:red"><em>转载请注明出处，本文永久链接：<a href="http://sharkdtu.com/posts/blog-reborn.html">http://sharkdtu.com/posts/blog-reborn.html</a></em></span></p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[MLlib决策树与集成树]]></title>
      <url>http://sharkdtu.com/posts/mllib-tree.html</url>
      <content type="html"><![CDATA[<h2 id="模型简介"><a href="#模型简介" class="headerlink" title="模型简介"></a>模型简介</h2><p>决策树是一种常见的分类与回归机器学习算法，由于其模型表达性好，便于理解，并能取得较好的效果，而受到广泛的应用。下图是一个简单的决策树，决策树每个非叶子节点包含一个条件，对于具有连续值的特征，该条件为一个上界，如果实例对应的特征值小于该上界则被划分到左子节点，否则被划分到右子节点，对于具有离散值的特征，该条件为一个子集，如果实例对应的特征值属于该子集则被划分到左子节点，否则被划分到右子节点。如此下去，一个实例从根节点开始，不断地被划分，直到叶子节点。对于分类问题，叶子节点输出其类别，对于回归问题，叶子节点输出其分值或概率。<a id="more"></a></p>
<p><img src="/images/decision-tree.png" width="500" height="230" alt="decision-tree" align="center"></p>
<p>给定训练数据，如何学习决策树模型呢？首先将决策树抽象为一般的机器学习问题，用数学的说法就是，一个决策树对应着输入空间的一个划分以及在划分单元上的输出值。假设已经将输入空间划分为 $m$ 个单元(即叶子节点) $R_1, R_2,… R_m$，在每个单元 $R_i$ 上有一个固定的输出 $c_i$，那么决策树模型可表示为：</p>
<p>$$ f(x) = \sum_{i}^m c_i I(x \in R_i) $$</p>
<p>一般的机器学习算法是找一个目标函数，然后不断地去优化目标，直至训练误差在允许的范围内，得到模型参数。决策树的学习则采用启发式的方法，从根节点开始，根据该节点上的训练样本找到最优切分点，将训练样本划分为两个子集，一般来说，对于分类问题，每个子集采用多数表决的方式决定类别，对于回归问题，每个子集输出其中所有样本的平均值；每一次划分的依据是，划分后相对于划分前，不确实性有所减少，上述最优切分点就是不确定性减少的最多的切分方案，这里的不确定性可以是熵、基尼系数、平方误差、方差等；为了防止过拟合，需要设定划分的终止条件，通常有如下限制：1）树的最大深度，2）一次划分至少减少的不确定，3）一次划分后，子集中至少包含的样本实例数。</p>
<h2 id="决策树的训练"><a href="#决策树的训练" class="headerlink" title="决策树的训练"></a>决策树的训练</h2><p>MLlib中决策树的学习同样是利用前面所述的启发式方法，借助<code>RDD</code>进行分布式计算，其本质上是数据并行。下面是mllib中决策树学习的总体流程图，其最关键的一步就是如何找最优切分点，首先简单介绍下前后两步：初始化元数据以及如何判断切分终止条件，由于这两步实质上是由用户的参数配置和训练样本决定，暂且统称为算法参数，然后再着重详细讨论mllib如何分布式计算最优切分点。</p>
<p><img src="/images/tree-process.png" width="200" height="500" alt="tree-process" align="center"></p>
<h3 id="算法参数"><a href="#算法参数" class="headerlink" title="算法参数"></a>算法参数</h3><p>算法参数是由训练数据以及用户的参数配置共同决定，mllib提供给用户的自定义参数是通过类<code>Strategy</code>来封装，下面列出一些重要的参数并详细说明其意义：</p>
<ul>
<li>algo：指定决策树的类型，有分类树(classification)和回归树(Regression)</li>
<li>numClasses：如果是分类树，指定有多少种类别</li>
<li>subsamplingRate：训练样本采样率，如果在(0,1)之间则表明采样部分训练数据进行训练</li>
<li>impurity：指定不确定性指标，其中基尼系数(Gini)、熵(Entropy)用于分类树，方差(Variance)用于回归树</li>
<li>minInfoGain：每次切分至少减少的不确定性，否则停止切分，用于终止迭代计算</li>
<li>minInstancesPerNode：切分后，每个子节点至少包含的样本实例数，否则停止切分，用于终止迭代计算</li>
<li>maxDepth：树的最大深度，用于终止迭代计算</li>
<li>categoricalFeaturesInfo：指定离散特征，是一个map，featureId-&gt;K，其中K表示特征值可能的情况(0, 1, …, K-1)</li>
<li>maxBins：最大分箱数，当某个特征的特征值为连续时，该参数意思是将连续的特征值离散化为多少份</li>
</ul>
<h3 id="特征划分"><a href="#特征划分" class="headerlink" title="特征划分"></a>特征划分</h3><p>训练样本的每个特征可能具有各自的特点，mllib中将特征作如下划分：</p>
<ol>
<li>特征值只在有限的离散值范围内，把这类特征称为 categorical feature，这类特征需要通过配置参数指定，即前面所述的<code>categoricalFeaturesInfo</code>;</li>
<li>特征值为连续值，把这类特征称为 continuous feature，这类特征需要对其特征值进行离散化，便于后续的寻找最优切分点。</li>
</ol>
<p>在初始化元数据阶段，会根据输入的训练数据，得到样本的维度 <code>numFeatures</code>，在每个维度（特征）上计算所有可能的切分方案，在寻找最优切分点阶段会遍历所有可能的切分方案，找出最优的切分点。不同的特征、不同的决策树（分类还是回归），切分方案都不一样，下面作详细介绍。</p>
<h4 id="categorical-feature-切分"><a href="#categorical-feature-切分" class="headerlink" title="categorical feature 切分"></a>categorical feature 切分</h4><p>如果特征是 categorical feature(由参数<code>categoricalFeaturesInfo</code>指定)，那么有两种切分方法，对于取值较少的特征，采用不排序切分方法，例如，一个人的收入情况可能有三种情况“高、中、低”，其所有可能的切分方案为“高 | 中低”、“中 | 高低”、“高中 | 低”三种，更一般地，如果一个特征可能的取值为 $k$，那么所有可能的切分方案为 $2^{k-1}-1$ ，被切分后的部分看成一个个箱子（bin），如果被切分为“高 | 中低”，那么其分箱情况为：“高”在一个分箱，“中低”在另一个分箱，不难发现，所有可能的分箱数（bins）为 $2\times(2^{k-1}-1)=2^k-2$，我们把这种特征称为unordered feature，这种切分方法只被用于多分类问题。对于取值较多的特征，如果采用不排序的切分方法，那么所有可能的分箱数 $2^k-2$ 就会超过最大值maxBins了，这时就采用称之为排序切分的方法，将每个可能的取值看成一个分箱，例如一个人的年龄阶层可能有5种情况“老中青少幼”，那个所有可能的分箱数为5种，分别是：“老”、“中”、“青”、“少”、“幼”，所有可能的切分方案为4种（分箱数-1），具体按照什么顺序切分，mllib中按分箱内所有实例的不确定性顺序进行切分，例如上述5个分箱内的样本不确定性（基尼系数或是方差）从小到大为“老”、“中”、“青”、“少”、“幼”，那么可能的切分方案为：“老 | 中青少幼”、“老中 | 青少幼”、“老中青 | 少幼”、“老中青少 | 幼”，这种排序切分方案适用于二分类和回归问题。</p>
<h4 id="continuous-feature-切分"><a href="#continuous-feature-切分" class="headerlink" title="continuous feature 切分"></a>continuous feature 切分</h4><p>如果特征是 continuous feature(参数<code>categoricalFeaturesInfo</code>指定以外的特征)，那么首先就需要离散化，将连续的特征值划分到不同的分箱（bin）里去，分箱代表一个区间，一个实例的对应特征值在区间里的表示属于那个分箱，mllib采用的分位点排序策略，首先对所有训练数据进行采样，得到部分样本实例，假如其对应的某个特征值分别为0.1, 0.11, 0.2, 0.11, 0.6, 0.3, 0.22，对特征值进行排序得到0.1, 0.11, 0.11, 0.2, 0.22, 0.3, 0.6，如果参数maxBins设置为3，那么理论上平均每个分箱的实例数为样本实例数/numBin，即7/3=2.33，从左到右进行扫描，得到分箱为 $(-\infty, 0.2)，[0.2, 0.3)，[0.3, \infty)$ ，对应的所有切分方案（上界）为2种，分别为0.2, 0.3。</p>
<h3 id="最优切分点与树的形成"><a href="#最优切分点与树的形成" class="headerlink" title="最优切分点与树的形成"></a>最优切分点与树的形成</h3><p>经过上述分箱划分后，每个特征都对应一组分箱bins，如下图所示，根据分箱情况，将实例的所有特征映射到其对应的分箱中，每个分箱就包含一组实例，得到一些统计信息，用于计算不确定性。</p>
<p><img src="/images/tree-bins.png" width="400" height="230" alt="tree-bins" align="center"></p>
<p>对于分类树，假设不确定性指标为基尼系数gini，则每个bin的统计信息包含：1)每个类别对应的实例数 $C_i$ ；2)该bin的实例总和 $C$ 。那么可以根据以下公式求得基尼系数为：</p>
<p>$$ gini = \sum_{i}^K \frac {C_i} {C} (1 - \frac {C_i} {C}) $$</p>
<p>对于回归树，其不确定性指标为方差variance，则每个bin的统计信息包含：1)该bin的所有实例总和C；2)该bin所有实例的lebel总和 $\sum y_i$ ；3)该bin所有实例的lebel平方和 $\sum {y_i}^2$ 。那么可以根据以下公式（其中u为均值）求得方差为：</p>
<p>$$ variance = \frac {1} {C} \sum {(y_i - u)}^2 = \frac {1} {C} (\sum {y_i}^2 - \frac {1} {C} {(\sum {y_i})}^2) $$</p>
<p>Mllib在生成决策树的过程中，是按照树的层次一步步迭代，每一次对处于同一层的节点进行划分，相应地会触发一个job，如果树的深度为N，那么至少需要跑N个job才能训练得到一棵树，如下图所示。</p>
<p><img src="/images/tree-job.png" width="400" height="230" alt="tree-job" align="center"></p>
<p>假设已经迭代到上图中的job2，首先会为每个要切分的节点初始化一个<code>DTStatsAggregator</code>对象，<code>DTStatsAggregator</code>封装了所有特征分箱的统计信息，这些统计信息用于计算分箱的不确定性，下图显示一次迭代计算最优切分点的过程。</p>
<p><img src="/images/tree-job-process.png" width="600" height="330" alt="tree-job-process" align="center"></p>
<p>如上图，首先在每个partition上做一次map，遍历该partition上的所有实例，根据前面的分箱情况，将实例映射到分箱中，并更新分箱的统计信息， map操作后得到记录是<code>(nodeId, DTStatsAggregator)</code>，其中<code>nodeId</code>表示节点id，即需要对哪个节点进行切分，<code>DTStatsAggregator</code>对象保存了所有特征所有分箱的统计信息，然后再根据<code>nodeId</code>做一次reduce，将不同分区的分箱统计信息归并起来，最终得到不同节点(nodeId)上所有样本的分箱统计信息<code>(nodeId, DTStatsAggregator)</code>，最后再做一次map，对于每个节点，遍历所有可能的切分方案，找到不确定性减少最多的切分方案，即最优切分点。<code>DTStatsAggregator</code>对象中维护了一个大数组，该数组保存了所有特征的分箱统计信息，如下图所示，某个特征有3个bin，每个bin的统计信息由前面所述3个计算方差的统计值组成。</p>
<p><img src="/images/tree-bin-array.png" width="400" height="230" alt="tree-bin-array" align="center"></p>
<p>如何根据bin的统计信息遍历所有可能的切分方案呢？再看看前面讨论的特征划分，在初始化介绍后，我们是知道每个特征的所有可能的划分方案的，不同特征的划分也做了详细的区分。假如上图中feature是 continuous feature，那么3个bin对应2种切分方案，遍历这两种切分方案，得到切分后不确定减小的较多的切分方案，类似地，遍历所有特征的所有切分可能，最后得到一个最优切分点。</p>
<h2 id="集成树"><a href="#集成树" class="headerlink" title="集成树"></a>集成树</h2><p>集成树 Ensemble Tree 是综合多个决策树以得到更加泛化的模型，这样可以消除噪声，避免过拟合。常见的集成树有随机森林(Random Forests)和梯度提升树(Gradient-Boosted Tree)，下面分别详细介绍。</p>
<h3 id="随机森林"><a href="#随机森林" class="headerlink" title="随机森林"></a>随机森林</h3><p>随机森林(Random Forests)其实就是多个决策树，每个决策树有一个权重，对未知数据进行预测时，会用多个决策树分别预测一个值，然后考虑树的权重，将这多个预测值综合起来，对于分类问题，采用多数表决，对于回归问题，直接求平均。<br>随机森林模型训练实质上是同时训练多个决策树，每个决策树的训练样本是从原始训练集中抽样而来。在具体训练模型时，mllib做了一个优化，是同时对多个决策树进行训练，如下图所示，每个job会对一组节点进行切分，分组是按照树的层次逐步进行，每组需要切分的节点个数视内存大小而定，有一个参数<code>maxMemoryInMB</code>控制一次切分计算使用的最大内存大小，通过简单的估计节点切分计算所需内存（主要是计算分箱统计信息占用内存的大小）来决定一组节点的个数。</p>
<p><img src="/images/random-forest-job.png" width="400" height="230" alt="random-forest-job" align="center"></p>
<h3 id="梯度提升树"><a href="#梯度提升树" class="headerlink" title="梯度提升树"></a>梯度提升树</h3><p>梯度提升树(Gradient-Boosted Tree)简称GBT，是一种更加复杂的模型，它实质上是采用Boost方法，利用基本决策树模型得到的一种集成树模型。GBT的训练是每次训练一颗树，然后利用这颗树对每个实例进行预测，通过一个损失函数，计算损失函数的负梯度值作为残差，利用这个残差更新样本实例的label，然后再次训练一颗树去拟合残差，如此进行迭代，直到满足模型参数需求。GBT只适用于二分类和回归，不支持多分类，在预测的时候，不像随机森林那样求平均值，GBT是将所有树的预测值相加求和。</p>
<p><span style="color:red"><em>转载请注明出处，本文永久链接：<a href="http://sharkdtu.com/posts/mllib-tree.html">http://sharkdtu.com/posts/mllib-tree.html</a></em></span></p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[工作半年小感]]></title>
      <url>http://sharkdtu.com/posts/half-year-work-feeling.html</url>
      <content type="html"><![CDATA[<p><img src="/images/fight.jpg" alt="fight"></p>
<p>毕业到现在满打满半年了，这半年里可以说是变化巨大，正所谓计划赶不上变化，深圳房价翻倍了，身体不行了…很多时候感慨毕业选择来深圳就是个错误，整个人变得尤其的浮躁。一切都是当年设定的预想过于理想化了，导致落差大，使得自己成天瞎浮想。然而自己对于工作的激情却一直没消退，而且更加地热爱它，像是处于一种享受被强奸的状态。下面简单讲讲自己入职半年的个人感受吧。<a id="more"></a></p>
<p>在学校，常常认为会写代码才是王道，看论文、推导数学公式、以及啃算法都是浪费时间，光有理论得不到实践，不久就会忘了，还不知道学到的理论以后用得上用不上，so，本来在学校是应该修炼内功的，却在学招式，但是在职场，仅有点招式是没有优势的，因为招式慢慢地随着工作就会丰富，然而内功（包括理论基础以及对行业的认识广度）却不会，如果你不去有意识地去学习，自己永远只是一个码农搬砖的，而称不上工程师或者程序员。</p>
<p>可以说，这半年来我算是比较幸运的，因为我热爱我的工作，并且对它更加有激情，这得益于我们项目组新来的leader。刚入职的时候，我接手前人留下的垃圾代码，各种bug各种坑，不知道这种代码是怎么允许上线到生产环境中的，每天处理用户的bug反馈及抱怨，帮用户看问题，有种崩溃的感觉，这期间有跟mentor提过把代码重写一遍，但是mentor说成本太大，虽然代码有点乱，但是大部分还算稳定的，最好不要去动它。所以一直忍痛维护这套垃圾代码，自己实在受不了，私下默默地在重写，重写完后，跟mentor提过一次，还是同样的答复：风险太大。没办法，忍着，后面被安排到做另一个项目，同样，代码也是极其的乱，仅仅关注于功能实现，完全不考虑系统的整体结构以及可维护性，感觉特没意思。这个项目做了大概一个月后，项目组来了新leader，我对工作的激情从此也点燃起来了，新leader来的第一件事就是规范代码管理，以及调整人员组织结构，通过沟通尽量安排我们做自己喜欢的方向。这时我也有机会把我重写的代码完善上线，上线后虽然有少数几个小bug（很快得到修复），但是得到多数用户的好评，心中还是会有小成就感的。</p>
<p><span style="color:red"><em>转载请注明出处，本文永久链接：<a href="http://sharkdtu.com/posts/half-year-work-feeling.html">http://sharkdtu.com/posts/half-year-work-feeling.html</a></em></span></p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[Spark 应用程序调优]]></title>
      <url>http://sharkdtu.com/posts/spark-tun.html</url>
      <content type="html"><![CDATA[<p>对于很多刚接触Spark的人来说，可能主要关心数据处理的逻辑，而对于如何高效运行Spark应用程序了解较少。由于Spark是一种分布式内存计算框架，其性能往往受限于CPU、内存、网络等多方面的因素，对于用户来说，如何在有限的资源下高效地运行Spark应用程序显得尤为重要。下面只针对Spark-On-Yarn的一些常用调优策略做详细分析。<a id="more"></a></p>
<h2 id="配置参数优化"><a href="#配置参数优化" class="headerlink" title="配置参数优化"></a>配置参数优化</h2><h3 id="资源申请参数"><a href="#资源申请参数" class="headerlink" title="资源申请参数"></a>资源申请参数</h3><p>Spark-On-Yarn资源调度由Yarn来管理，用户只需指定Spark应用程序要申请的资源即可。我们首先来理解几个资源配置项，一旦资源配置确定，则只能在这些有限的资源下运行Spark应用程序。</p>
<ul>
<li>num-executors：同时运行的executor数。</li>
<li>executor-cores：一个executor上的core数，表示一次能同时运行的task数。一个Spark应用最多可以同时运行的task数为num-executors*executor-cores。</li>
<li>driver-memory：driver的内存大小。</li>
<li>executor-memory：executor内存大小，视任务处理的数据量大小而定。</li>
</ul>
<p>一开始我们只能通过大致的估算来确定上述资源的配置，例如一个Spark应用程序处理的数据大小为1T，如果读出来默认是500个partitions（可以通过测试运行，从web中查看的到），那么平均每个partition的大小为1T/500≈2G，默认情况下，考虑中间处理过程中的数据膨胀以及一些额外内存消耗，executor中可用于存放rdd的阈值设定为<code>spar.storage.memoryFraction=0.6</code>，所以存储partition需要的内存为<code>executor-memory*0.6</code>，稳妥一点设置executor-memory大于2G/0.6，如果一个executor不止是处理一个partition，假如num-executors设置为100，那么平均每个executor处理的partition为500/100=5，这时如果需要缓存rdd，那么executor-memory就要设置为大于5*2G/0.6；如果读出来的分区数很少（如100），一个partition很大（1T/100≈10G），使得executor-memory有可能OOM，那么就需要考虑加大分区数（调用repartition(numPartitions)等），增加task数量来减少一个task的数据量。一般来说一个executor处理的partition数最好不要超过5个，否则增加num-executors数，接上面的例子，500个分区，配置num-executors为100，每个executor需要处理5个partition。driver-memory的大小取决于最后的action操作，如果是调用collect，那么driver-memory的大小就取决于结果集rdd的大小，如果是调用count，那么driver-memory的大小只需要满足运行需求就够了，对于需要长时间迭代的Spark应用，driver端需要维护rdd的依赖关系，所以需要设置较大的内存。</p>
<p>上述仅仅是大致估算的资源配置，实际还要根据运行情况不断的调优，以达到资源最大化利用。例如，我们在运行日志中找到如下信息，它表明rdd_0的partition1内存大小为717.5KB，当我们得到这个信息后，就可以再次调整上述参数。</p>
<pre><code>INFO BlockManagerMasterActor: Added rdd_0_1 in memory on mbk.local:50311 (size: 717.5 KB, free: 332.3 MB)
</code></pre><h3 id="运行时参数"><a href="#运行时参数" class="headerlink" title="运行时参数"></a>运行时参数</h3><p>(1) spark.serializer</p>
<p>序列化对于Spark应用的性能来说，影响是非常大的，它涉及到网络传输以及存储，Spark默认是使用<code>org.apache.spark.serializer.JavaSerializer</code>，内部使用的是Java的<code>ObjectOutputStream</code>框架，这种序列化方式压缩比小，而且速度慢，强烈建议采用kyro序列化方式，它速度快，而且压缩比高，性能是Java序列化的10倍，修改配置<code>spark.serializer=org.apache.spark.serializer.KryoSerializer</code>即可，一般来说使用kyro序列化方式，需要在程序里面对用户自定义的可序列化的类进行注册，例如下面代码所示：</p>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">valconf = <span class="keyword">new</span> <span class="type">SparkConf</span>()</span><br><span class="line">conf.registerKryoClasses(<span class="type">Array</span>(classOf[<span class="type">MyClass1</span>], classOf[<span class="type">MyClass2</span>]))</span><br><span class="line">valsc = <span class="keyword">new</span> <span class="type">SparkContext</span>(conf)</span><br></pre></td></tr></table></figure>
<p>但是如果你不注册，kyro也是可以工作的，只是序列化效率差一点。</p>
<p>(2) spark.rdd.compress</p>
<p>这个参数决定了RDD Cache的过程中，RDD数据是否需要进一步压缩再Cache到内存或磁盘中，从内存看来，当内存比较稀缺时，如果不做压缩就Cache，就很可能会引发GC拖慢程序，从磁盘看来，压缩后数据量变小以减少磁盘IO。所以如果出现内存吃紧或者磁盘IO问题，就需要考虑启用RDD压缩。默认是关闭的。</p>
<p>(3) spark.storage.memoryFraction</p>
<p>前面提到的executor-memory决定了每个executor可用内存的大小，而spark.storage.memoryFraction则决定了在这部分内存中有多少可以用于管理RDD Cache数据，剩下的内存用来保证任务运行时各种其它内存空间的需要。<code>spark.executor.memoryFraction</code>默认值为0.6，官方文档建议这个比值不要超过JVM Old Gen区域的比值，因为RDD Cache数据通常都是长期驻留内存的，理论上也就是说最终会被转移到Old Gen区域，如果这部分数据允许的尺寸太大，势必把Old Gen区域占满，造成频繁的FULL GC。如果发现Spark应用在运行过程中发生频繁的FULL GC，就需要考虑减小该配置，所以建议这个配置不要加大，如果内存吃紧，可以考虑采用内存和磁盘的混合缓存模式，进一步减少RDD Cache还可以考虑序列化以及压缩等。</p>
<p>(4) spark.shuffle.memoryFraction</p>
<p>在启用Spill的情况（<code>spark.shuffle.spill</code>默认开启）下，<code>spark.shuffle.memoryFraction</code>表示Shuffle过程中使用的内存达到总内存多少比例的时候开始Spill。<code>spark.shuffle.memoryFraction</code>默认值为0.2，调整该值可以调整Shuffle过程中Spill的频率。总的来说，如果Spill太过频繁，可以适当增加<code>spark.shuffle.memoryFraction</code>的大小，增加用于Shuffle的内存，减少Spill的次数。然而这样一来为了避免内存溢出，对应的可能需要减少RDD cache占用的内存，即减小<code>spark.storage.memoryFraction</code>的值，这样RDD cache的容量减少，有可能带来性能影响，因此需要综合考虑，如果在你的Spark应用程序中RDD Cache较少，Shuffle数据量较大，就需要把<code>spark.shuffle.memoryFraction</code>调大一些，把<code>spark.storage.memoryFraction</code>调小一些。</p>
<p>(5) spark.shuffle.file.buffer.kb</p>
<p>每次shuffle过程驻留在内存的buffer大小，在shuffle中间数据的产生过程中可减少硬盘的IO操作。<code>spark.shuffle.file.buffer.kb</code>默认为32，若Spark应用程序运行过程中Shuffle称为瓶颈，根据需要适当的加大该配置。</p>
<h2 id="接口使用优化"><a href="#接口使用优化" class="headerlink" title="接口使用优化"></a>接口使用优化</h2><p>对于Spark新手来说，可能不太了解RDD接口内部实现细节，主要关心业务数据处理，然而这往往导致编写出来的Spark应用程序运行效率不高，资源利用浪费等。下面简单介绍一些常见的Spark应用开发注意细节。</p>
<h3 id="缓存接口"><a href="#缓存接口" class="headerlink" title="缓存接口"></a>缓存接口</h3><p>Spark比MapReduce快的很大一部分原因是它可以把中间结果RDDCache起来，不用每次需要时重新计算。但是如果Cache使用不当，会造成内存吃紧，要么带来不必要的磁盘IO，要么引起频繁的FULL GC，拖慢程序运行。</p>
<p>对于一个需要多次使用的临时RDD（类似于临时变量），尽可能要把它Cache起来，这样这个临时RDD只会计算一次，以后每次都会从Cache里直接取。如下面的例子，需要统计第一个字段大于100的数目和第二个字段大于100的数目，如果data不做Cache，因为只有遇到RDD的Action接口时才出发计算，所以在计算firstCnt时会读一遍数据，计算secondCnt时还会再读一遍数据，这样就造成一些不必要的计算，对data做了Cache后，在计算firstCnt时读一次，计算secondCnt就会直接从Cache中取而不用再次读一次。</p>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> data = sc.textFile(path)</span><br><span class="line">data.cache()</span><br><span class="line"><span class="keyword">val</span> firstCnt = data.filter(x(<span class="number">0</span>).toInt =&gt; <span class="number">100</span>).count()</span><br><span class="line"><span class="keyword">val</span> secondCnt = data.filter(x(<span class="number">1</span>).toInt =&gt; <span class="number">100</span>).count()</span><br></pre></td></tr></table></figure>
<p>很多时候会看到这样的代码，在对两个RDD进行Join时，把两个RDD都Cache起来再做Join，这里一定要明白一点，没有调用Action接口，计算是不会触发的，下面的代码如果后续不再用到rdd1和rdd2，是没有必要对rdd1和rdd2做Cache的，这里要做Cache的是data。</p>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> data = <span class="keyword">val</span> data = sc.textFile(path)</span><br><span class="line"><span class="keyword">val</span> rdd1 = data.map(…).cache()</span><br><span class="line"><span class="keyword">val</span> rdd2 = data.map(…).cache()</span><br><span class="line"><span class="keyword">val</span> rdd3 = rdd1.join(rdd2).count()</span><br></pre></td></tr></table></figure>
<p>对于内部需要多次迭代的Spark应用来说，应该尽量将每次迭代用到的临时RDD缓存起来，在这个临时RDD被更新时，需要将旧的缓存手动清除掉。如下例子显示，每次迭代都需要在curRDD基础上进行更新得到updatedRDD，在一轮迭代结束后要更新curRDD为updatedRDD，在更新前手动将之前的curRDDCache清理掉，防止内存被耗光，引发频繁FULL GC。</p>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> data = sc.textFile(path)</span><br><span class="line"><span class="comment">// some transformations in init(data)</span></span><br><span class="line">varcurRDD = init(data).cache()</span><br><span class="line"><span class="keyword">val</span> result = <span class="keyword">new</span> <span class="type">ArrayBuffer</span>[<span class="type">Double</span>]()</span><br><span class="line"><span class="comment">// some transformations and an action in getResult(curRDD)</span></span><br><span class="line">result += getResult(curRDD)</span><br><span class="line"><span class="comment">// Start Iteration</span></span><br><span class="line"><span class="keyword">var</span> changed = <span class="literal">true</span></span><br><span class="line"><span class="keyword">while</span>(changed) &#123;</span><br><span class="line">  <span class="comment">// some transformations in iteration(curRDD)</span></span><br><span class="line">  valupdatedRDD = iteration(curRDD).cache()</span><br><span class="line">  <span class="comment">// getResultand check if the value is changed</span></span><br><span class="line">  <span class="keyword">val</span> x = getResult(updatedRDD)</span><br><span class="line">  <span class="comment">// convergence</span></span><br><span class="line">  <span class="keyword">if</span>(x == result.last) changed = <span class="literal">false</span></span><br><span class="line">  <span class="comment">// Unpersist old RDD and assign new RDD</span></span><br><span class="line">  curRDD.unpersist(<span class="literal">false</span>)</span><br><span class="line">  curRDD = updatedRDD</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在对RDD做缓存时，还应考虑内存大小情况选择合适的缓存方式，Spark提供以下几种缓存：</p>
<ul>
<li>MEMORY_ONLY：直接将RDD对象保存到内存中，Spark默认选项</li>
<li>MEMORY_AND_DISK：当内存不够的时候，保存到磁盘中（内存较为稀缺的时候用，比MEMORY_ONLY占用更少的内存，但是会带来磁盘IO）</li>
<li>MEMORY_ONLY_SER：将RDD序列化后保存到内存中（内存较为稀缺的时候用，比MEMORY_ONLY占用更少的内存）</li>
<li>MEMORY_AND_DISK_SER：将RDD序列化后保存到内存中，内存不够时保存到磁盘中（内存较为稀缺的时候用，比MEMORY_ONLY_SER更安全）</li>
<li>DISK_ONLY：保存到磁盘中（不建议用）</li>
<li>MEMORY_ONLY_2：与MEMORY_ONLY类似，只是保存两份</li>
<li>MEMORY_AND_DISK_2：与MEMORY_AND_DISK类似，只是保存两份</li>
<li>OFF_HEAP ：将序列化后的RDD保存到Tachyon（一种分布式内存文件系统）中，相比于MEMORY_ONLY_SER可以避免GC的额外开销。这种缓存方式还在试验阶段</li>
</ul>
<p>根据具体情况判断使用何种缓存方式，调用的时候直接通过如<code>rdd.persist(StorageLevel.MEMORY_AND_DISK_SER)</code>方式实现，调用<code>rdd.cache()</code>默认是<code>rdd.persist(StorageLevel.MEMORY_ONLY)</code>。</p>
<h3 id="引发Shuffle的相关接口"><a href="#引发Shuffle的相关接口" class="headerlink" title="引发Shuffle的相关接口"></a>引发Shuffle的相关接口</h3><p>一个Spark应用程序运行快慢，往往受限于中间的Shuffle过程，Shuffle涉及到网络以及磁盘IO，是整个Spark应用程序运行过程中较为耗时的阶段。在编写Spark应用程序时，应当尽量减少Shuffle次数。下面列举常见的可能引发Shuffle的接口。</p>
<ul>
<li>distinct</li>
<li>Intersection/subtracted</li>
<li>reduceByKey/aggregateByKey</li>
<li>repartition</li>
<li>cogroup</li>
<li>join</li>
<li>sortBy/sortByKey</li>
<li>groupBy/groupByKey</li>
<li>partitionBy</li>
</ul>
<p>如果executor内存不足以处理一个partition，那么这时考虑调用repartition来加大分区数，使得每个partition的数据量减少以至于executor可以处理，一般来说上述接口也可以接受numPartitions参数来指定分区数。上述接口连续调用不一定会带来多次Shuffle，只要partition类型和partition数不变，是不会增加Shuffle次数的，如下代码则只有一次Shuffle：</p>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">rdd.map(x =&gt; (x, x+<span class="number">1</span>)).repartition(<span class="number">1000</span>).reduceByKey(_ + _).count()</span><br></pre></td></tr></table></figure>
<p>然而如下代码却会有两次Shuffle：</p>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">rdd.map(x =&gt; (x, x+<span class="number">1</span>)).repartition(<span class="number">1000</span>).reduceByKey(_ + _, <span class="number">3000</span>).count()</span><br></pre></td></tr></table></figure>
<p>很多人在一开始调用了触发Shuffle的相关接口，后面可能数据膨胀了，发现需要更多的partition，所以在后面调用触发Shuffle的相关接口时加大partition数，这样就会导致多次Shuffle，所以一开始就确定好最后的partition数，以免做不必要的Shuffle。</p>
<h3 id="接口对比"><a href="#接口对比" class="headerlink" title="接口对比"></a>接口对比</h3><p>(1) sortBy/sortByKey与takeOrdered</p>
<p>有时候我们可能希望对数据集排序取前n条记录，很多人会像如下代码一样实现：</p>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">rdd.sortBy(x =&gt; x.key).take(n)</span><br><span class="line"><span class="comment">//or rdd.sortByKey().take(n)</span></span><br></pre></td></tr></table></figure>
<p>然而，有一个更有效的办法，就是按照以下方式实现：</p>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">rdd.takeOrdered(n)</span><br></pre></td></tr></table></figure>
<p>以上两者的区别在于，第一种方式需要把所有partition的排序结果进行归并再取前n条记录，第二种方式是从每个排好序的partition中取出前n条记录最后再归并为n条记录，大大降低了网络IO，提升整体性能。</p>
<p>(2) groupBy/groupByKey与aggregateByKey</p>
<p>在做分组计算时，首先会想到使用<code>groupBy/groupByKey</code>接口，值得一提的是，<code>groupBy/groupByKey</code>接口特别占用内存，它是把具有相同key值的所有value放到一个buffer数组里，如果某个key对应的value非常多，极其容易引发OutOfMemoryError，通过<code>groupBy/groupByKey</code>实现的分组计算功能是可以通过<code>aggregateByKey</code>或者<code>reduceByKey</code>来实现的，<code>aggregateByKey/reduceByKey</code>内部是通过combineByKey实现的，当内存超过一定阈值会spill到磁盘，相对来说较为安全。当通过<code>groupBy/groupByKey</code>接口最后返回的<code>RDD[(K, V)]</code>中<code>V</code>不是序列时，可以用<code>reduceByKey</code>实现，当<code>V</code>是序列时可以用<code>aggregateByKey</code>实现，例如需要统计key对应的value最大值：</p>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//rdd: RDD[(int, int)]</span></span><br><span class="line">rdd.groupByKey().map((k, vb) =&gt; (k, vb.max))</span><br></pre></td></tr></table></figure>
<p>我们完全可以用reduceByKey来实现上述功能：</p>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">rdd.reduceByKey ((v1, v2) =&gt; <span class="type">Math</span>.max(v1, v2))</span><br></pre></td></tr></table></figure>
<p>再比如，就想返回key对应的所有value：</p>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//rdd: RDD[(int, int)]</span></span><br><span class="line">rdd.groupByKey()</span><br></pre></td></tr></table></figure>
<p>我们完全可以用aggregateByKey来实现上述功能：</p>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">rdd. aggregateByKey(<span class="type">Seq</span> ())(</span><br><span class="line">(u, v) =&gt; v::u,</span><br><span class="line">(u1, u2) =&gt; u1 ++ u2</span><br><span class="line">)</span><br></pre></td></tr></table></figure>
<p>以上是简单提出几个需要注意的接口调用，如果不了解RDD接口的使用，可以参见<a href="http://spark.apache.org/docs/latest/api/scala/index.html#org.apache.spark.package" target="_blank" rel="external">社区文档</a>。</p>
<p><span style="color:red"><em>转载请注明出处，本文永久链接：<a href="http://sharkdtu.com/posts/spark-tun.html">http://sharkdtu.com/posts/spark-tun.html</a></em></span></p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[写在最后一个寒假前]]></title>
      <url>http://sharkdtu.com/posts/before-last-winter-vacation.html</url>
      <content type="html"><![CDATA[<p>明天回家，这应该是我的上学生涯最后的一个寒假，寒假过后差不多就开始步入职场。突然觉得要好好珍惜这个假期，但是又不知从何去珍惜。记得每次寒假前总是计划着要看好多书学好多东西，最终证明带的书终究还是压箱底的，在家宁可过着猪一般的生活，躺在被子里发呆也不愿意做任何事。不过想想，家里真的是不适合学习，大冬天，没暖气，手端着书本拿着鼠标都会冻得发紫，所有的学习热情都被冻得拔凉拔凉的，假期前的美好计划也都不了了之。又想着最后一个长假，这样过去真是有点敷衍人生，还是激励下自己克服寒冷，借着本站点好好写点东西，但凡假期遇到的好玩或者有所感触的事都把它记录下来，培养自己码字的习惯，要是自己能坚持下来写东西，也就无悔了。<a id="more"></a></p>
<p>想想这学期过得其实还是挺舒服的，没有以前那样苦逼地为导师折腾这折腾那，也没有忍受导师一些无理取闹的要求，这学期心态也特别好，遇到困难时心里也只是想着忍耐，没有抱怨，反正离毕业也不远了。总的来说，从暑假到现在进行的还是很顺心的，这学期应该说是我读研以来最为辉煌的一段时间，工作早早地搞定了，即将去自己喜欢的城市，论文也进展地有条不紊，两年多的痛苦到这学期也显现出点价值来了，不久前拿了国奖，因为前段时间被导师的一些破事折腾的心情极其烦躁，之前说好的拿国奖请客也没有开展下去，只是请了几个平时较为熟悉的实验室同学，一直觉得这确实有点不对。</p>
<p>说说找工作吧，我的找工作历程相对来说比较简单，追求也没有太高，见好就收了，所有没有太多的波折。倒是找暑期实习挺曲折的，大概是5-6月份的时候，投简历真是面一家挂一家，要么是不对口，要么是自己水平太差，平时在实验室干的活跟找工作也好像没有两毛钱关系，互联网公司对刚毕业的学生来说主要还是看基础，外企基本就是问算法，leetcode上的题刷几遍后就可以八九不离十的拿下，甚至有同学直接去github上找现成的代码打印，跟考期复习一样背熟也行。我本身是比较熟悉C/C++的，也研读过apue/unp这两本书，一直是冲着linux后台这个方向去找，但是事与愿违，可能是自我感觉太良好了。不过好在我努力地复习基础，仔细研究我心仪公司（其实主要是该公司总部在心仪的城市）的笔试面试题，最后顺利地拿下暑期实习offer，可是从事php开发，当时也不爽了一段时间，毕竟要重新开始学，不过心态调整的还不错，学习起来也很快，作为初级程序员真的不能逃避学新东西，否则成长起来会很慢。虽说实习进了心仪的公司，但是由于实验室的很多破事，需要经常回学校露露脸，所以万般无奈没选择去心仪的城市，最后在实习生转正阶段大费周折地调岗换城市，这应该算是找工作中最为波折的一段了，好在付出没有白白浪费，顺利地去了心仪的城市，岗位也换成了一个较为喜欢的方向（大数据平台开发），整个过程下来觉得我还算幸运的。</p>
<p><span style="color:red"><em>转载请注明出处，本文永久链接：<a href="http://sharkdtu.com/posts/before-last-winter-vacation.html">http://sharkdtu.com/posts/before-last-winter-vacation.html</a></em></span></p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[Akka-remote使用入门]]></title>
      <url>http://sharkdtu.com/posts/start-akka-remote.html</url>
      <content type="html"><![CDATA[<p>在上一篇文章中讲了<a href="/posts/start-akka-actor.html">akka-actor的简单使用</a>，那主要是展现了akka在一台机器上的并发应用，这一篇接着介绍akka-remote使用，简单了解akka在不同机器上的并发应用。我们知道，在一台机器上是多个本地actor之间发送消息，那么如果是在多台机器上，则不同机器上的actor是通过网络通信来发送消息的。假如，我们还是用之前的蒙特卡洛求π的例子，之前在一台机器上计算启动多个actor同时计算，其根源还是利用的一台机器的资源，如果将计算任务分发到多台机器上同时运行，最后汇总下多台机器的计算结果，每台机器的计算任务就不会太大。<a id="more"></a><br>如下图所示，每个方框代表一台机器，我们通过 driver actor 发起计算到 master actor ， master在本地创建一个路由 actor workerRouter来分发任务，任务需要在不同的机器上运行，最后汇总不同机器的计算结果，得到π的近似值。</p>
<p><img src="/images/akka-remote.png" alt="akka-remote"></p>
<p>同样首先创建sbt项目，在项目根目录下新建sbt项目描述文件<code>build.sbt</code>：</p>
<pre><code>name := &quot;akka-remote-pi&quot;
[空行]
version := &quot;1.0.0&quot;
[空行]
scalaVersion := &quot;2.10.4&quot;
[空行]
libraryDependencies += &quot;com.typesafe.akka&quot; %% &quot;akka-actor&quot; % &quot;2.3.7&quot;
[空行]
libraryDependencies += &quot;com.typesafe.akka&quot; %% &quot;akka-remote&quot; % &quot;2.3.7&quot;
</code></pre><p>我们定义actor之间的消息格式，与上一篇文章中<a href="/posts/start-akka-actor.html">akka-actor</a>消息一样：</p>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">sealed</span> <span class="class"><span class="keyword">trait</span> <span class="title">PiMessage</span></span></span><br><span class="line"><span class="comment">// 计算启动消息</span></span><br><span class="line"><span class="keyword">case</span> <span class="class"><span class="keyword">object</span> <span class="title">Calculate</span> <span class="keyword">extends</span> <span class="title">PiMessage</span></span></span><br><span class="line"><span class="comment">// 分配的任务，需要做几次投掷测试</span></span><br><span class="line"><span class="keyword">case</span> <span class="class"><span class="keyword">class</span> <span class="title">Work</span>(<span class="params">times: <span class="type">Int</span></span>) <span class="keyword">extends</span> <span class="title">PiMessage</span></span></span><br><span class="line"><span class="comment">// Worker一次任务计算结果，包含落在内切圆里的点的个数</span></span><br><span class="line"><span class="keyword">case</span> <span class="class"><span class="keyword">class</span> <span class="title">Result</span>(<span class="params">value: <span class="type">Int</span></span>) <span class="keyword">extends</span> <span class="title">PiMessage</span></span></span><br><span class="line"><span class="comment">// 总的计算结果，包含π的近似值与计算所花的时间</span></span><br><span class="line"><span class="keyword">case</span> <span class="class"><span class="keyword">class</span> <span class="title">PiApproximation</span>(<span class="params">pi: <span class="type">Double</span>, duration: <span class="type">Duration</span></span>)</span></span><br><span class="line">    <span class="keyword">extends</span> <span class="type">PiMessage</span></span><br></pre></td></tr></table></figure>
<p>worker actor也与上一篇一样，收到分发的任务后，通过随机掷点的方法统计落在内切圆中点个数：</p>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Worker</span> <span class="keyword">extends</span> <span class="title">Actor</span> </span>&#123;</span><br><span class="line">  <span class="comment">// 蒙特卡洛测试</span></span><br><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">testMonteCarlo</span></span>(times: <span class="type">Int</span>): <span class="type">Int</span> = &#123;</span><br><span class="line">    <span class="keyword">var</span> acc = <span class="number">0</span></span><br><span class="line">    <span class="keyword">for</span> (i &lt;- <span class="number">0</span> until times) &#123;</span><br><span class="line">      <span class="keyword">val</span> x = random * <span class="number">2</span> - <span class="number">1</span> <span class="comment">//生成[-1,1]区间的横坐标</span></span><br><span class="line">      <span class="keyword">val</span> y = random * <span class="number">2</span> - <span class="number">1</span> <span class="comment">//生成[-1,1]区间的纵坐标</span></span><br><span class="line">      <span class="keyword">if</span> (x * x + y * y &lt; <span class="number">1</span>) acc += <span class="number">1</span></span><br><span class="line">    &#125;</span><br><span class="line">    acc</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">receive</span> </span>= &#123;</span><br><span class="line">    <span class="keyword">case</span> <span class="type">Work</span>(times) =&gt;</span><br><span class="line">      sender ! <span class="type">Result</span>(testMonteCarlo(times))</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>worker actor需要启动远程通信功能，需要在项目资源目录里添加<code>worker.conf</code>配置：</p>
<pre><code>akka {
  actor {
    provider = &quot;akka.remote.RemoteActorRefProvider&quot;
  }
  remote {
    netty.tcp {
      //这里是worker actor远程通信地址和端口，
      //我这里是在本机上测试的
      hostname = &quot;127.0.0.1&quot;
      port = 2554
    }
  }
}
</code></pre><p>下面定义 master actor，与上一篇有点不一样，这里利用了配置文件功能，在创建<code>WorkerRouter</code>时从配置中加载路由配置，<code>nrOfMessages</code>和<code>times</code>也从配置文件中读取，这就不需要直接放到启动参数中去了。</p>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Master</span> <span class="keyword">extends</span> <span class="title">Actor</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 计算发起者</span></span><br><span class="line">    <span class="keyword">var</span> calculateSender: <span class="type">ActorRef</span> = _</span><br><span class="line">    <span class="comment">// worker计算结果的累加统计</span></span><br><span class="line">    <span class="keyword">var</span> acc: <span class="type">Int</span> = <span class="number">0</span></span><br><span class="line">    <span class="comment">// 记录收到的结果消息个数</span></span><br><span class="line">    <span class="keyword">var</span> nrOfResults: <span class="type">Int</span> = <span class="number">0</span></span><br><span class="line">    <span class="comment">// 计算启动时间</span></span><br><span class="line">    <span class="keyword">val</span> start: <span class="type">Long</span> = <span class="type">System</span>.currentTimeMillis</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 路由actor</span></span><br><span class="line">    <span class="keyword">val</span> workerRouter = context.actorOf(</span><br><span class="line">      <span class="type">FromConfig</span>.props(<span class="type">Props</span>[<span class="type">Worker</span>]),</span><br><span class="line">      name = <span class="string">"workerRouter"</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">val</span> cfg = context.system.settings.config</span><br><span class="line">    <span class="keyword">val</span> nrOfMessages: <span class="type">Int</span> = cfg.getInt(<span class="string">"parameters.nrOfMessages"</span>)</span><br><span class="line">    <span class="keyword">val</span> times: <span class="type">Int</span> = cfg.getInt(<span class="string">"parameters.times"</span>)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">receive</span> </span>= &#123;</span><br><span class="line">      <span class="keyword">case</span> <span class="type">Calculate</span> =&gt;</span><br><span class="line">        calculateSender = sender <span class="comment">// 先保存计算发起者的ActorRef，以便后续回复结果</span></span><br><span class="line">        <span class="keyword">for</span> (i &lt;- <span class="number">0</span> until nrOfMessages) workerRouter ! <span class="type">Work</span>(times)</span><br><span class="line"></span><br><span class="line">      <span class="keyword">case</span> <span class="type">Result</span>(value) =&gt;</span><br><span class="line">        acc += value</span><br><span class="line">        nrOfResults += <span class="number">1</span></span><br><span class="line">        <span class="keyword">if</span> (nrOfResults == nrOfMessages) &#123;</span><br><span class="line">          <span class="keyword">val</span> pi = (<span class="number">4.0</span> * acc) / (nrOfMessages * times)</span><br><span class="line">          calculateSender ! <span class="type">PiApproximation</span>(pi,</span><br><span class="line">            (<span class="type">System</span>.currentTimeMillis - start).millis)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>master actor需要启动远程通信功能，需要在项目资源目录里添加<code>master.conf</code>配置文件：</p>
<pre><code>akka {
  actor {
    provider = &quot;akka.remote.RemoteActorRefProvider&quot;
    deployment {
      &quot;/master/workerRouter&quot; {
        router = round-robin-pool
        nr-of-instances = 10 //在target.nodes机器上启动10个worker
        target.nodes = [ //路由分发任务的worker actor地址
        //  &quot;akka.tcp://WorkerSystem@127.0.0.1:2554&quot;,
          &quot;akka.tcp://WorkerSystem@127.0.0.1:2554&quot;
        ]
      }
    }
  }
  remote {
    netty.tcp {
      //这里是master actor远程通信地址和端口，
      //我这里是在本机上测试的
      hostname = &quot;127.0.0.1&quot;
      port = 2553
    }
  }
}
parameters {//参数
  nrOfMessages = 1000
  times = 1000
}
</code></pre><p>下面我们开始定义 driver actor，它需要一个master地址参数，主要是通过<code>actorSelection</code>函数查询到 master actor，并向其发起计算，最后得到计算结果：</p>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Driver</span>(<span class="params">masterPath</span>) <span class="keyword">extends</span> <span class="title">Actor</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">receive</span> </span>= &#123;</span><br><span class="line">      <span class="keyword">case</span> <span class="type">Calculate</span> =&gt;</span><br><span class="line">        context.actorSelection(masterPath) ! <span class="type">Calculate</span></span><br><span class="line"></span><br><span class="line">      <span class="keyword">case</span> <span class="type">PiApproximation</span>(pi, duration) =&gt;</span><br><span class="line">        println(</span><br><span class="line">          <span class="string">"Pi: \t"</span> + pi + <span class="string">"\n"</span> +</span><br><span class="line">          <span class="string">"Spend: \t"</span> + duration)</span><br><span class="line">        context.system.shutdown()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>类似地，需要开始远程功能，则添加配置<code>driver.conf</code>：</p>
<pre><code>akka {
  actor {
    provider = &quot;akka.remote.RemoteActorRefProvider&quot;
  }
  remote {
    netty.tcp {
      //这里是worker actor远程通信地址和端口，
      //我这里是在本机上测试的
      hostname = &quot;127.0.0.1&quot;
      port = 0
    }
  }
}
</code></pre><p>所有actor和配置都定义完了，下面我们开始分别编写启动actor的函数以及入口函数：</p>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">startWorkerSystem</span></span>(): <span class="type">Unit</span> = &#123;</span><br><span class="line">  <span class="type">ActorSystem</span>(<span class="string">"WorkerSystem"</span>, <span class="type">ConfigFactory</span>.load(<span class="string">"worker"</span>))</span><br><span class="line">  println(<span class="string">"Started WorkerSystem"</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">startMasterSystem</span></span>(): <span class="type">Unit</span> = &#123;</span><br><span class="line">  <span class="keyword">val</span> system = <span class="type">ActorSystem</span>(<span class="string">"MasterSystem"</span>, <span class="type">ConfigFactory</span>.load(<span class="string">"master"</span>))</span><br><span class="line">  system.actorOf(<span class="type">Props</span>(classOf[<span class="type">Master</span>]))</span><br><span class="line">  println(<span class="string">"Started MasterSystem"</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">startDriverSystem</span></span>(): <span class="type">Unit</span> = &#123;</span><br><span class="line">  <span class="keyword">val</span> system = <span class="type">ActorSystem</span>(<span class="string">"DriverSystem"</span>, <span class="type">ConfigFactory</span>.load(<span class="string">"driver"</span>))</span><br><span class="line">  <span class="keyword">val</span> masterPath = <span class="string">"akka.tcp://MasterSystem@127.0.0.1:2553/user/master"</span></span><br><span class="line">  <span class="keyword">val</span> driver = system.actorOf(<span class="type">Props</span>(classOf[<span class="type">Driver</span>], masterPath),</span><br><span class="line">    name = <span class="string">"driver"</span>)</span><br><span class="line">  driver ! <span class="type">Calculate</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">main</span></span>(args: <span class="type">Array</span>[<span class="type">String</span>]) &#123;</span><br><span class="line">  <span class="keyword">if</span> (args.isEmpty &amp;&amp; args.head == <span class="string">"worker"</span>)</span><br><span class="line">    startWorkerSystem()</span><br><span class="line">  <span class="keyword">if</span> (args.isEmpty &amp;&amp; args.head == <span class="string">"master"</span>)</span><br><span class="line">    startMasterSystem()</span><br><span class="line">  <span class="keyword">if</span> (args.isEmpty &amp;&amp; args.head == <span class="string">"driver"</span>)</span><br><span class="line">    startDriverSystem()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>用sbt编译后，可以在一个终端里直接<code>sbt run</code>运行，也可以开三个终端，先开启worker，然后开启master，最后开启driver。</p>
<p><span style="color:red"><em>转载请注明出处，本文永久链接：<a href="http://sharkdtu.com/posts/start-akka-remote.html">http://sharkdtu.com/posts/start-akka-remote.html</a></em></span></p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[Akka-actor使用入门]]></title>
      <url>http://sharkdtu.com/posts/start-akka-actor.html</url>
      <content type="html"><![CDATA[<p>学习scala编程，不可避免的会接触到actor模式，它使得并发编程不再像噩梦般萦绕着开发者，Akka是actor的一个开源实现。由于本人水平有限，自认为还不能把actor设计思想讲明白，所以本文仅仅是一个使用akka-actor的入门参考以及个人的入门心得，其具体原理及设计思想请参考相关资料，推荐<a href="http://doc.akka.io/docs/akka/2.3.7/general/actor-systems.html" target="_blank" rel="external">Akka的官方文档</a>，里面讲的很清晰，另外，<a href="http://rerun.me/2014/09/11/introducing-actors-akka-notes-part-1/" target="_blank" rel="external">国外一个人的笔记</a>写的相当不错，原理加上其配图讲的非常形象。<a id="more"></a></p>
<p>下面我通过一个简单例子来阐述akka-actor的使用，假如我们需要编写一个程序，利用<a href="https://zh.wikipedia.org/wiki/%E8%92%99%E5%9C%B0%E5%8D%A1%E7%BE%85%E6%96%B9%E6%B3%95" target="_blank" rel="external">蒙特卡洛算法</a>求圆周率π，我们知道蒙特卡洛算法是采用统计模拟方法，在一个边长为2的正方形内，一个点落在其内切圆内的概率为π/4（圆的面积/正方形的面积），编写程序时，假设有多个actor同时随机在正方形内掷点，统计所有actor掷的点，把那些落在内切圆内的点的个数加起来再除以所有actor掷的总次数，即为一个点落在内切圆内概率的近似值，进而可以得到π的近似值。</p>
<p>首先我们通过sbt创建项目，根据sbt项目的目录结构生成项目的雏形（可以将以下命令写到一个脚本里，以后重复使用）：</p>
<pre><code>[hadoop@master ~]$ mkdir -p akka-actor-pi/lib
[hadoop@master ~]$ mkdir -p akka-actor-pi/project
[hadoop@master ~]$ mkdir -p akka-actor-pi/src/main/scala
[hadoop@master ~]$ mkdir -p akka-actor-pi/src/main/resources
[hadoop@master ~]$ mkdir -p akka-actor-pi/src/test/scala
</code></pre><p>在项目根目录akka-actor-pi下新建sbt项目描述文件<code>build.sbt</code>，注意每行之间有一个空行:</p>
<pre><code>name := &quot;akka-actor-pi&quot;
[空行]
version := &quot;1.0.0&quot;
[空行]
scalaVersion := &quot;2.10.4&quot;
[空行]
libraryDependencies += &quot;com.typesafe.akka&quot; %% &quot;akka-actor&quot; % &quot;2.3.7&quot;
</code></pre><p>在project目录里添加<code>build.properties</code>文件，在其中声明sbt的版本信息：</p>
<pre><code>sbt.version=0.13.6
</code></pre><p>如下图所示为程序求π的actor系统结构图，程序入口通过向master发起计算请求，master将任务分发到许多worker上运行（类似于启动多个线程并行计算），中间通过一个路由actor来间接的分发任务，把路由actor看成网络中的路由器，当其收到任务请求后，它会按某种调度方式来分发到它关联的那些actor上，在我们的程序实现中，采用轮询的方式挨个分发任务。</p>
<p><img src="/images/akka-actor.png" alt="akka-actor"></p>
<p>下面开始定义这个系统中流动的消息，我们需要四种消息：</p>
<ul>
<li>Calculate： 发送给master来启动计算；</li>
<li>Work： 从master发送给各worker，包含工作分配的内容；</li>
<li>Result： 从worker发送给master，包含worker的计算结果；</li>
<li>PiApproximation： 从master返回给调用端，包含π的最终计算结果和整个计算耗费的时间；</li>
</ul>
<p>发送给actor的消息应该永远是不可变的，在scala里有<code>case classes</code>来构造完美的消息，下面是用<code>case class</code>创建四种消息，其中创建一个通用的基础<code>trait</code>(定义为<code>sealed</code>以防止在其他地方创建消息)。</p>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">sealed</span> <span class="class"><span class="keyword">trait</span> <span class="title">PiMessage</span></span></span><br><span class="line"><span class="comment">// 计算启动消息</span></span><br><span class="line"><span class="keyword">case</span> <span class="class"><span class="keyword">object</span> <span class="title">Calculate</span> <span class="keyword">extends</span> <span class="title">PiMessage</span></span></span><br><span class="line"><span class="comment">// 分配的任务，需要做几次投掷测试</span></span><br><span class="line"><span class="keyword">case</span> <span class="class"><span class="keyword">class</span> <span class="title">Work</span>(<span class="params">times: <span class="type">Int</span></span>) <span class="keyword">extends</span> <span class="title">PiMessage</span></span></span><br><span class="line"><span class="comment">// Worker一次任务计算结果，包含落在内切圆里的点的个数</span></span><br><span class="line"><span class="keyword">case</span> <span class="class"><span class="keyword">class</span> <span class="title">Result</span>(<span class="params">value: <span class="type">Int</span></span>) <span class="keyword">extends</span> <span class="title">PiMessage</span></span></span><br><span class="line"><span class="comment">// 总的计算结果，包含π的近似值与计算所花的时间</span></span><br><span class="line"><span class="keyword">case</span> <span class="class"><span class="keyword">class</span> <span class="title">PiApproximation</span>(<span class="params">pi: <span class="type">Double</span>, duration: <span class="type">Duration</span></span>)</span></span><br><span class="line">    <span class="keyword">extends</span> <span class="type">PiMessage</span></span><br></pre></td></tr></table></figure>
<p>下面定义 worker actor，收到分发的任务后，通过随机掷点的方法统计落在内切圆中点个数。</p>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Worker</span> <span class="keyword">extends</span> <span class="title">Actor</span> </span>&#123;</span><br><span class="line">  <span class="comment">// 蒙特卡洛测试</span></span><br><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">testMonteCarlo</span></span>(times: <span class="type">Int</span>): <span class="type">Int</span> = &#123;</span><br><span class="line">    <span class="keyword">var</span> acc = <span class="number">0</span></span><br><span class="line">    <span class="keyword">for</span> (i &lt;- <span class="number">0</span> until times) &#123;</span><br><span class="line">      <span class="keyword">val</span> x = random * <span class="number">2</span> - <span class="number">1</span> <span class="comment">//生成[-1,1]区间的横坐标</span></span><br><span class="line">      <span class="keyword">val</span> y = random * <span class="number">2</span> - <span class="number">1</span> <span class="comment">//生成[-1,1]区间的纵坐标</span></span><br><span class="line">      <span class="keyword">if</span> (x * x + y * y &lt; <span class="number">1</span>) acc += <span class="number">1</span></span><br><span class="line">    &#125;</span><br><span class="line">    acc</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">receive</span> </span>= &#123;</span><br><span class="line">    <span class="keyword">case</span> <span class="type">Work</span>(times) =&gt;</span><br><span class="line">      sender ! <span class="type">Result</span>(testMonteCarlo(times))</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>下面定义 master actor，主要是给一些worker分发计算任务，收集Worker的计算结果，然后计算出π的近似值，master包含三个参数，<code>nrOfWorkers</code>表示一共有几个Worker同时运行，<code>nrOfMessages</code>表示要发多少任务下去（一个worker有可能会运行多个任务），<code>times</code>表示Worker运行一次任务需要掷几次点。</p>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Master</span>(<span class="params">nrOfWorkers: <span class="type">Int</span>, nrOfMessages: <span class="type">Int</span>, times: <span class="type">Int</span></span>)</span></span><br><span class="line">  <span class="keyword">extends</span> <span class="type">Actor</span> &#123;</span><br><span class="line">  <span class="keyword">var</span> calculateSender: <span class="type">ActorRef</span> = _  <span class="comment">//保存计算发起者的ActorRef</span></span><br><span class="line">  <span class="keyword">var</span> acc: <span class="type">Int</span> = <span class="number">0</span></span><br><span class="line">  <span class="keyword">var</span> nrOfResults: <span class="type">Int</span> = <span class="number">0</span></span><br><span class="line">  <span class="keyword">val</span> start: <span class="type">Long</span> = <span class="type">System</span>.currentTimeMillis</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 创建一个路由actor来分发任务</span></span><br><span class="line">  <span class="keyword">val</span> workerRouter = context.actorOf(</span><br><span class="line">    <span class="type">Props</span>[<span class="type">Worker</span>].withRouter(<span class="type">RoundRobinRouter</span>(nrOfWorkers)),</span><br><span class="line">    name = <span class="string">"workerRouter"</span>)</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">receive</span> </span>= &#123;</span><br><span class="line">    <span class="keyword">case</span> <span class="type">Calculate</span> =&gt;</span><br><span class="line">      <span class="keyword">for</span> (i &lt;- <span class="number">0</span> until nrOfMessages) workerRouter ! <span class="type">Work</span>(times)</span><br><span class="line">      calculateSender = sender</span><br><span class="line"></span><br><span class="line">    <span class="keyword">case</span> <span class="type">Result</span>(value) =&gt;</span><br><span class="line">      acc += value</span><br><span class="line">      nrOfResults += <span class="number">1</span></span><br><span class="line">      <span class="keyword">if</span> (nrOfResults == nrOfMessages) &#123;</span><br><span class="line">        <span class="comment">// 所有Worker工作完毕，计算π的近似值，并给发起者回复</span></span><br><span class="line">        <span class="keyword">val</span> pi = (<span class="number">4.0</span> * acc) / (nrOfMessages * times)</span><br><span class="line">        calculateSender ! <span class="type">PiApproximation</span>(pi,</span><br><span class="line">          (<span class="type">System</span>.currentTimeMillis - start).millis)</span><br><span class="line">      &#125;   </span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>定义完所有actor的功能后，我们可以开始编写入口程序，来使用这些actor完成π的近似计算了，程序首先创建 master actor的引用，发送计算请求消息，等待计算结果，注意这里是通过ask的方式（？标识符）发送消息，前面我们是通过tell（！标识符）方式发送消息，这两种方式的区别在于，tell方式发送消息时发出后不管的，一般在actor之间用这种方式发消息，reply消息可以通过tell方式发回来，而ask方式发送消息是期望得到一个结果的，其返回类型是<code>Future</code>类型，最后做一个转换，得到所期望的消息，如下代码是通过<code>Await</code>阻塞等待结果。</p>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">object</span> <span class="title">Pi</span> </span>&#123;</span><br><span class="line">  <span class="keyword">implicit</span> <span class="keyword">val</span> timeout = <span class="type">Timeout</span>(<span class="number">100</span> seconds)</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">main</span></span>(args: <span class="type">Array</span>[<span class="type">String</span>]) &#123;</span><br><span class="line">    <span class="keyword">if</span> (args.length &lt; <span class="number">3</span>) &#123;</span><br><span class="line">      <span class="type">System</span>.err.println(<span class="string">"Usage: Pi &lt;nrOfWorkers&gt; &lt;nrOfMessages&gt; &lt;times&gt;"</span>)</span><br><span class="line">      <span class="type">System</span>.exit(<span class="number">1</span>)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">val</span> system = <span class="type">ActorSystem</span>(<span class="string">"PiSystem"</span>)</span><br><span class="line">    <span class="keyword">val</span> master = system.actorOf(<span class="type">Props</span>(<span class="keyword">new</span> <span class="type">Master</span>(</span><br><span class="line">      args(<span class="number">0</span>).toInt, args(<span class="number">1</span>).toInt, args(<span class="number">2</span>).toInt)),</span><br><span class="line">      name = <span class="string">"master"</span>)</span><br><span class="line">    <span class="keyword">val</span> future = master ? <span class="type">Calculate</span></span><br><span class="line">    <span class="keyword">val</span> approximationPi = <span class="type">Await</span>.result(future, timeout.duration)</span><br><span class="line">                               .asInstanceOf[<span class="type">PiApproximation</span>]</span><br><span class="line">    println(<span class="string">"Pi: \t"</span> + approximationPi.pi)</span><br><span class="line">    println(<span class="string">"Spend: \t"</span> + approximationPi.duration)</span><br><span class="line">    system.shutdown()</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>我们再把上述代码重新整理一遍，在项目代码目录<code>src/main/scala</code>中新建文件<code>Pi.scala</code>。</p>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> scala.math.random</span><br><span class="line"><span class="keyword">import</span> scala.concurrent.<span class="type">Await</span></span><br><span class="line"><span class="keyword">import</span> scala.concurrent.duration._</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> akka.actor._</span><br><span class="line"><span class="keyword">import</span> akka.routing.<span class="type">RoundRobinRouter</span></span><br><span class="line"><span class="keyword">import</span> akka.pattern.ask</span><br><span class="line"><span class="keyword">import</span> akka.util.<span class="type">Timeout</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">object</span> <span class="title">Pi</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">sealed</span> <span class="class"><span class="keyword">trait</span> <span class="title">PiMessage</span></span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">case</span> <span class="class"><span class="keyword">object</span> <span class="title">Calculate</span> <span class="keyword">extends</span> <span class="title">PiMessage</span></span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">case</span> <span class="class"><span class="keyword">class</span> <span class="title">Work</span>(<span class="params">times: <span class="type">Int</span></span>) <span class="keyword">extends</span> <span class="title">PiMessage</span></span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">case</span> <span class="class"><span class="keyword">class</span> <span class="title">Result</span>(<span class="params">value: <span class="type">Int</span></span>) <span class="keyword">extends</span> <span class="title">PiMessage</span></span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">case</span> <span class="class"><span class="keyword">class</span> <span class="title">PiApproximation</span>(<span class="params">pi: <span class="type">Double</span>, duration: <span class="type">Duration</span></span>)</span></span><br><span class="line">    <span class="keyword">extends</span> <span class="type">PiMessage</span></span><br><span class="line"></span><br><span class="line">  <span class="class"><span class="keyword">class</span> <span class="title">Worker</span> <span class="keyword">extends</span> <span class="title">Actor</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">testMonteCarlo</span></span>(times: <span class="type">Int</span>): <span class="type">Int</span> = &#123;</span><br><span class="line">      <span class="keyword">var</span> acc = <span class="number">0</span></span><br><span class="line">      <span class="keyword">for</span> (i &lt;- <span class="number">0</span> until times) &#123;</span><br><span class="line">        <span class="keyword">val</span> x = random * <span class="number">2</span> - <span class="number">1</span></span><br><span class="line">        <span class="keyword">val</span> y = random * <span class="number">2</span> - <span class="number">1</span></span><br><span class="line">        <span class="keyword">if</span> (x * x + y * y &lt; <span class="number">1</span>) acc += <span class="number">1</span></span><br><span class="line">      &#125;</span><br><span class="line">      acc</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">receive</span> </span>= &#123;</span><br><span class="line">      <span class="keyword">case</span> <span class="type">Work</span>(times) =&gt;</span><br><span class="line">        sender ! <span class="type">Result</span>(testMonteCarlo(times))</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="class"><span class="keyword">class</span> <span class="title">Master</span>(<span class="params">nrOfWorkers: <span class="type">Int</span>, nrOfMessages: <span class="type">Int</span>, times: <span class="type">Int</span></span>)</span></span><br><span class="line">    <span class="keyword">extends</span> <span class="type">Actor</span> &#123;</span><br><span class="line">    <span class="keyword">var</span> calculateSender: <span class="type">ActorRef</span> = _</span><br><span class="line">    <span class="keyword">var</span> acc: <span class="type">Int</span> = <span class="number">0</span></span><br><span class="line">    <span class="keyword">var</span> nrOfResults: <span class="type">Int</span> = <span class="number">0</span></span><br><span class="line">    <span class="keyword">val</span> start: <span class="type">Long</span> = <span class="type">System</span>.currentTimeMillis</span><br><span class="line"></span><br><span class="line">    <span class="keyword">val</span> workerRouter = context.actorOf(</span><br><span class="line">      <span class="type">Props</span>[<span class="type">Worker</span>].withRouter(<span class="type">RoundRobinRouter</span>(nrOfWorkers)),</span><br><span class="line">      name = <span class="string">"workerRouter"</span>)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">receive</span> </span>= &#123;</span><br><span class="line">      <span class="keyword">case</span> <span class="type">Calculate</span> =&gt;</span><br><span class="line">        <span class="keyword">for</span> (i &lt;- <span class="number">0</span> until nrOfMessages) workerRouter ! <span class="type">Work</span>(times)</span><br><span class="line">        calculateSender = sender</span><br><span class="line"></span><br><span class="line">      <span class="keyword">case</span> <span class="type">Result</span>(value) =&gt;</span><br><span class="line">        acc += value</span><br><span class="line">        nrOfResults += <span class="number">1</span></span><br><span class="line">        <span class="keyword">if</span> (nrOfResults == nrOfMessages) &#123;</span><br><span class="line">          <span class="keyword">val</span> pi = (<span class="number">4.0</span> * acc) / (nrOfMessages * times)</span><br><span class="line">          calculateSender ! <span class="type">PiApproximation</span>(pi,</span><br><span class="line">            (<span class="type">System</span>.currentTimeMillis - start).millis)</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">implicit</span> <span class="keyword">val</span> timeout = <span class="type">Timeout</span>(<span class="number">100</span> seconds)</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">main</span></span>(args: <span class="type">Array</span>[<span class="type">String</span>]) &#123;</span><br><span class="line">    <span class="keyword">if</span> (args.length &lt; <span class="number">3</span>) &#123;</span><br><span class="line">      <span class="type">System</span>.err.println(<span class="string">"Usage: Pi &lt;nrOfWorkers&gt; &lt;nrOfMessages&gt; &lt;times&gt;"</span>)</span><br><span class="line">      <span class="type">System</span>.exit(<span class="number">1</span>)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">val</span> system = <span class="type">ActorSystem</span>(<span class="string">"PiSystem"</span>)</span><br><span class="line">    <span class="keyword">val</span> master = system.actorOf(<span class="type">Props</span>(<span class="keyword">new</span> <span class="type">Master</span>(</span><br><span class="line">      args(<span class="number">0</span>).toInt, args(<span class="number">1</span>).toInt, args(<span class="number">2</span>).toInt)),</span><br><span class="line">      name = <span class="string">"master"</span>)</span><br><span class="line">    <span class="keyword">val</span> future = master ? <span class="type">Calculate</span></span><br><span class="line">    <span class="keyword">val</span> approximationPi = <span class="type">Await</span>.result(future, timeout.duration)</span><br><span class="line">      .asInstanceOf[<span class="type">PiApproximation</span>]</span><br><span class="line">    println(<span class="string">"Pi: \t"</span> + approximationPi.pi)</span><br><span class="line">    println(<span class="string">"Spend: \t"</span> + approximationPi.duration)</span><br><span class="line">    system.shutdown()</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>用sbt编译运行项目，并可以看到打印的计算结果。</p>
<pre><code>[hadoop@master akka-actor-pi]$ sbt compile
[hadoop@master akka-actor-pi]$ sbt
[info] Loading project definition from /home/hadoop/project/akka-actor-pi/project
[info] Set current project to akka-actor-pi (in build file:/home/hadoop/project/akka-actor-pi/)
&gt; run 4 10000 10000
[info] Running Pi 4 10000 10000
Pi:     3.1415648
Spend:  9678 milliseconds
[success] Total time: 10 s, completed Dec 12, 2014 10:22:06 PM
</code></pre><p><span style="color:red"><em>转载请注明出处，本文永久链接：<a href="http://sharkdtu.com/posts/start-akka-actor.html">http://sharkdtu.com/posts/start-akka-actor.html</a></em></span></p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[Spark源码编译并在YARN上运行WordCount实例]]></title>
      <url>http://sharkdtu.com/posts/spark-build.html</url>
      <content type="html"><![CDATA[<p>在学习一门新语言时，想必我们都是”Hello World”程序开始，类似地，分布式计算框架的一个典型实例就是WordCount程序，接触过Hadoop的人肯定都知道用MapReduce实现WordCount，当前内存分布式计算框架Spark因为其计算速度之快，并且可以部署到Hadoop YARN中运行，已经受到各大公司的青睐，Spark社区提供了一些编译好的jar包，但是其中没有适配Hadoop-2.2.0的jar包，为了避免版本问题，需要自己编译指定hadoop版本的Spark jar包。下面介绍如何编译Spark源码并在YARN上运行WordCount程序。<a id="more"></a></p>
<p>Spark是由Scala编写的，虽然我们也可以通过Spark的Java或Python接口来编写应用程序，但是为了能更好的学习Spark，建议采用Scala来编写应用程序，Scala是一种函数式编程语言，其简洁和优雅的编程风格相信在不久后会让你喜欢上它的。之前我已经安装配置好Hadoop环境，请参考本站前面的一篇文章”<a href="/posts/centos-hadoop-setup.html">CentOS下Hadoop-2.2.0集群安装配置</a>“，下面我们来安装Scala编译环境。</p>
<h2 id="Scala安装配置"><a href="#Scala安装配置" class="headerlink" title="Scala安装配置"></a>Scala安装配置</h2><p>去<a href="http://www.scala-lang.org/" target="_blank" rel="external">Scala官网</a>下载Scala，我下载的是Scala-2.10.4（因为官方提供的spark-1.1.0编译好的jar包都是基于scala-2.10.4编译的），将下载后的软件包解压到用户根目录，并配置好环境变量。</p>
<pre><code>[hadoop@master ~]$ tar zxvf scala-2.10.4.tgz
[hadoop@master ~]$ vim ~/.bash_profile
 #添加Scala环境变量
 export SCALA_HOME=$HOME/scala-2.10.4
 export PATH=$SCALA_HOME/bin:$PATH
</code></pre><h2 id="Spark安装配置"><a href="#Spark安装配置" class="headerlink" title="Spark安装配置"></a>Spark安装配置</h2><p>去<a href="http://spark.apache.org/" target="_blank" rel="external">Spark社区</a>下载Spark源码，我下载的是spark-1.1.0版本，解压，并进入源码根目录，执行以下命令编译：</p>
<pre><code>[hadoop@master ~]$ tar zxvf spark-1.1.0.tgz
[hadoop@master ~]$ cd spark-1.1.0
[hadoop@master ~]$ sbt/sbt assembly -Pyarn -Phadoop-2.2 -Pspark-ganglia-lgpl -Pkinesis-asl -Phive
</code></pre><p>值得说明的是，我指定编译规则是集成hadoop2.2的，以便与我的Hadoop环境适配，这里需要等待漫长的编译过程…中途要下载大量的依赖包。编译完成后，可以在目录assembly中找到编译打包好的jar包：</p>
<pre><code>[hadoop@master scala-2.10]$ ls
spark-assembly-1.1.0-hadoop2.2.0.jar
[hadoop@master scala-2.10]$ pwd
/home/hadoop/spark-1.1.0/assembly/target/scala-2.10
</code></pre><p>下面我们配置Spark的环境变量：</p>
<pre><code>[hadoop@master ~]$ vim ~/.bash_profile
 #添加Spark环境变量
 export SPARK_HOME=$HOME/spark-1.1.0
 export PATH=$SPARK_HOME/bin:$PATH
</code></pre><p> 如何让Spark能知道我们的YARN在哪呢，这里需要在Spark配置文件中指定一下YARN的位置：</p>
<pre><code>[hadoop@master ~]$ cd $SPARK_HOME/conf/
[hadoop@master conf]$ cp spark-env.sh.template spark-env.sh
[hadoop@master conf]$ vim spark-env.sh
 #添加如下两行，指定你的java和yarn的位置
 export JAVA_HOME=/home/hadoop/jdk1.7.0_40/
 export YARN_CONF_DIR=/home/hadoop/hadoop-2.2.0/etc/hadoop/
</code></pre><p>环境都准备好了，下面我们介绍如何构建Spark WordCount项目，并将其部署到YARN上运行。</p>
<h2 id="构建WordCount项目"><a href="#构建WordCount项目" class="headerlink" title="构建WordCount项目"></a>构建WordCount项目</h2><p>这里采用sbt的方式来构建项目，需要满足sbt的项目目录结构（其中target目录是编译后自动生成的）：</p>
<pre><code>|--build.sbt
|--lib
|--project
|--src
|   |--main
|   |    |--scala
|   |--test
|         |--scala
|--sbt
|--target
</code></pre><p>先创建出项目的目录结构，并从spark目录中拷贝sbt构建工具和前面编译好的jar包：</p>
<pre><code>[hadoop@master ~]$ mkdir -p spark-wordcount/lib
[hadoop@master ~]$ mkdir -p spark-wordcount/project
[hadoop@master ~]$ mkdir -p spark-wordcount/src/main/scala
[hadoop@master ~]$ mkdir -p spark-wordcount/src/test/scala
[hadoop@master ~]$ cp -R $SPARK_HOME/sbt spark-wordcount/
[hadoop@master ~]$ cp -R $SPARK_HOME/assembly/target/scala-2.10/spark-assembly-1.1.0-hadoop2.2.0.jar spark-wordcount/lib/
</code></pre><p>创建sbt项目构建文件build.sbt，该文件指定项目名、scala版本以及声明依赖，但是本实例先不加入依赖，直接将依赖的jar包放到项目的lib目录下:</p>
<pre><code>[hadoop@master ~]$ cd spark-wordcount
[hadoop@master spark-wordcount]$ vim build.sbt
name := &quot;WordCount&quot;
[空行]
version := &quot;1.0.0&quot;
[空行]
scalaVersion := &quot;2.10.4&quot;
</code></pre><p>在project目录下新建文件build.properties，里面指定sbt的版本：</p>
<pre><code>[hadoop@master project]$ vim build.properties
sbt.version=0.13.5
</code></pre><p>到这里，WordCount项目的准备工作都已做好了，下面我们用scala来编写Spark WordCount程序，在目录src/main/scala/目录下新建文件WordCount.scala：</p>
<figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> org.apache.spark._</span><br><span class="line"><span class="keyword">import</span> <span class="type">SparkContext</span>._</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">object</span> <span class="title">WordCount</span> </span>&#123;</span><br><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">main</span></span>(args: <span class="type">Array</span>[<span class="type">String</span>]) &#123;</span><br><span class="line">    <span class="keyword">if</span> (args.length &lt; <span class="number">2</span>) &#123;</span><br><span class="line">      <span class="type">System</span>.err.println(<span class="string">"Usage: WordCount &lt;inputfile&gt; &lt;outputfile&gt;"</span>);</span><br><span class="line">      <span class="type">System</span>.exit(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">val</span> conf = <span class="keyword">new</span> <span class="type">SparkConf</span>().setAppName(<span class="string">"WordCount"</span>)</span><br><span class="line">    <span class="keyword">val</span> sc = <span class="keyword">new</span> <span class="type">SparkContext</span>(conf)</span><br><span class="line">    <span class="keyword">val</span> result = sc.textFile(args(<span class="number">0</span>))</span><br><span class="line">                   .flatMap(line =&gt; line.split(<span class="string">" "</span>))</span><br><span class="line">                   .map(word =&gt; (word, <span class="number">1</span>))</span><br><span class="line">                   .reduceByKey(_ + _)</span><br><span class="line">    result.saveAsTextFile(args(<span class="number">1</span>))</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>代码编写完毕后，开始编译打包，在项目根目录下用sbt编译打包：</p>
<pre><code>[hadoop@master spark-wordcount]$ sbt/sbt clean compile package
</code></pre><p>编译完成后，我们能看到会多出一个target目录，里面就包括了编译后的.class文件和打好的jar包：</p>
<pre><code>[hadoop@master spark-wordcount]$ sbt/sbt clean compile package
[hadoop@master spark-wordcount]$ ls target/scala-2.10/
classes  wordcount_2.10-1.0.0.jar
</code></pre><h2 id="在YARN上运行WordCount"><a href="#在YARN上运行WordCount" class="headerlink" title="在YARN上运行WordCount"></a>在YARN上运行WordCount</h2><p>在hdfs上首先上传一些待处理的文本：</p>
<pre><code>[hadoop@master spark-wordcount]$ $HADOOP_HOME/bin/hdfs dfs -put ~/test.txt input
</code></pre><p>编写如下启动脚本，将Spark WordCount提交到YARN上运行：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">#!/usr/bin/env bash</span><br><span class="line"></span><br><span class="line">PROJECT_HOME=/home/hadoop/project/spark-wordcount</span><br><span class="line">#以防万一，确保删除输出目录</span><br><span class="line">$HADOOP_HOME/bin/hdfs dfs -rmr /user/hadoop/output</span><br><span class="line"></span><br><span class="line">$SPARK_HOME/bin/spark-submit \</span><br><span class="line">--class WordCount \</span><br><span class="line">--master yarn-cluster \</span><br><span class="line">--num-executors 2 \</span><br><span class="line">--driver-memory 1g \</span><br><span class="line">--executor-memory 1g \</span><br><span class="line">--executor-cores 1 \</span><br><span class="line">$PROJECT_HOME/target/scala-2.10/wordcount_2.10-1.0.0.jar \</span><br><span class="line">hdfs://master:9000/user/hadoop/input \</span><br><span class="line">hdfs://master:9000/user/hadoop/output</span><br></pre></td></tr></table></figure>
<p>这里采用的是yarn-cluster部署模式，num-executors指定启动的executor数量，driver-memory指定drive端所需内存大小，executor-memory指定executor所需内存大小，executor-cores指定每个executor使用的内核数。运行如上脚本后，打开Hadoop的web页面可以看到运行正常，如下图所示：</p>
<p><img src="/images/spark-on-yarn.png" alt="spark-on-yarn"></p>
<p><span style="color:red"><em>转载请注明出处，本文永久链接：<a href="http://sharkdtu.com/posts/spark-build.html">http://sharkdtu.com/posts/spark-build.html</a></em></span></p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[《活着》–让人瞬间安静的一本书]]></title>
      <url>http://sharkdtu.com/posts/to-live.html</url>
      <content type="html"><![CDATA[<p><img src="/images/to-live.jpg" alt="to-live"></p>
<p>之前是看了电影版的《活着》，不禁感慨老谋子前期的作品还是非常用心的，一般来说，被禁播的电影一定是非常值得看的，因为这类电影往往是不受Party意识形态的约束，自由地讲述故事。从这部电影中我们能清晰地看到，从民国到解放、从大跃进到文革时期的中国普通老百姓的生活状况，个人觉得不失为中小学的教育素材。当时看电影时就哽咽数次，如今现在工作找的差不多了，所以想到要把原著好好读一读，读完发现电影与原著还是有些差别，但基本路子还在，不知道是不是被阉割数次后结果。<a id="more"></a></p>
<p>书中讲述主人公福贵从风光到破产再到艰难，展现了民国时期地主和解放后普通老百姓的生活现状，作者给我们讲述了人们纷纷在“活着”的信念下一一死去，这大概是“活着”书名的由来。刚开始读的时候，给人的第一感觉是抨击当时地主的那种骄奢淫逸的生活，当主人公把家产输光破产后，又营造出一种让人怜悯的氛围，接下来就是讲述与主人公有关的亲人一一死去的故事，在每个人死去之前都给死者一个人生小巅峰，让读者跟着欣慰一小下。</p>
<p>第一个死去的是主人公福贵的儿子有庆，这大概是我看到一半的时候，当时看后就觉得心寒，放下书发了个朋友圈，整个人都安静很多了，在这之前，作者还假装让有庆得个长跑第一名，然后就毫不犹豫的将其写死，当然这一过程是做了大量的铺垫，漓淋尽致地反映出当时的社会状况。当时正处大跃进时期，全国上下大张旗鼓地炼钢铁，而大多数人对于炼钢缺乏常识，书中描述福贵他们村挨家挨户的轮流守炉，最开始由于不知道该怎么炼，因为有庆天真的说了句需要加水才能煮钢铁，大伙儿就纷纷不停地加水，个个跟打了鸡血似的天天在那煮钢铁，还是有一次机缘巧合，福贵媳妇儿家珍累地忘了加水，才稍微将钢铁给煮成了，这足以让我们看到大跃进时期人们的愚昧无知。另外，在当时，底层老百姓生活疾苦，在政府看来，老百姓的生也不值一文，年仅12岁的有庆每天起早摸黑的去给羊割草喂食，为了不磨损鞋底，光着脚跑去上学，无论严寒地洞，最后为了抢着给县长夫人献血，被无良医生将血抽光，不幸死去，这在当时福贵和家珍只能跟着哭，却毫无反抗说理之力，最后也只能是感慨有庆命不好。</p>
<p>福贵的女儿凤霞在一次高烧中落下了不能说话的毛病，一直以来是听话懂事，为家里分担大大小小的活，说到这里，得提一句我姐，小时候也是因为家里比较苦，为了减轻家里的负担，辍学出去打工（也有可能是她学习太差真心不想读:)），在我读书期间给了我很多的关怀和帮助，现在常常觉得有姐真心是件很幸福的事。有点跑偏了，回到凤霞的故事，凤霞的死是正值文革时期，全国上下铺天盖地的打斗，哪哪都是宣传报毛主席像，凤霞在这个时候却很幸运的嫁出去了，福贵和家珍都为她高兴，对方是个老老实实的工人阶层，对福贵和家珍也非常孝顺，看到这里，心中不由地会有些小欣慰，紧接着凤霞怀有孩子，给一家子掀起了一个小高潮，然而，作者真的是毫不留情，就在凤霞生孩子的时候，由于大出血不幸的死去了，所幸孩子还在，这一段在书中貌似没过多的描述，在电影里是这样的一个镜头，凤霞在医院生产时，当时医院的一些医生大夫都被打倒为走资派，整个医院被红卫兵小护士占领，凤霞生完孩子后大出血，然而这个时候医院却没有权威的或者懂得处理的医生，导致不幸丧命。当时整个社会一片混乱，知识分子个个都被打倒，甚至一直任劳任怨的村里队长也被他们一句话就视为走资派，毫无反抗之力。然而福贵和家珍还是一样只能感慨凤霞的命不好。</p>
<p>家珍一直是拖着病干活，只为了多赚几分工，在当时，如果不能干活，则会被人说闲话，自己也找不到任何存在感，终于家珍也倒下了，一辈子任劳任怨，没过过几天好日子，想想下自己的妈妈，又何尝不是像家珍一样的为家里上上下下操着一辈子的心呢。后来福贵的女婿也在一次工作事故中不幸身亡，留下福贵和小外孙，紧接着小外孙也因为发高烧吃坏了东西离开了，越是看到后面，心里越是一阵阵刺痛，有着这等活着的信念，却还是不能逃离死神。</p>
<p>通读整篇小说，看到一个个对活着充满希望的人一一死去，不禁感慨作者的血腥，然而正是由于作者这种无情的写法，给我们展现了民国后期到文革结束这一段时间中国老百姓的疾苦生活，同时抨击了大跃进、文化大革命给人民带来的种种伤痛，让我们这些未曾经历那个时代的人也能感受到当时的种种境况，真为作者叫好。</p>
<p><span style="color:red"><em>转载请注明出处，本文永久链接：<a href="http://sharkdtu.com/posts/to-live.html">http://sharkdtu.com/posts/to-live.html</a></em></span></p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[CentOS下Hadoop-2.2.0集群安装配置]]></title>
      <url>http://sharkdtu.com/posts/centos-hadoop-setup.html</url>
      <content type="html"><![CDATA[<p>对于一个刚开始学习Spark的人来说，当然首先需要把环境搭建好，再跑几个例子，目前比较流行的部署是Spark On Yarn，作为新手，我觉得有必要走一遍Hadoop的集群安装配置，而不仅仅停留在本地(local)模式下学习，因为集群模式下跨多台机器，环境相对来说更复杂，许多在本地(local)模式下遇不到的问题在集群模式下往往出现，下面将结合实际详细介绍在 CentOS-6.x 系统上 hadoop-2.2.0 的集群安装（其他Linux发行版无太大差别），最后运行WordCount程序以验证Hadoop集群安装是否成功。<a id="more"></a></p>
<h2 id="机器准备"><a href="#机器准备" class="headerlink" title="机器准备"></a>机器准备</h2><p>假设集群中有三台机器，机器可以为三台物理机或虚拟机，保证三台机器可以互相通信，其中一台机器作为master（运行NameNode和ResourceManager），另外两台机器作为slave或worker（运行DataNode和NodeManager）。下面我准备的机器相关配置如下，注意每台机器要保证用户名一致。</p>
<table>
<thead>
<tr>
<th style="text-align:center">主机名</th>
<th style="text-align:center">用户名</th>
<th style="text-align:center">IP 地址</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">master</td>
<td style="text-align:center">hadoop</td>
<td style="text-align:center">192.168.100.10</td>
</tr>
<tr>
<td style="text-align:center">slave1</td>
<td style="text-align:center">hadoop</td>
<td style="text-align:center">192.168.100.11</td>
</tr>
<tr>
<td style="text-align:center">slave2</td>
<td style="text-align:center">hadoop</td>
<td style="text-align:center">192.168.100.12</td>
</tr>
</tbody>
</table>
<h2 id="工具准备"><a href="#工具准备" class="headerlink" title="工具准备"></a>工具准备</h2><p>为了避免在三台机器中重复安装配置工作，我们可以只在master机器上做安装配置，然后直接将配置好的软件打包发到每台slave机器上解压即可，首先我们应配置master机器到其他机器ssh免密码登陆，这是所有后续安装工作的前提。</p>
<h3 id="1-配置host"><a href="#1-配置host" class="headerlink" title="1. 配置host"></a>1. 配置host</h3><p>在master机器中配置host，在/etc/hosts文件中添加以下配置：</p>
<pre><code>192.168.100.10     master
192.168.100.11     slave1
192.168.100.12     slave2
</code></pre><h3 id="2-配置master免密码登录"><a href="#2-配置master免密码登录" class="headerlink" title="2. 配置master免密码登录"></a>2. 配置master免密码登录</h3><p>首先运行如下命令生成公钥：</p>
<pre><code>[hadoop@master ~]$ ssh-keygen -t  rsa
</code></pre><p>将公钥拷贝到每台机器中，包括本机，以使得ssh localhost免密码登录：</p>
<pre><code>[hadoop@master ~]$ ssh-copy-id -i ~/.ssh/id_rsa.pub  hadoop@master
[hadoop@master ~]$ ssh-copy-id -i ~/.ssh/id_rsa.pub  hadoop@slave1
[hadoop@master ~]$ ssh-copy-id -i ~/.ssh/id_rsa.pub  hadoop@slave2
</code></pre><p>为了更好的管理集群，切换到root身份，重复上述ssh无密码设置过程，保证root身份也无能密码登录：</p>
<pre><code>[root@master ~]$ su root
[root@master ~]$ ssh-keygen -t  rsa
[root@master ~]$ ssh-copy-id -i ~/.ssh/id_rsa.pub  root@master
[root@master ~]$ ssh-copy-id -i ~/.ssh/id_rsa.pub  root@slave1
[root@master ~]$ ssh-copy-id -i ~/.ssh/id_rsa.pub  root@slave2
</code></pre><p>完成上述操作后，切换回hadoop用户，现在master机器可以ssh免密码的登录集群中每台机器，下面我们开始现在master机器中开始安装配置hadoop。</p>
<h2 id="JDK安装"><a href="#JDK安装" class="headerlink" title="JDK安装"></a>JDK安装</h2><p>从<a href="http://www.oracle.com/technetwork/articles/javase/index-jsp-138363.html" target="_blank" rel="external">oracle官网</a>下载jdk，放到<code>/home/hadoop</code>目录下（后续所有安装包默认安装在<code>/home/hadoop</code>目录下），我下载的版本为jdk1.7.0_40，解压后设置jdk的环境变量，环境变量最好不要设置为全局的（在/etc/profile中），只设置当前用户的环境变量即可.</p>
<pre><code>[hadoop@master ~]$ pwd
/home/hadoop
[hadoop@master ~]$ vim .bash_proflie
 # JAVA ENVIRONMENT
 export JAVA_HOME=$HOME/jdk1.7.0_40
 export PATH=$JAVA_HOME/bin:$PATH
 export CLASSPATH=.:$JAVA_HOME/lib/dt.jar:$JAVA_HOME/lib/tools.jar
[hadoop@master ~]$ source .bash_proflie
</code></pre><h2 id="Hadoop安装"><a href="#Hadoop安装" class="headerlink" title="Hadoop安装"></a>Hadoop安装</h2><p>从<a href="http://hadoop.apache.org/releases.html" target="_blank" rel="external">Apache官网</a>下载hadoop发行版，放到<code>/home/hadoop</code>目录下，我下载的版本为hadoop-2.2.0，解压软件包后，首先设置hadoop的环境变量。</p>
<pre><code>[hadoop@master ~]$ vim .bash_proflie
 # HADOOP ENVIRONMENT
 export HADOOP_HOME=$HOME/hadoop-2.2.0
 export HADOOP_MAPRED_HOME=$HADOOP_HOME
 export HADOOP_COMMON_HOME=$HADOOP_HOME
 export HADOOP_HDFS_HOME=$HADOOP_HOME
 export YARN_HOME=$HADOOP_HOME
 export HADOOP_CONF_DIR=$HADOOP_HOME/etc/hadoop
 export HDFS_CONF_DIR=$HADOOP_HOME/etc/hadoop
 export YARN_CONF_DIR=$HADOOP_HOME/etc/hadoop
 export HADOOP_LOG_DIR=$HADOOP_HOME/logs
[hadoop@master ~]$ source .bash_proflie
</code></pre><p>下面我们开始配置hadoop，进入hadoop的配置目录，首先我们先在<code>hadoop-env.sh</code>和<code>yarn-env.sh</code>中设置好jdk的路径，然后开始修改hadoop相关配置文件。</p>
<h3 id="配置hdfs"><a href="#配置hdfs" class="headerlink" title="配置hdfs"></a>配置hdfs</h3><p>在配置文件<code>hdfs-site.xml</code>中添加以下内容。</p>
<figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">configuration</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">property</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- hdfs地址 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">name</span>&gt;</span>fs.defaultFS<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">value</span>&gt;</span>hdfs://master:9000<span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">property</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- hdfs中每一个block所存的份数，我这里设置1份，默认是3份 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">name</span>&gt;</span>dfs.replication<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">value</span>&gt;</span>1<span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">property</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 开启hdfs web访问 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">name</span>&gt;</span>dfs.webhdfs.enabled<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">value</span>&gt;</span>true<span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">configuration</span>&gt;</span></span><br></pre></td></tr></table></figure>
<h3 id="配置yarn"><a href="#配置yarn" class="headerlink" title="配置yarn"></a>配置yarn</h3><p>为了能够运行MapReduce程序，需要让各个NodeManager在启动时加载shuffle server，Reduce Task通过该server从各个NodeManager上远程拷贝Map Task产生的中间结果。在配置文件<code>yarn-site.xml</code>中添加以下内容。</p>
<figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">configuration</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">property</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">name</span>&gt;</span>yarn.resourcemanager.hostname<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">value</span>&gt;</span>master<span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">property</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">name</span>&gt;</span>yarn.nodemanager.aux-services<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">value</span>&gt;</span>mapreduce_shuffle<span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">property</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">name</span>&gt;</span>yarn.nodemanager.aux-services.mapreduce.shuffle.class<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">value</span>&gt;</span>org.apache.hadoop.mapred.ShuffleHandler<span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">configuration</span>&gt;</span></span><br></pre></td></tr></table></figure>
<h3 id="配置MapReduce计算框架"><a href="#配置MapReduce计算框架" class="headerlink" title="配置MapReduce计算框架"></a>配置MapReduce计算框架</h3><p>为了利用MapReduce中的WordCount验证hadoop集群是否安装成功，需要为hadoop配置MapReduce计算框架。在配置文件<code>mapred-site.xml</code>中添加以下内容。</p>
<figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">configuration</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">property</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 指定yarn为MapReduce的资源调度平台 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">name</span>&gt;</span>mapreduce.framework.name<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">value</span>&gt;</span>yarn<span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">configuration</span>&gt;</span></span><br></pre></td></tr></table></figure>
<h3 id="配置slaves"><a href="#配置slaves" class="headerlink" title="配置slaves"></a>配置slaves</h3><p>在配置文件<code>slaves</code>中添加以下内容。</p>
<pre><code>slave1
slave2
</code></pre><p>到这里为止，我们已经完成master机器上hadoop的配置，更多关于hadoop的配置参数说明请查看<a href="(http://hadoop.apache.org/docs/r2.2.0/">官方文档</a>)左侧的Configuration一栏，下面我们要将master机器上所有的安装配置操作同步到集群中所有节点上，为了避免挨个节点的重复劳动，我们前面也设置好了ssh无密码登录，现在我们简单写几个脚本，完成同步安装操作。</p>
<h3 id="同步配置"><a href="#同步配置" class="headerlink" title="同步配置"></a>同步配置</h3><p>首先同步/etc/hosts文件，这个需要切换到root用户下来完成，运行如下脚本：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">#! /bin/bash</span><br><span class="line">for SLAVE in `cat /home/hadoop/hadoop-2.2.0/etc/hadoop/slaves`; do</span><br><span class="line">  if test `expr match $SLAVE &quot;*slave*&quot;` -eq 0; then</span><br><span class="line">    rsync /etc/hosts $SLAVE:/etc/hosts</span><br><span class="line">    #清空防火墙配置</span><br><span class="line">    iptables -F</span><br><span class="line">  fi</span><br><span class="line">done</span><br></pre></td></tr></table></figure>
<p>其中rsync为文件同步命令，每次配置作修改后可以通过rsync命令进行文件的同步操作。完成上述操作后，切换回hadoop用户下，运行如下脚本，完成hadoop的同步安装：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">#! /bin/bash</span><br><span class="line"></span><br><span class="line">for SLAVE in `cat $HADOOP_CONF_DIR/slaves`; do</span><br><span class="line">  if test `expr match $SLAVE &quot;*slave*&quot;` -eq 0; then</span><br><span class="line">    #拷贝jdk</span><br><span class="line">    scp /home/hadoop/jdk-7u40-linux-i586.gz  hadoop@$SLAVE:/home/hadoop/</span><br><span class="line">    #拷贝hadoop</span><br><span class="line">    scp /home/hadoop/hadoop-2.2.0.tar.gz     hadoop@$SLAVE:/home/hadoop/</span><br><span class="line">    #拷贝环境变量配置</span><br><span class="line">    scp /home/hadoop/.bash_profile           hadoop@$SLAVE:/home/hadoop/</span><br><span class="line"></span><br><span class="line">    echo &quot;set up $&#123;SLAVE&#125;...&quot;</span><br><span class="line">    ssh $SLAVE &quot;cd /home/hadoop;</span><br><span class="line">                tar zxvf jdk-7u40-linux-i586.gz;</span><br><span class="line">                tar zxvf hadoop-2.2.0.tar.gz;</span><br><span class="line">                source /home/hadoop/.bash_profile;</span><br><span class="line">                exit&quot;</span><br><span class="line">    rsync -r /home/hadoop/hadoop-2.2.0/etc/hadoop $SLAVE:/home/hadoop/hadoop-2.2.0/etc/hadoop</span><br><span class="line">  fi</span><br><span class="line">done</span><br></pre></td></tr></table></figure>
<p>每次修改hadoop配置后，运行如下脚本同步到集群所有节点：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">#! /bin/bash</span><br><span class="line">for SLAVE in `cat $HADOOP_CONF_DIR/slaves`; do</span><br><span class="line">  if test `expr match $SLAVE &quot;*slave*&quot;` -eq 0; then</span><br><span class="line">    rsync -r /home/hadoop/hadoop-2.2.0/etc/hadoop $SLAVE:/home/hadoop/hadoop-2.2.0/etc/hadoop</span><br><span class="line">  fi</span><br><span class="line">done</span><br></pre></td></tr></table></figure>
<p>到这里，hadoop的集群安装就完成了，我们启动hadoop环境，通过WordCount来测试一下集群，一次运行如下命令以启动hdfs和yarn。</p>
<h2 id="WordCount测试"><a href="#WordCount测试" class="headerlink" title="WordCount测试"></a>WordCount测试</h2><p>在master集群上启动hdfs和yarn后台进程：</p>
<pre><code>[hadoop@master ~]$ $HADOOP_HOME/bin/hadoop namenode -format
[hadoop@master ~]$ $HADOOP_HOME/sbin/start-dfs.sh
[hadoop@master ~]$ $HADOOP_HOME/sbin/start-yarn.sh
</code></pre><p>启动后可以通过 <a href="http://master:50070" target="_blank" rel="external">http://master:50070</a> 地址访问hdfs，通过 <a href="http://master:8088" target="_blank" rel="external">http://master:8088</a> 地址访问yarn，如果不能访问，请检查master机器上的iptables配置，将相关配置清除。另外，在启动集群过程中可能会遇到DataNode、NodeManager启动不起来的现象，这也可能是slave机器上的防火墙配置导致集群间RPC通信失败的缘故，比较暴力的做法是把集群中所有机器的防火墙服务直接给停掉，相关iptables防火墙操作请自行google，下面我们运行hadoop自带的WordCount实例来验证hadoop是否能正常工作。</p>
<p>首先准备输入文件，上传到hdfs：</p>
<pre><code>[hadoop@master ~]$ mkdir input
[hadoop@master ~]$ sh -c &apos;echo &quot;hello hadoop&quot; &gt; input/f1.txt&apos;
[hadoop@master ~]$ sh -c &apos;echo &quot;hello java&quot; &gt; input/f2.txt&apos;
[hadoop@master ~]$ sh -c &apos;echo &quot;hello world&quot; &gt; input/f3.txt&apos;
[hadoop@master ~]$ $HADOOP_HOME/bin/hdfs -mkdir /input
[hadoop@master ~]$ $HADOOP_HOME/bin/hdfs -put input/* /input/
</code></pre><p>运行WordCount Mapreduce实例：</p>
<pre><code>[hadoop@master ~]$ $HADOOP_HOME/bin/hadoop jar $HADOOP_HOME/share/hadoop/mapreduce/hadoop-examples-2.2.0.jar wordcount /input /output
</code></pre><p>作业提交后，可以访问web ui页面，观察MapReduce作业的运行情况。</p>
<p><span style="color:red"><em>转载请注明出处，本文永久链接：<a href="http://sharkdtu.com/posts/centos-hadoop-setup.html">http://sharkdtu.com/posts/centos-hadoop-setup.html</a></em></span></p>
]]></content>
    </entry>
    
  
  
</search>
